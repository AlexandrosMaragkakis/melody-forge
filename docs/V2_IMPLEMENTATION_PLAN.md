# Melody Forge V2 implementation plan

This checklist mirrors every stable ID in [V2_REQUIREMENTS.md](./V2_REQUIREMENTS.md). The status column is authoritative and uses only `not started`, `in progress`, `done`, or `blocked`. A checked box is permitted only when status is `done`; all other boxes remain unchecked.

For every milestone, complete rows in dependency order, run the relevant typecheck/lint/unit/build/browser subset, update the traceability matrix and `PROJECT_STATUS.md`, retain artifacts, fix regressions, and create a safe scoped checkpoint when authorized. A row may move only with its stable ID and must never be deleted to conceal unfinished work.

## 2026-08-10 post-coordinator checkpoint boundary

This checkpoint targets `v2` and was developed from clean fetched `v2` at
`19c9c356ddb3e1f27fb7e61344133c56fa35376e`. M1 is complete, M2 is partially
implemented, and M3 through M11 are unstarted. Integrated M2 code now also
includes the headless bootstrap coordinator behind the current reducer.
Rows remain `in progress`, not `done`, wherever UI, browser, export,
later-payload, or full-envelope evidence remains.

`src/persistence/v2/bootstrapCoordinator.ts` now loads preferences independently,
strict-loads active V2 first, otherwise migrates an untouched V1 source through
both equivalence gates and two-phase activation, or installs the exact fresh
kernel. Its 16 focused tests prove preference-read isolation, blocked/unavailable
storage handling, crash retry, pending/verified resume, revision/collision/abort
failure propagation, stale-CAS protection, both mismatch gates, invalid-source
preservation, and no V1 writes. It remains intentionally disconnected from
`App` and the V1 reducer.
Candidate Save/Seed, nonempty undo, later mode/entity codecs, M11 envelopes,
recovery UI, and real-browser IndexedDB evidence remain deferred. Stop again
before M3. The checkpoint commit is the commit containing this note; the final
handoff records its exact resolved SHA.

## Milestone 1 — V1 archaeology, fixtures, specifications, and migration design

Exit gate: V1 is fully inventoried and baselined, immutable legacy hashes and golden fixtures exist, migration decisions are documented, and no V2 product code starts without a recoverable baseline.

| Check | ID | Status | Deliverable and UI | Automated evidence | Browser evidence |
| --- | --- | --- | --- | --- | --- |
| [x] | `V1-001` | done | Read every repository instruction, document, plan, ADR, and status file before modifying product code. Implementation: `docs/V1_COMPATIBILITY_BASELINE.md`; repository archaeology. UI: N/A. | Complete authored-document inventory recorded | N/A |
| [x] | `V1-002` | done | Inspect and record initial git status; preserve unrelated user changes. Implementation: `PROJECT_STATUS.md`; `docs/V1_COMPATIBILITY_BASELINE.md`. UI: N/A. | Clean `81209c5…` baseline record and worktree audit | N/A |
| [x] | `V1-003` | done | Record a complete sorted SHA-256 manifest for `/legacy`, detecting additions/removals as well as changed bytes. Implementation: `docs/LEGACY_SHA256.txt`. UI: N/A. | Manifest comparison command/test | N/A |
| [x] | `V1-005` | done | Run and record the complete V1 unit/integration suite baseline. Implementation: `PROJECT_STATUS.md`; `docs/V1_COMPATIBILITY_BASELINE.md`. UI: N/A. | Pre-change `npm test`: 16 files / 148 tests passed | N/A |
| [x] | `V1-006` | done | Run and record V1 typecheck, lint, and production build baselines. Implementation: `PROJECT_STATUS.md`; `docs/V1_COMPATIBILITY_BASELINE.md`. UI: N/A. | Pre-change typecheck/lint/build all passed; 978 build modules | N/A |
| [x] | `V1-007` | done | Run and record the complete V1 browser-E2E baseline. Implementation: `PROJECT_STATUS.md`; Playwright artifacts. UI: Existing V1 workspace. | Initial suite 2/2 plus focused V1 compatibility suite 2/2 | Desktop/mobile runs with console audit |
| [x] | `V1-008` | done | Launch V1 and inspect every existing workflow in a real browser. Implementation: `docs/MANUAL_QA.md`; `docs/V1_COMPATIBILITY_BASELINE.md`; V1 E2E specs. UI: Existing V1 workspace. | Browser assertions for persisted data/IDs/ticks | Legacy, Modern, playback, evolution, favorite, history, JSON, MIDI at desktop/mobile |
| [x] | `V1-009` | done | Capture representative desktop and narrow-viewport baseline screenshots. Implementation: `screenshots/v1-baseline-*.png`. UI: Existing V1 workspace. | Four retained screenshot hashes recorded | Visual inspection at desktop/mobile |
| [x] | `V1-010` | done | Inventory domain types, Legacy/Modern/evolution versions, storage keys, project/favorite/history schemas, JSON envelopes, and MIDI behavior. Implementation: `docs/V1_COMPATIBILITY_BASELINE.md`; `docs/V2_ARCHITECTURE.md`. UI: N/A. | Inventory reviewed against source constants and adapters | N/A |
| [x] | `V1-011` | done | Create golden fixtures for real V1 projects and individual exported JSON. Implementation: `src/test/fixtures/v1/`; `src/persistence/v1Fixtures.test.ts`. UI: Project import UI. | Static manifest and decode/re-encode tests pass | Golden project imported in both Chromium projects |
| [x] | `V1-012` | done | Create golden fixtures for V1 favorites and library migration. Implementation: `src/test/fixtures/v1/project-favorites-only.v1.json`; project fixture. UI: V1 Favorites / future Library. | Favorite identity and standalone-history tests pass | Imported favorites expanded and Legacy favorite auditioned at desktop/mobile |
| [x] | `V1-013` | done | Create golden fixtures for arbitrary-length and partial-bar phrases, including a five-beat phrase. Implementation: Modern/Legacy five-beat JSON and MIDI fixtures. UI: V1 candidate timing / future timeline. | Exact 2,400-tick events, stored pitches, and MIDI hash tests pass | Five-beat Modern and imported Legacy material auditioned at desktop/mobile |
| [x] | `V1-014` | done | Create golden fixtures for V1 evolutionary histories, including retained elites and ancestry. Implementation: `project-five-beat-history.v1.json`. UI: V1 Earlier/Later / future History strip. | Stable snapshot/candidate/elite ID assertions pass | Imported generation 0/1 history navigated at desktop/mobile |
| [x] | `V1-015` | done | Preserve existing V1 documentation/history; add V2 documents rather than replacing or rewriting it. Implementation: Existing `README.md`, `docs/PRODUCT_SPEC.md`, `docs/LEGACY_BEHAVIOR.md`, `docs/IMPLEMENTATION_PLAN.md`, ADR 0001. UI: N/A. | Documentation diff audit | N/A |
| [ ] | `DOC-001` | in progress | Maintain `docs/V2_PRODUCT_SPEC.md`. Implementation: `docs/V2_PRODUCT_SPEC.md`. UI: N/A. | Documentation presence/content audit | N/A |
| [ ] | `DOC-002` | in progress | Maintain `docs/V2_UI_SPEC.md`. Implementation: `docs/V2_UI_SPEC.md`. UI: N/A. | Documentation presence/content audit | N/A |
| [ ] | `DOC-003` | in progress | Maintain `docs/V2_ARCHITECTURE.md`. Implementation: `docs/V2_ARCHITECTURE.md`. UI: N/A. | Documentation presence/content audit | N/A |
| [ ] | `DOC-004` | in progress | Maintain this exhaustive traceability matrix with stable IDs, interpretation, implementation, UI, automated evidence, browser evidence, and status. Implementation: `docs/V2_REQUIREMENTS.md`; `docs/V2_REQUIREMENT_IDS.txt`. UI: N/A. | Exact seven-field/nonempty/status validation and locked-ID set audit | N/A |
| [ ] | `DOC-005` | in progress | Maintain a dependency-ordered granular implementation checklist whose milestone/ID order is locked by a reviewed raw-manifest count and SHA-256 and whose status, reconstructed deliverable/UI, automated evidence, and browser evidence exactly mirror every requirement row. Implementation: `docs/V2_IMPLEMENTATION_PLAN.md`; `docs/V2_REQUIREMENT_IDS.txt`; `scripts/check-v2-traceability.mjs`. UI: N/A. | Locked raw-manifest count/SHA-256, ordered milestone/ID, and exact cross-document field validation | N/A |
| [x] | `DOC-006` | done | Mirror the complete checklist in `PROJECT_STATUS.md` using only `not started`, `in progress`, `done`, and `blocked`, exactly one generated marker pair, root-correct links, and byte-exact generated content. Implementation: `PROJECT_STATUS.md`; `scripts/sync-v2-status.mjs`; `scripts/check-v2-traceability.mjs`. UI: N/A. | `npm run status:sync && npm run traceability:check` validates 691 locked IDs/statuses/checkboxes/fields across 11 milestones and an exact generated block | N/A |
| [ ] | `DOC-007` | not started | For completed status items, record files, tests, screenshots, browser evidence, and checkpoint SHA where applicable. Implementation: `PROJECT_STATUS.md`. UI: N/A. | Evidence-field audit | Artifact-link audit |
| [ ] | `DOC-008` | not started | For unfinished/blocked items, record exact remaining work and next action; never rename unfinished work as a limitation or remove checklist history. Implementation: `PROJECT_STATUS.md`. UI: N/A. | Status-history audit | N/A |
| [ ] | `DOC-011` | not started | Make safe, scoped local checkpoint commits when the initial worktree permits; record SHAs and exclude unrelated changes. Implementation: Git history; `PROJECT_STATUS.md`. UI: N/A. | Commit scope audit | N/A |
| [ ] | `BND-001` | not started | Remain a local-first monophonic melody laboratory. Implementation: Domain and architecture boundaries. UI: All destinations. | Monophony/offline tests | Offline browser workflow |
| [ ] | `BND-002` | not started | Keep melody, percussion, tonal context, instrument/performance sound, and audition pairing as separate domain concerns. Implementation: Planned separate domain modules. UI: Inspector and mode workspaces. | Separation/independence tests | Change each concern without changing others |
| [ ] | `BND-003` | not started | Do not add chords, chord progressions, harmony generation, or harmonic accompaniment. Implementation: Domain boundary. UI: N/A. | Forbidden-feature audit | UI audit |
| [ ] | `BND-004` | not started | Do not add polyphonic melody generation. Implementation: Melody invariants. UI: N/A. | Overlap/polyphony rejection tests | Import rejection/adaptation UI |
| [ ] | `BND-005` | not started | Do not become a DAW, general sequencer, manual drum-grid editor, full piano-roll note editor, notation engraver, recorder, or mixer. Implementation: Architecture/product docs. UI: All workspaces. | Feature-scope audit | UI audit |
| [ ] | `BND-006` | not started | Do not expose a general synthesizer editor. Implementation: Audio/performance boundary. UI: Sound inspector. | Component/API audit | UI audit |
| [ ] | `BND-007` | not started | Do not add ML/LLMs, embeddings, preference prediction, model training, or an automatic universal melody-quality score. Implementation: Algorithm boundaries. UI: Explore/Evolve explanations. | Dependency/score audit | Labels/explanations audit |
| [ ] | `BND-008` | not started | Do not add accounts, user management, cloud sync, sharing, analytics, telemetry, or a backend. Implementation: Architecture/build. UI: N/A. | Network/dependency audit | Offline/network audit |
| [ ] | `BND-009` | not started | Do not add a plugin framework, microservices, or speculative platform architecture. Implementation: Architecture. UI: N/A. | Dependency/source audit | N/A |
| [ ] | `ARC-001` | not started | Extend the current React/TypeScript/Vite architecture and stack; switch frameworks only with concrete documented impossibility evidence. Implementation: Existing stack; V2 ADRs. UI: Whole app. | Dependency/ADR audit | Existing workflows remain usable |
| [ ] | `ARC-002` | not started | Maintain explicit boundaries among melody domain, transport/timing, tonal theory/relationships, generators, evolutionary algorithms, descriptors/distances, percussion, playback/rendering, app state, persistence/migrations, I/O, visualization, and UI. Implementation: Planned modules/ADRs. UI: N/A. | Dependency-boundary tests/lint | N/A |
| [ ] | `ARC-003` | not started | Keep evolutionary/descriptor logic pure and independent of React, audio, storage, and browser APIs. Implementation: Planned pure modules. UI: N/A. | Node/purity/dependency tests | N/A |
| [ ] | `ARC-004` | not started | Use small strategy interfaces for six algorithms without creating a general plugin platform. Implementation: Planned strategy contracts. UI: Mode host. | Contract tests | Switch/use modes |
| [ ] | `ARC-005` | not started | Evaluate maintained audio, MIDI, chart, and evolutionary libraries only where they materially improve correctness; wrap each behind an adapter and record concise ADRs; third-party names never become domain identity. Implementation: Planned ADRs/adapters. UI: N/A. | Dependency/identity tests | Adapter-level workflows |

## Milestone 2 — Versioned transport, meter/bars, seed streams, and persistence foundations

Exit gate: The canonical tick transport, meter/partial-bar model, deterministic labelled seeds, and non-destructive versioned storage migration pass their foundation tests.

| Check | ID | Status | Deliverable and UI | Automated evidence | Browser evidence |
| --- | --- | --- | --- | --- | --- |
| [ ] | `TRN-001` | in progress | Introduce exactly one tick/PPQ transport authority for melody, beat, tempo, meter, bars/partial bars, loop boundaries, swing, playheads, MIDI, and WAV; ticks remain canonical rather than floating seconds. Implementation: `src/domain/transport/`; planned controller/render adapters. UI: Shared transport. | Pure authority, validation, meter, rational swing, and tick conversion tests pass; controller integration remains | Synced playback/playhead/export workflow remains |
| [ ] | `TRN-002` | in progress | Preserve the existing integer PPQ model or migrate through a compatible versioned equivalent. Implementation: `src/domain/transport/compatibility.ts`; planned V1 migrator. UI: Technical inspector. | Candidate-specific 96/480 PPQ profiles and exact rational adaptation previews pass; persisted migration remains | Audition/import V1 timing remains |
| [ ] | `TRN-003` | not started | Shared transport exposes play/pause. Implementation: Planned transport controller. UI: Sticky transport. | Controller state tests | Play/pause workflow |
| [ ] | `TRN-004` | not started | Shared transport exposes immediate stop. Implementation: Planned transport controller. UI: Sticky transport. | Cancellation tests | Stop during melody/beat/effects |
| [ ] | `TRN-005` | in progress | Shared transport displays current bar and beat. Implementation: `src/domain/transport/meter.ts`; planned transport UI. UI: Sticky transport. | Tick→bar/beat/group/subdivision and partial-bar position tests pass | Live position inspection remains |
| [ ] | `TRN-006` | in progress | Shared transport exposes loop on/off. Implementation: `src/domain/transport/`; planned controller/UI. UI: Sticky transport. | Half-open loop, wrap, and boundary domain tests pass; controller state remains | Loop partial/odd phrases remains |
| [ ] | `TRN-007` | in progress | Shared transport exposes canonical V2 tempo from exactly 30–300 BPM inclusive; compatibility-only V1 tempo remains governed by its separate profile. Implementation: `src/domain/transport/types.ts`; `validation.ts`; planned controller/UI. UI: Sticky transport. | Exact below/min/max/above/finite tempo validation and tick/seconds conversion tests pass; live synchronization remains | Change/reject tempo boundaries during playback remains |
| [ ] | `TRN-008` | in progress | Shared transport exposes meter. Implementation: `src/domain/transport/`; planned UI. UI: Sticky transport. | Meter construction, validation, grouping, and conversion tests pass | Change common/odd meters remains |
| [ ] | `TRN-009` | in progress | Shared transport exposes bars or exact loop length, including partial bars. Implementation: `src/domain/transport/meter.ts`; planned UI. UI: Sticky transport. | Exact-loop and clipped partial-bar partition tests pass | Five-beat and partial-bar workflow remains |
| [ ] | `TRN-010` | not started | Shared transport exposes master volume. Implementation: Planned audio bus state. UI: Sticky transport. | Volume state/audio tests | Adjust during playback |
| [ ] | `TRN-011` | not started | Shared transport exposes active melody voice. Implementation: Planned performance state. UI: Sticky transport. | Voice-selection tests | Switch voice without regeneration |
| [ ] | `TRN-012` | not started | Shared transport exposes beat mute/unmute. Implementation: Planned beat/audio state. UI: Sticky transport. | Mute state tests | Toggle during playback |
| [ ] | `TRN-013` | not started | Put swing, subdivision, effects, detailed sound, and advanced beat settings in popovers or inspector rather than crowding transport. Implementation: Planned popovers/inspector. UI: Transport/inspector. | Disclosure tests | Desktop/mobile popover checks |
| [ ] | `TRN-014` | in progress | Support 4/4 meter. Implementation: `src/domain/transport/presets.ts`; planned adapters/UI. UI: Transport/control rail. | Exact 4/4 2+2 and 1,920-tick fixture passes | 4/4 playback/grid/export remains |
| [ ] | `TRN-015` | in progress | Support 3/4 meter. Implementation: `src/domain/transport/presets.ts`; planned adapters/UI. UI: Transport/control rail. | Exact 3/4 3 and 1,440-tick fixture passes | 3/4 playback/grid/export remains |
| [ ] | `TRN-016` | in progress | Support compound 6/8 meter. Implementation: `src/domain/transport/presets.ts`; planned adapters/UI. UI: Transport/control rail. | Exact 6/8 3+3 and 1,440-tick fixture passes | 6/8 grouping/playback/grid/export remains |
| [ ] | `TRN-017` | in progress | Support 5/4 meter with meaningful grouping. Implementation: `src/domain/transport/presets.ts`; planned adapters/UI. UI: Transport/control rail. | Exact 5/4 3+2 and 2,400-tick fixture passes | 5/4 playback/grid/export remains |
| [ ] | `TRN-018` | in progress | Support 5/8 meter with meaningful grouping. Implementation: `src/domain/transport/presets.ts`; planned adapters/UI. UI: Transport/control rail. | Exact 5/8 2+3 and 1,200-tick fixture passes | 5/8 playback/grid/export remains |
| [ ] | `TRN-019` | in progress | Support 7/8 meter with meaningful grouping. Implementation: `src/domain/transport/presets.ts`; planned adapters/UI. UI: Transport/control rail. | Exact 7/8 2+2+3 and 1,680-tick fixture passes | 7/8 playback/grid/export remains |
| [ ] | `TRN-020` | not started | All directly compared candidates share transport, meter, loop duration, and bar grid. Implementation: Planned comparison-session contract. UI: Candidate comparison workspaces. | Compatibility/invariant tests | Audition comparison modes |
| [ ] | `TRN-021` | in progress | Incompatible imported/cross-bred timing gets an explicit adaptation preview; never silently stretch, trim, or distort. Implementation: `src/domain/transport/compatibility.ts`; planned general adaptation service. UI: Preview dialog/inspector. | Pure V1 PPQ boundary preview reports exact rationals/rounding/impossibility without apply; general timing cases remain | Incompatible-parent/import workflow remains |
| [ ] | `TRN-022` | not started | Tempo changes during playback keep melody, percussion, effects, and visual playhead synchronized. Implementation: Planned scheduler/clock. UI: Shared transport/piano roll. | Mid-play tempo synchronization tests | Live tempo change inspection |
| [ ] | `TRN-023` | not started | Starting another candidate atomically replaces the old schedule without overlap. Implementation: Planned scheduler/controller. UI: Candidate audition controls. | No-overlap/stale-token tests | Rapid candidate switching |
| [ ] | `TRN-024` | not started | Stop immediately cancels pending melody, beat, effects, and visual schedules. Implementation: Planned scheduler/controller. UI: Shared transport. | Cleanup/ghost-schedule tests | Stop/replay stress workflow |
| [ ] | `TRN-025` | in progress | Persist exactly one canonical `transport.swing`; transport More and Beat controls are synchronized views, and no beat genome, candidate, pattern, or performance preset may own a divergent swing value. Implementation: `src/domain/transport/`; planned state/UI adapters. UI: Transport More and Beat controls. | Exact rational swing/domain ownership tests pass; reducer/schema isolation tests remain | Edit from both surfaces and verify synchronized playback/export remains |
| [ ] | `TRN-026` | in progress | Persist exactly one canonical comparison/display grid as `transport.gridTicks`; rulers, comparison alignment, snapping proposals, descriptor opportunities, and default swing subdivision consume it, while source-grid metadata is provenance only. Implementation: `src/domain/transport/`; planned grid consumers. UI: Transport More, piano roll, import/adaptation preview. | Grid bounds/default domain tests pass; cross-consumer identity tests remain | Change grid and verify every view without rewriting event ticks remains |
| [ ] | `TRN-027` | in progress | Canonical V2 meter numerator is exactly 1–32, denominator is one of `1, 2, 4, 8, 16`, and there are at most 32 ordered positive beat groups whose sum equals the numerator. Implementation: `src/domain/transport/validation.ts`; planned meter UI/codecs. UI: Meter/grouping controls and errors. | Exact below/min/max/above numerator, denominator set, group-count/value/sum, and validator/constructor boundary tests pass; decoder/UI/codecs remain | Configure valid boundaries and reject each invalid meter remains |
| [ ] | `TRN-028` | in progress | Canonical V2 loop length is a positive safe integer spanning at most 256 derived `BarSpan` values including a partial final bar; loop start is exactly zero and no code rounds the endpoint to a full bar. Implementation: `src/domain/transport/validation.ts`; `meter.ts`; planned controls/codecs. UI: Bars/exact-loop controls, roll, errors. | Exact zero/max/plus-one, safe-integer, 256-bar, partial-final-bar, half-open partition, and no-rounding tests pass; UI/codecs remain | Configure full/partial max loops and reject overflow remains |
| [ ] | `TRN-029` | in progress | Canonical `gridTicks` is a positive safe integer dividing one denominator beat, and `ceil(loopEndTick / gridTicks)` is at most 65,536 grid opportunities. Implementation: `src/domain/transport/validation.ts`; planned grid consumers/UI. UI: Grid/loop controls, roll, import/adaptation errors. | Exact divisor/non-divisor and 65,536/65,537 opportunity boundary tests pass; consumer/UI allocation tests remain | Configure boundary grids/loops and inspect actionable rejection remains |
| [ ] | `TRN-030` | in progress | Every canonical V2 transport and new V2 creative operation uses exactly 480 PPQ; strict validators reject every other PPQ, while imported MIDI uses only previewed rational conversion and V1 alternate PPQ uses only a compatibility timing profile. Implementation: `src/domain/transport/validation.ts`; `compatibility.ts`; planned codecs/importers. UI: Technical inspector and adaptation preview. | Exact 480, alternate/unsafe PPQ, profile separation, and rational preview tests pass; full importer/codecs remain | Inspect canonical/compatibility PPQ and reject silent mixing remains |
| [ ] | `TRN-031` | in progress | V1 compatibility timing profiles preserve positive-safe-integer source PPQ/grid/ticks/loop and positive finite tempo exactly and are exempt, where preservation requires, from canonical tempo, meter, 256-bar, and 65,536-opportunity caps; they remain compatibility audition/display/V1-export only. Implementation: `src/domain/transport/compatibility.ts`; planned migration/export integration. UI: V1 timing badge/technical inspector. | Non-480/mixed and oversized-profile preservation, identity, validation, and source-byte tests pass; migration/export UI remains | Import/audition/export cap-exempt V1 sources remains |
| [ ] | `TRN-032` | in progress | Any compatibility source entering V2 comparison or creative work requires a previewed immutable 480-PPQ derivative; its source-boundary array is dense, ordered, and capped at exactly 258 entries—two endpoints for each of at most 128 frozen V1 events plus loop start/end—with sparse, unordered, invalid, or oversized direct input rejected before rational mapping/allocation; rounding/repair/collapse and canonical cap violations are reported, impossible adaptation is rejected atomically, and source/project bytes remain unchanged. Implementation: `src/domain/transport/compatibility.ts`; planned adaptation transaction/UI. UI: Adaptation preview/errors. | Dense/sparse and 258/259 source-boundary rejection plus exact/rational source/result-grid previews, collapse, incompatible/over-opportunity grid, 257-bar rejection, source immutability, and deterministic proposal tests pass; transaction/UI remains | Preview/cancel/reject/confirm representative sources remains |
| [ ] | `TRN-033` | in progress | Swing initialization uses `gridTicks` only when two grid subdivisions divide one denominator beat; otherwise a coarse grid initializes swing subdivision to half the denominator beat. Later grid and swing edits remain independent and each rejects its own divisor violations. Implementation: `src/domain/transport/validation.ts`; planned reducer/UI. UI: Grid and Swing controls in Transport More/Beat. | Fine/coarse initialization, independent-field, divisor, identity, and reset/adaptation tests pass at domain level; reducer/UI remain | Initialize/edit fine/coarse grid and swing from both surfaces remains |
| [ ] | `TRN-034` | in progress | Canonical swing amount is an integer from 500 permille straight through 750 inclusive; two positive-safe-integer swing subdivisions must divide one denominator beat, and incomplete final pairs remain straight without moving the loop endpoint. Implementation: `src/domain/transport/validation.ts`; `swing.ts`; planned controls/adapters. UI: Swing amount/subdivision controls and disclosure. | Exact 499/500/750/751, subdivision divisor, incomplete-tail, rational endpoint, and inverse tests pass; UI/adapters remain | Adjust boundary swing values and inspect tail/playback/export remains |
| [ ] | `TRN-035` | in progress | Exact rational score/performed transforms, loop wrapping, and inverse swing use zero tick tolerance; only final floating audio-clock/playhead conversion may clamp a closed endpoint within `1e-7` tick, while larger/out-of-loop values are rejected and the fake-clock gate remains within `1e-9` second. Implementation: `src/domain/transport/swing.ts`; `time.ts`; planned scheduler/playhead. UI: Technical timing evidence/playhead. | Exact rational endpoint/inverse and `5e-8` clamp/`2e-7` reject tests pass; fake-clock scheduler/browser gates remain | Inspect endpoint playhead and recorded clock tolerance remains |
| [ ] | `TRN-036` | in progress | A schema-valid V1 positive finite tempo outside canonical 30–300 BPM remains exact for compatibility audition/display/V1 export, but a canonical derivative reports `canonical-tempo-out-of-range` and cannot proceed until an explicit tempo adaptation is previewed and confirmed without mutating the source. Implementation: `src/domain/transport/compatibility.ts`; planned adaptation transaction/UI/export. UI: V1 timing badge and tempo adaptation preview. | Out-of-range compatibility-profile preservation and canonical rejection/proposal tests pass; compatibility audition/display, V1 export, and explicit tempo-adaptation transaction/UI remain | Audition/display/export out-of-range V1 tempo, then preview/cancel/confirm adaptation remains |
| [ ] | `V1-020` | in progress | Preserve every schema-valid positive safe-integer non-480 V1 `ticksPerBeat` and exact source tick in a candidate-specific compatibility profile; require previewed rational adaptation before any 480-PPQ V2 workflow. Implementation: `src/domain/transport/compatibility.ts`; `src/persistence/v2/v1Migration.ts`; planned export/UI integration. UI: Transport profile/adaptation preview. | Golden 96/480 profiles, rational proposals, mixed-PPQ conversion, strict graph validation, and equivalence tests pass; audition/export UI remains | Import/audition/re-export source and confirmation UI remain |
| [ ] | `V1-018` | in progress | Preserve exact event timing and audible output for arbitrary phrase lengths; represent a five-beat V1 phrase as an exact loop, using a partial bar or documented deterministic migration rather than forcing 4/4. Implementation: `src/persistence/v2/v1Migration.ts`; `v1Equivalence.ts`; transport compatibility profiles; planned V2 UI/export integration. UI: Shared transport and piano roll. | Five-beat fixture mapping preserves every tick, loop endpoint, MIDI, schedule, and direct V1 MIDI bytes; V2 UI/export remains | Play and inspect five-beat fixture |
| [ ] | `DATA-001` | in progress | Every normalized durable entity carries an embedded stable `id` and independent data `version`; transport, tonal timeline, custom scale, performance, and pairing references use IDs rather than containing-map identity. Implementation: `src/domain/transport/`; `src/domain/v2/`; registered M2 persistence boundary; planned later entity codecs. UI: Technical inspector. | Canonical transport and registered M2 row ID/version/identity/reference/closure tests pass; later entity codecs remain | Export/import and inspect IDs/versions remains |
| [ ] | `DATA-002` | in progress | Strict decoders validate normalized table key equals embedded ID, dispatch recorded schema/algorithm/render/descriptor/RNG/provenance versions independently, copy V1 IDs during migration, and exclude timestamps/UI ordering from creative identity. Implementation: `src/domain/v2/registry.ts`; `validation.ts`; `graph.ts`; `melody.ts`; `src/persistence/v2/m2PersistenceBoundary.ts`; planned M11 envelope codecs. UI: Import errors/technical inspector. | Registered M2 exact-key/version/identity/reference/closure and corruption tests pass; future payloads and complete M11 envelopes remain | Import mismatch/future version and reload stable identity |
| [ ] | `DATA-003` | in progress | Implement the exact schema-2 `ProjectV2` and `StoredProjectRecordV2` roots with their complete named field/type/reference keysets: project identity/version/schema/name/seed/algorithm/timestamps/destination/modes; nullable independent `comparisonTransportId` and `auditionTiming`; performance/beat/pairing, loop/mute/focus, Create/six mode/history/undo references; non-decreasing `nextPreferenceOccurrence`; unique occurrence-ordered immutable `preferenceRecordIds`; Unicode-ID-sorted unique selected-candidate/active-preference/rating/annotation/migration-receipt sets; unique order-bearing Library references; rating/annotation sets equal all and only table rows owned by this project, with kind-correct strong targets in the same graph, while globally reusable `MelodyCandidateV2` rows store neither rating nor annotation IDs and no metadata row is candidate-hidden, duplicated, or unreachable; and stored-record ID/version/revision/project with revision outside creative data. A non-null active pairing must exactly match focused melody, audition timing, active performance, and shared beat; compatibility audition uses its focused migrated candidate's profile, compatibility-performance singleton, and null pairing/shared beat while any retained comparison transport stays dormant; any redundant mismatch rejects the graph. New V2 projects set `comparisonTransportId` to the default transport and `auditionTiming={kind:'canonical-transport', transportId:comparisonTransportId}`; fresh V1 migration sets both null until compatibility focus; only durable playback configuration serializes, reload is stopped, and runtime clock/audio/scheduling state is forbidden. `algorithmVersions` is the exact closed, declared-order `AlgorithmVersionRegistryV2`: version `algorithm-version-registry-v2`; generators `{legacy:'legacy-simple-v1', modern:'modern-constrained-v1', v1Evolution:'interactive-evolution-v1'}`; foundations `{rng:'sfc32-v1', seedTree:'labelled-seed-tree-v2', transport:'transport-v2', v1CompatibilityTiming:'v1-compat-timing-v1', v1TimingAdaptation:'v1-timing-adaptation-proposal-v1', v1Migration:'v1-project-migration-v2.0.0', melodyRender:'melody-render-v2.0.0', candidateProvenance:'candidate-provenance-v2', preferenceEffect:'preference-effect-v2', beatGenerator:'beat-generator-v2.0.0'}`; strategies `{breed:'breed-strategy-v2.0.0', drift:'drift-strategy-v2.0.0', islands:'islands-strategy-v2.0.0', map:'map-elites-strategy-v2.0.0', pareto:'nsga2-pareto-strategy-v2.0.0', pairLab:'pair-lab-strategy-v2.0.0'}`; formulas `{customScaleDegeneracy:'custom-scale-degeneracy-v2.0.0', tonalRelationship:'tonal-relationship-v2.0.0', tonalMutationBands:'tonal-mutation-bands-v2.0.0', descriptorCore:'descriptor-core-v2.0.0', motifNgrams:'motif-ngrams-v2.0.0', rhythmEntropy:'rhythm-entropy-v2.0.0', meterSyncopation:'meter-syncopation-v2.0.0', pitchParentDistance:'pitch-parent-distance-v2.0.0', rhythmParentDistance:'rhythm-parent-distance-v2.0.0', tonalParentDistance:'tonal-parent-distance-v2.0.0', structuralDistance:'structural-distance-v2.0.0', beatDistance:'beat-distance-v2.0.0', phenotypeDistance:'phenotype-distance-v2.0.0', beatDescriptors:'beat-descriptors-v2.0.0', accentCoincidence:'accent-coincidence-v2.0.0', tonalAxis:'tonal-axis-v2.0.0', islandsGlobalDiversity:'islands-global-diversity-v2.0.0', pairPhenotypeDistance:'pair-phenotype-distance-v2.0.0', pairLabTransition:'pair-lab-transition-v2.0.0', operatorAttemptBudget:'operator-attempt-budget-v2.0.0'}`; and midiImport `{bounds:'midi-import-bounds-v2.0.0', monophonicExtraction:'midi-monophonic-extraction-v2.0.0'}`; no missing, extra, reordered, or substituted registry key/value is accepted. Implementation: `src/domain/v2/types.ts`; `project.ts`; `registry.ts`; `defaults.ts`; `graph.ts`; `src/persistence/v2/bootstrapCoordinator.ts`; planned App UI bootstrap. UI: New/open/migrated project bootstrap, transport/Technical inspector, and recovery UI. | `kernel.test.ts` passes exact fresh/migrated roots, defaults, registry, references, pairing redundancy, and strict reload; the headless coordinator passes active/fresh/migrated restore tests; App/UI and later payloads remain | Create and migrate projects, focus compatibility/canonical pairings, reload, and inspect exact durable root/playback/preference/version/metadata evidence and mismatch recovery |
| [ ] | `DATA-004A` | in progress | Freeze the M2 lock, Create, and mode-state support records in declared serialized key order. Lock kinds are exactly relative-pitch-shape, rhythm, contour, opening-event, closing-event, rest-positions, tonal-context, tonic, scale; event scope is exactly pitch, rhythm, or both. `ComponentLockV2`, `AbsolutePitchRequirementV2`/`AbsolutePitchLockV2`, `EventLockV2`, and `RegionLockV2` have their exact declared ID/version/kind-or-scope/event-or-region/captured-event/source-fingerprint fields; `LockSetV2` is exactly version `lock-set-v2`, component locks, nullable absolute lock, event locks, region locks. M2 melody registration accepts only migrated-v1 candidates with exactly empty lock arrays/null absolute lock, catalogue scale references, null borrowing policies and rendered-event borrowing, and empty custom-scale/beat tables; native candidates, nonempty locks, borrowed-pitch/custom-scale/beat provenance, and opaque future records reject until their owning milestone registers exact codecs. `LegacyCreateSettingsV2` is exactly tonic pitch class, scale ID, note count, tempo, population size, seed; `ModernCreateSettingsV2` is exactly tonic pitch class, scale ID, low/high register octave, note count, phrase beats, tempo, grid of 120/240/480 ticks, rest flag, max leap, tonic closure, population size, seed. `CreateStateV2` is exactly ID, version `create-state-v2`, project ID, active generator `legacy` or `modern`, and both settings records. `ModeStateV2` is exactly ID, version `mode-state-v2`, project ID, one of `breed`, `drift`, `islands`, `map`, `pareto`, `pair-lab`, and a registered payload. M2 registers only exact `{version:'empty-mode-state-v2', initialized:false}` and exact Breed payload version `breed-mode-state-v2.0.0` with initialized true, population/parent ID arrays, population size, mutation strength, elite flag, literal conservative-directed crossover, exact-deduplication/novelty flags, seed, generation ordinal. Empty is legal for any unmaterialized mode and Breed payload only for Breed; every later or unknown payload/key rejects until its milestone registers an exact semantic/reference codec. Create and mode IDs use their exact project-singleton formulas, mode identity includes mode, ownership must match the root, candidate arrays are duplicate-free in domain order, and parents are an order-bearing population subset of at most two. Fresh state is Legacy Create plus six empty modes; V1 maps both generator settings into Create and its evolution settings into Breed without inventing other mode state. Implementation: `src/domain/v2/locks.ts`; `state.ts`; `validation.ts`; `graph.ts`; `src/persistence/v2/v1Migration.ts`; planned later lock/mode codecs. UI: Create controls, mode host/bootstrap, migration preview, lock/Technical state inspector. | M2 empty-lock, Create, six empty-mode/Breed, identity, ownership, array, V1 mapping, and future-payload rejection tests pass; UI and later modes remain | Migrate locked/borrowed/catalogue/custom/beat boundary fixtures, inspect exact empty lock and rejection evidence, then create/open/switch/reload all six mode wrappers without mutation |
| [ ] | `DATA-004B` | in progress | Freeze the M2 normalized snapshot/history support records in declared key order. `SnapshotV2` is exactly ID, version `snapshot-v2`, project ID, native-v2 or migrated-v1 source kind, history mode, generation ordinal, seed, closed algorithm registry, candidate/selected/rating/annotation ID arrays, and registered payload; M2 registers only exact migrated-V1 payload version `migrated-v1-snapshot-payload-v2` with source seed, source generator version, and nullable exact V1 population/mutation/elite settings, so native/later payloads reject until registered. History modes are exactly Create, all six modes, import, and transform. `HistoryActionSummaryV2` is exact version `history-action-summary-v2` plus generate/evolve/promote-anchor/migrate-v1/import/transform kind. `HistoryGraphV2` is exactly ID/version/project, monotonic next occurrence, occurrence-ordered node IDs, relative-order root subset, nullable active node, and nullable exact V1 length/index source. `HistoryNodeV2` is exactly ID/version/project/graph, occurrence ordinal, nullable single parent, snapshot ID, mode, action, and nullable exact V1 source ordinal/snapshot/previous-generation/parent-resolution evidence. Native snapshot and graph/node IDs follow the specified content/singleton formulas while migrated snapshot IDs copy V1 and same-ID differing bytes reject. Graph ordinals are contiguous by node order, roots exactly null-parent nodes, every parent is earlier in the same graph, every node is reachable once, children are derived not serialized, and active is null exactly when empty. V1 migration keeps repeated equal snapshot memberships as distinct nodes, resolves a matching previous-generation ID to the greatest earlier ordinal or falls back to the preceding stored row, preserves source evidence and receipt ordinal mappings, and maps empty history to length zero/index -1/empty arrays/occurrence zero/null active. Implementation: `src/domain/v2/history.ts`; `state.ts`; `graph.ts`; `src/persistence/v2/v1Migration.ts`; planned native history codecs. UI: History strip/tree, migrated-history preview, restore controls, and Technical graph inspector. | Normalized migrated snapshot/history, repeated occurrence, ordering/parent/active, receipt mapping, malformed graph, and strict reload tests pass; native branch UI/codecs remain | Migrate empty/repeated/linked histories, inspect normalized branches and source evidence, reload/restore active nodes, and exercise malformed graph recovery |
| [ ] | `DATA-004C` | in progress | Freeze the M2 Library, rating, annotation, and undo support records in declared key order. `LibraryOriginReferenceV2` is exactly kind `project-save`, `v1-favorite`, or `json-import`, nullable project/history/source-hash fields, and source ID; `LibraryItemV2` is exactly ID, version `library-item-v2`, kind melody-candidate/beat/pairing, strong kind-correct component ID, name, note, favorite flag, nullable saved epoch, and origins. Component is a strong reference while origin project/history IDs are soft evidence so global rows survive their deletion. Names are at most 80 Unicode scalars, notes at most 280, and times are null or non-negative safe integers. For an absent item only, a migrated V1 favorite initializes exactly name `Untitled Melody`, empty note, favorite true, saved time null, and never reads a clock; explicit native/import Save initializes the same name/note, favorite false, and one injected safe-integer metadata-clock value, while Favorite initializes true. A same-ID Save/import/migration preserves existing name, note, favorite, and saved time byte-for-byte and stable-unions only origins; only an explicit edit or Favorite action changes editable metadata. Every origin retains all keys: project-save requires project ID and null source hash; V1-favorite and JSON-import require project ID plus source hash; history is nullable and other inapplicable fields are null. Origins are duplicate-free stableStringify-byte sorted. Library identity is `stableId('library-item',{version,kind,componentId})`; editable metadata/origins never rename it, same component ID plus equal immutable bytes merges origins, differing bytes abort, global rows survive removal of a project reference, and explicit deletion is separate. `RatingV2` and `AnnotationV2` are exact ID/version/project/target-kind/target-ID/value-or-text singletons for melody-candidate/beat/pairing strong targets; ratings are integers 1–5 with unrated represented by absence, annotation text is at most 280 Unicode scalars, and editable value/text is excluded from the specified stable identity. Globally reusable `MelodyCandidateV2` stores no rating/annotation IDs: in a project graph the root sets own all and only project metadata rows, while a standalone candidate envelope selects only rows targeting its root candidate and treats their source `projectId` as soft evidence until atomic Save/Seed rehomes singleton IDs into the target; an existing target value/text wins, source rows never persist, and candidate content/identity is never rewritten. `UndoStateV2` is exact ID/version/project/monotonic next occurrence/undo/redo stacks of exact occurrence/history/command/forward/inverse entries and exact target-kind/target-ID/field-path/before/after patches. At most 128 entries total are oldest-to-newest with stack top last; ordinals are unique, below next occurrence, and never reused; patch lists are nonempty reverse exact inverses using registered command/path semantics. Undo may toggle only active preference membership, never delete/reorder its immutable log or rewind its ordinal, and history navigation creates no undo entry. Implementation: `src/domain/v2/library.ts`; `state.ts`; `graph.ts`; `src/persistence/v2/indexedDbAuthority.ts`; planned nonempty undo/command registry and UI. UI: Library editor/Favorite/delete confirmation, rating/annotation inspector, undo/redo controls, migration preview, and Technical records. | Library defaults/origin merge/local-wins/collision/global survival, metadata targets, empty undo, and obsolete project-owned-row cleanup tests pass; nonempty undo and UI remain | Migrate favorite and native/import Save/Favorite with injected time, repeat same-ID Save/import/migration and verify metadata preservation/origin union, explicitly edit, remove, and delete Library items, rate/annotate each kind, exhaust undo/redo/reload, and inspect exact records and collision/bound recovery |
| [ ] | `DEF-001` | in progress | New-project defaults are name `Untitled Melody`, root seed `melody-forge`, Create destination, and Legacy strategy; migrated values take precedence. Implementation: `src/domain/v2/defaults.ts`; `project.ts`; `kernel.test.ts`; `src/persistence/v2/bootstrapCoordinator.ts`; planned App UI. UI: New project/Create shell. | Fresh revision-1 kernel and headless install/strict-load tests assert exact name/root seed/Create/Legacy defaults and migrated-value precedence; UI bootstrap remains | Create new project and compare migrated project |
| [ ] | `DEF-002` | in progress | Transport defaults are 480 PPQ, 108 BPM, 4/4 grouped 2+2, `gridTicks = 240`, straight swing `subdivisionTicks = 240`/`amountPermille = 500`, one displayed bar unless content is longer, loop off, and master 80%. Implementation: `src/domain/transport/`; `src/domain/v2/defaults.ts`; `performance.ts`; planned controller/UI. UI: Shared transport. | Exact 480/108/4/4/group/grid/swing/loop and Soft Pluck master-volume fresh-kernel tests pass; live UI integration remains | New-project transport/grid/swing inspection remains |
| [ ] | `DEF-003` | not started | Modern defaults are C Ionian, 8 events, 4 quarter-note beats, eighth grid, C4–C6, rests off, max leap 4 degrees, tonic closure on, population 8, seed `paper-kite`. Implementation: Planned Create defaults registry. UI: Modern controls. | Exact Modern default/generation fixture | Open Modern and inspect/generate |
| [ ] | `DEF-004` | not started | Legacy defaults remain the V1 defaults: C Ionian, 8 events, 108 BPM, population 8, seed `legacy-amber`. Implementation: Existing V1 defaults; planned V2 registry adapter. UI: Legacy controls. | V1/default compatibility fixture | Open Legacy and inspect/generate |
| [ ] | `DEF-005` | not started | Evolution defaults are population 8, one retained selected elite per selected parent where slots permit, exact deduplication, and novelty protection on. Implementation: Planned evolution defaults/policy registry. UI: Evolve controls/summaries. | Exact population/elite/dedup/novelty default tests | Start each mode and inspect defaults |
| [ ] | `DEF-006` | not started | Tonality defaults freeze tonic and scale, allow relative pitch shape to evolve, leave absolute-pitch lock off, and disable borrowing/modulation. Implementation: Planned tonal defaults registry. UI: Tonal and Locks controls. | Exact lock/tonality default tests | New-project tonal controls inspection |
| [ ] | `DEF-007` | not started | Custom-scale defaults target cardinality 7 with hard range 4–9. Implementation: Planned custom-scale defaults registry. UI: Custom-scale controls. | Exact bounds/default tests | Open custom-scale controls |
| [ ] | `DEF-008` | not started | Beat defaults are muted with all five semantic lanes enabled, Straight, density 42%, canonical transport swing 0%, fill Every 4 bars, variation strength 25%, index 0, comparison lock on, and seed path `beat/default`. Implementation: Planned beat defaults registry. UI: Beat controls. | Exact lane/mute/family/density/fill/variation/index/lock/seed/swing-alias tests | Materialize default beat and inspect |
| [ ] | `DEF-009` | not started | Sound defaults are Soft Pluck, articulation 55%, velocity accents 35%, reverb 10%, delay off, melody 82%, beat 68%, effects return 20%. Implementation: Planned performance preset/default registry. UI: Sound inspector/transport. | Exact performance default/audio-plan tests | Audition and inspect defaults |
| [ ] | `DEF-009A` | not started | Store every non-tick `PerformanceSettingsV2` numeric as integer percentage points: articulation/accent/melody/beat/effects/master `0…100`, reverb amount `0…30`, delay amount `0…25`, delay feedback `0…75`; reverb `tailTicks` is a safe integer `120…3840` and delay ticks are exactly `120`, `240`, `480`, or `960`. The complete Soft Pluck default before ID is version `performance-v2`, voice `soft-pluck`, articulation `55`, accent `35`, reverb `{enabled:true, amount:10, tailTicks:960}`, delay `{enabled:false, amount:0, delayTicks:240, feedback:20}`, melody `82`, beat `68`, effects `20`, master `80`; disabled effects retain all fields and ID is exactly `stableId('performance', allFieldsAbove)`. Implementation: Planned performance entity validator/default constructor/identity service. UI: Sound inspector, transport levels, and Technical identity. | Exact complete-record/ID golden, integer/unit, every inclusive bound/out-of-bound, delay-choice, disabled-field retention, codec, audio-plan, and migration-precedence tests | Open a new project, inspect every Soft Pluck/effect/bus value and ID, then exercise boundary validation |
| [ ] | `DEF-010` | not started | Breed defaults to Conservative directed preset with Parent A required and Parent B optional. Implementation: Planned Breed defaults. UI: Breed workspace. | Exact state/default tests | Open Breed and inspect actions |
| [ ] | `DEF-011` | not started | Drift defaults to Mixed bias and four candidates in each Near, Medium, and Far band. Implementation: Planned Drift defaults. UI: Drift workspace. | Exact three-band capacity/default tests | Run default Drift batch |
| [ ] | `DEF-012` | not started | Islands defaults to eight candidates per island, migration every three completed island generations, and no more than two pinned elites per island. Implementation: Planned Islands defaults/capacity policy. UI: Islands workspace. | Exact population/cadence/pin-limit tests | Run/pin through default cadence |
| [ ] | `DEF-013` | not started | Map defaults to an 8×8 archive, smooth→angular horizontal axis, sparse→dense vertical axis, eight candidates per batch, and at most three challengers per cell. Implementation: Planned Map defaults/capacity policy. UI: Map workspace. | Exact archive/bin/batch/queue default tests | Populate default Map and inspect queue limit |
| [ ] | `DEF-014` | not started | Pareto defaults to maximize population novelty plus approach sounding-note density 0.65; two to four objectives are allowed and compare tray initially displays up to four. Implementation: Planned Pareto defaults/capacity policy. UI: Pareto workspace. | Exact objective/default/bounds/tray tests | Open and run default Pareto |
| [ ] | `DEF-015` | not started | Pair Lab defaults to eight melodies, eight beats, three collaborators per component, and hall-of-fame capacity four. Implementation: Planned Pair Lab defaults/capacity policy. UI: Pair Lab workspace. | Exact population/collaborator/hall-capacity tests | Cold-start Pair Lab and inspect |
| [ ] | `PERS-001` | in progress | Use a versioned storage adapter rather than direct feature access to browser storage. Implementation: `src/persistence/v2/indexedDbAuthority.ts`; `m2PersistenceBoundary.ts`; `bootstrapCoordinator.ts`; `src/persistence/uiPreferencesV2.ts`; planned App/recovery UI. UI: Recovery/status UI. | Native async authority, registered graph, CAS/atomicity, typed-error, independent preference, and headless bootstrap tests pass; App reload/private-browser UI remains | Reload/private-storage errors |
| [ ] | `PERS-003` | in progress | `melody-forge:project:v1` remains a read-only migration/recovery source, never a schema-2 write target or active authority; automatic migration commits transactionally to IndexedDB while preserving the V1 bytes. Implementation: `src/persistence/v2/sourceEvidence.ts`; `v1Migration.ts`; `v1Equivalence.ts`; `indexedDbAuthority.ts`; `bootstrapCoordinator.ts`; `src/app/App.tsx` guard; planned recovery UI. UI: Migration/recovery UI. | Headless automatic orchestration, source preservation, two-phase authority, pending/verified retry, equivalence, stale-CAS, and corrupt/unsupported V1 non-overwrite tests pass; App/browser evidence remains | Seed V1 storage, migrate/reload, and inspect retained recovery source |
| [ ] | `PERS-003A` | in progress | Implement native IndexedDB database `melody-forge-v2` at database version `1` with exactly the embedded-key stores `projects`, `createStates`, `modeStates`, `historyGraphs`, `historyNodes`, `snapshots`, `melodyCandidates`, `melodyGenomes`, `transports`, `v1TimingProfiles`, `tonalTimelines`, `customScales`, `beats`, `performanceSettings`, `pairings`, `preferenceRecords`, `libraryItems`, `ratings`, `annotations`, `undoStates`, `migrationReceipts`, `appMetadata`; unique receipt `sourceHash` index; and `active-project` metadata version `active-project-metadata-v2`, project ID, revision. At M2, reserved stores may exist empty but persistence/load accepts only the exact root, support, migration, and foundation entity/payload codecs registered through DATA-003 and DATA-004A/B/C; future mode/native-snapshot rows and opaque or placeholder payloads reject until their owning milestone registers them. `V1MigrationReceiptV2` has exactly the declared-order root fields `id`, version `v1-migration-receipt-v2`, `migrationVersion`, `sourceKind`, `sourceHash`, `projectId`, `stagedRevision`, `status`, `candidateMappings`, `snapshotMappings`, `createdAtEpochMs`, `verifiedAtEpochMs`; `migrationVersion` is exactly `v1-project-migration-v2.0.0` and equals the staged registry, while `sourceKind` is only `local-storage-project-v1` or `project-envelope-v1` because candidate envelopes create no receipt. Each candidate mapping is exactly source-candidate, melody-genome, timing-profile, tonal-timeline, pairing, and compatibility-performance IDs; traversal records each unique candidate once in first-seen active-population stored order, then history by source ordinal and snapshot candidate order, then favorites stored order; repeated IDs with equal frozen bytes do not append and unequal bytes reject. Each source-ordinal-sorted snapshot mapping is exactly source history ordinal, source snapshot ID, and history-node ID. Receipt ID is `stableId('migration-receipt',{version,migrationVersion,sourceHash,projectId})`; status/revision/mappings/timestamps do not rename it, and both timestamps plus absent source metadata are null rather than omitted or wall-clock-filled. Transaction one stages the complete revision-1 graph over the registered M2 closure plus pending receipt without active-metadata change; strict load/read-back repeats decode/equivalence; transaction two conditionally verifies/swaps active only when prior-active and staged-revision expectations match. Normal save first validates a self-contained registered graph, then one read-write transaction compares the stored record to `expectedRevision`; saving the active project additionally requires `active-project.projectId` to match and its revision to equal `expectedRevision`, writes changed strong rows plus stored revision `expectedRevision + 1`, and advances active metadata to that same revision atomically, while saving an inactive project never changes active metadata. Any stored or active revision mismatch aborts the whole transaction as stale. Crashed pending data is inactive and same-source-hash revalidatable; stale revision/metadata, ID collision, quota, unavailable/blocked database, abort, decode, read-back, or equivalence failure is typed, exposes no partial graph, preserves active/in-memory state, and never falls back to localStorage. Implementation: `src/persistence/v2/indexedDbSchema.ts`; `indexedDbErrors.ts`; `indexedDbAuthority.ts`; `m2PersistenceBoundary.ts`; `bootstrapCoordinator.ts`; ADR 0003; planned App UI. UI: Migration/recovery/storage status with export, retry, and Library cleanup. | Exact 22 stores/index, registered closure, create/open/replace/save CAS, two-phase activation/retry, typed failure, rollback, Library merge, obsolete-row, and headless bootstrap tests pass under `fake-indexeddb`; real browser/App integration remains | Seed V1, interrupt before/after read-back, resume/reload, save active/inactive projects under current/stale metadata, and exercise blocked/quota/collision recovery while verifying active state |
| [ ] | `PERS-004` | in progress | The sole permitted V2 localStorage record is `melody-forge:ui-preferences:v2`; its fixed registry permits presentation-only preferences and rejects unknown keys plus every project/candidate ID, musical setting, seed, rating, lock, selection, history, or creative-state field, with no spill into extra keys. Implementation: `src/persistence/uiPreferencesV2.ts`; planned preference controls. UI: Presentation preferences and recovery notice. | Exact production registry, domain-field rejection, storage isolation, and fallback tests pass; UI control/reload browser evidence remains | Change allowed preferences and inspect storage/reload |
| [ ] | `PERS-004A` | in progress | Implement exact `UiPreferencesRecordV2` root `{version:'ui-preferences-v2', values}` and its fixed 15-leaf production `values` registry/default: `visualDensity='comfortable'`, `reducedMotionOverride='system'`, views Library `grid` and Map/Pareto `visual`, panel widths controls `244` and inspector `312`, and false disclosures `transportMore`, `sound`, `beatAdvanced`, `createAdvanced`, `mutationAdvanced`, `tonalAdvanced`, `history`, `technical`. Allowed alternatives are density `comfortable/compact`, motion `system/reduce`, Library `grid/list`, Map/Pareto `visual/table`, integer controls width `216…264`, integer inspector width `288…336`, and Boolean disclosures. The root permits only `version` then `values`, root metadata is excluded from the path count, and missing known leaves merge from defaults; unsupported version, missing/extra root key, unknown `values` path, or invalid present value rejects the whole record to defaults; breakpoint clamping never rewrites saved widths. Canonical localStorage encoding uses root order `version`, `values`, declared nested key order, compact JSON, no BOM, and no trailing LF. The generic decoder enforces existing byte/depth/scalar/path limits, with 63/64/65 paths tested through an injected synthetic registry because production has exactly 15 leaves. Implementation: `src/persistence/uiPreferencesV2.ts`; `uiPreferencesV2.test.ts`; planned preference controls. UI: Density/view/panel/disclosure controls and silent presentation reset behavior. | Exact 15-leaf defaults/types/ranges/key-order bytes, missing merge, whole-record fallback, no-domain-field, and 63/64/65 injected-registry tests pass; UI remains | Change every production preference, reload at multiple breakpoints, then seed missing/unknown/invalid records and inspect exact defaults without domain change |
| [ ] | `PERS-005` | in progress | The V2 UI-preference UTF-8 JSON value is capped at exactly 16,384 bytes before parsing; byte overflow or malformed input falls back to defaults without changing IndexedDB data. Implementation: `src/persistence/uiPreferencesV2.ts`; planned recovery presentation. UI: Preference recovery notice. | UTF-8 byte boundary, malformed/preparse, unpaired-surrogate, fallback, and storage-isolation tests pass; browser recovery evidence remains | Seed exact/oversized/malformed preferences and reload |
| [ ] | `PERS-006` | in progress | The V2 UI-preference object contains at most 64 registered property paths and nesting depth at most four; overflow, excessive depth, cycles, arrays, and non-plain structures are rejected to defaults without changing IndexedDB data. Implementation: `src/persistence/uiPreferencesV2.ts`; planned recovery presentation. UI: Preference recovery notice. | Path/depth/cycle/array/plain-object and 63/64/65 injected-registry boundary tests pass; browser recovery evidence remains | Seed boundary preference shapes and reload |
| [ ] | `PERS-007` | in progress | V2 UI-preference leaves may contain only finite numbers, booleans, null, or strings of at most 256 Unicode scalar values; non-finite/other types and 257-scalar strings are rejected to defaults deterministically. Implementation: `src/persistence/uiPreferencesV2.ts`; planned preference controls/recovery presentation. UI: Presentation preference controls/recovery. | Finite scalar, type, Unicode scalar/surrogate, normalization, fallback, and replay tests pass; browser controls/recovery remain | Exercise valid/invalid preference values and inspect fallback |
| [ ] | `PERS-008` | in progress | IndexedDB quota or availability failure has no localStorage domain-data fallback: retain the last committed revision and current in-memory work, offer project export and Library cleanup, and never silently drop nodes. Implementation: `src/persistence/v2/indexedDbErrors.ts`; `indexedDbAuthority.ts`; planned recovery UI/export/cleanup integration. UI: Storage error/recovery actions. | Typed unavailable/quota/abort/conflict/decode/read-back failures, rollback, no domain-localStorage fallback, and committed-state preservation tests pass; recovery UI/browser remains | Simulate quota/unavailable storage and exercise recovery |
| [ ] | `V1-017` | in progress | Never silently clear or replace browser storage during migration or on incompatible/corrupt input. Implementation: `src/app/App.tsx`; `App.test.tsx`; V2 source-evidence/authority/bootstrap modules; planned recovery UI. UI: Actionable recovery UI. | Corrupt/unsupported V1 mount no longer autosaves defaults; coordinator success, retry, mismatch, conflict, unavailable-IDB, and invalid-source paths preserve exact raw bytes; recovery UI/browser evidence remains | Seed old/corrupt storage and verify recovery |
| [ ] | `V1-016A` | in progress | Implement one pure deterministic V1→V2 mapping with no clock/random read: preserve controls, both exact strategy settings/seeds, loop preference, population order/active index, pitches/degrees/rests, versions, provenance, and every source tick; derive a normalized `migrated-v1` candidate plus melody genome whose rhythm copies timing/rest with accent zero/no tie, frozen-V1-rendered phenotype/MIDI, full-loop tonal timeline, candidate-specific compatibility timing profile, and beat-null compatibility pairing. Deterministic absent-field fallbacks are project name `Untitled Melody`, project/receipt timestamps null, comparison/audition timing null before focus, no generated/shared beat, muted accompaniment, and the compatibility singleton as active/pairing performance. Convert immutable snapshots into one original-order active history chain without inventing branches, and favorites to Favorite Library entries with identity/content/provenance and ordered origins intact. Implementation: `src/persistence/v2/v1Migration.ts`; registered `src/domain/v2/` builders/validators. UI: Migration preview, compatibility candidate inspector, History, and Library. | Pure full-field project/history/favorite mapping, exact defaults, no-clock/random, order, genome/event/timeline/timing/pairing/performance, no-beat, Library-origin, and replay tests pass; bootstrap/UI audition remains | Migrate population/history/favorite fixtures, inspect default/null/compatibility fields and normalized records, traverse exact order, and audition Library sources |
| [ ] | `V1-016B` | in progress | Preserve V1 conversion identity with exact source-scope boundaries. `sourceKind` is exactly `local-storage-project-v1`, `project-envelope-v1`, or `candidate-envelope-v1`, and every kind derives lowercase `sourceHash=SHA-256(UTF8(stableStringify({sourceKind,decodedV1})))`; formatting is irrelevant and untouched automatic/raw imported recovery evidence is not the hash input. Copy every applicable V1 candidate ID and complete-project snapshot ID; wrap each converted aggregate's exact frozen schema-1 decoder value as `compatibilitySource={version:'frozen-v1-candidate-v1', candidate}` with declared nested key order and matching root ID, never regenerate it from V2 fields, require native candidates to store null, derive zero-based event IDs by `stableId('event',{version:'v1-event-id-v2',sourceCandidateId,ordinal})`, and bypass the native candidate-ID formula. Only local-storage/project-envelope sources derive project ID `stableId('v1-project',{version:'v1-project-id-v2',sourceHash})`, root seed `v1-migration/${sourceHash}`, null organization/receipt timestamps, and a complete graph/two-phase migration receipt and active swap. A candidate-envelope instead yields one immutable converted preview closure with no project ID, root seed, project receipt, or active swap: file selection and Open remain in memory; Save to Library atomically writes/merges the closure, creates the exact current-project origin and explicit-import defaults `Untitled Melody`/empty note/favorite false/injected safe-integer saved time, and appends the current-project reference at its expected revision; Favorite defaults true; Seed atomically writes/merges and applies only the chosen registered current-project mode command. Collision or failed Save/Seed writes nothing, and the same source closure may enter multiple projects. Project-migrated V1 favorites instead use exact `Untitled Melody`/empty note/favorite true/null saved time. Duplicate source candidate IDs with identical frozen-decoder bytes deduplicate to one normalized candidate/Library identity with stable ordered origins, while same ID/different bytes abort the whole applicable transaction without rename/overwrite. Implementation: `src/persistence/v2/sourceHash.ts`; `sourceEvidence.ts`; `v1Traversal.ts`; `v1Migration.ts`; `indexedDbAuthority.ts`; candidate Save/Seed commands planned. UI: Project migration identity/Technical evidence; candidate preview/Open/Save/Seed; duplicate-origin Library and collision recovery. | Three source scopes/hashes, copied/event IDs, frozen source, project-only receipt, candidate-only preview, dedup/origin/collision, formatting, multi-project conversion, and two-phase authority tests pass; current-project Save/Seed commands/UI remain | Migrate project sources and import candidate sources; exercise Open/Save/Favorite/Seed across projects; inspect IDs/defaults/origins/frozen source; verify collision/stale command leaves active/recovery bytes unchanged |
| [ ] | `V1-016C` | in progress | Before staging and again after strict IndexedDB read-back, produce an explicit V1/V2 equivalence report comparing project settings, both strategy seeds, loop state, history order/active index, snapshot/candidate IDs, provenance, degrees/rests, PPQ, grid, tempo, every onset/duration, loop endpoint, derived MIDI, playback schedule, direct V1 MIDI bytes, and every `v1-triangle-compat` factory/gate/velocity constant; a hash alone is insufficient and any difference aborts activation without changing active state. Implementation: `src/persistence/v2/v1Equivalence.ts`; `v1Migration.ts`; `indexedDbAuthority.ts`; `bootstrapCoordinator.ts`. UI: Migration preview, verification, and actionable failure evidence. | Exact per-field reports, both coordinator mismatch gates, direct MIDI/playback/performance oracle, strict graph read-back, and unchanged-active rollback tests pass; UI/browser evidence remains | Migrate matching and mismatched fixtures, inspect the full report, and verify failed activation preserves the current project |
| [ ] | `V1-016` | in progress | Migrate existing saved projects, favorites, histories, seeds, exported JSON, and generated melodies without loss or reinterpretation. Implementation: `src/persistence/v2/v1Migration.ts`; `v1Equivalence.ts`; `bootstrapCoordinator.ts`; registered schema kernel and IndexedDB authority; planned import UI. UI: Library/history/project import. | Golden conversion/equivalence plus headless localStorage migration, activation, and reload tests pass; project/candidate import UI and browser audition remain | Reload/import and compare/audition |
| [ ] | `SEED-001` | in progress | Maintain one root project seed with stable labelled sub-seeds for generator, generation, algorithm, island, map cell, Pareto run, candidate, operator, melody, rhythm, tonality, beat, collaborator selection, and UI-only shuffling. Implementation: `src/domain/seedTree.ts`. UI: Planned Advanced technical panels. | Six path stability/isolation/collision/validation/derivation tests pass | UI provenance and repeated-action workflow remain |
| [ ] | `SEED-002` | not started | Creative randomness must never use `Math.random()`, timestamps, random UUIDs, object iteration order, worker completion order, or execution timing. Implementation: All pure creative algorithms. UI: N/A. | Source audit and determinism stress tests | Repeat/cancel/reload comparison |
| [ ] | `SEED-003` | not started | Beat-setting changes must not change melody; UI ordering must not alter populations. Implementation: Planned separated sub-seeds. UI: Beat controls/library sorting. | Independence tests | Change beat/sort and compare IDs/events |
| [ ] | `SEED-004` | not started | Identical project state, versions, settings, seeds, ratings, and action sequence reproduce ordered results, migrations, partner schedules, archives, fronts, descriptors, and provenance. Implementation: All deterministic services. UI: All workflows. | Full deterministic scenario tests | Reload/replay identical scenario |

## Milestone 3 — Compact application shell, piano roll, playhead, inspector, and responsive layout

Exit gate: Create/Evolve/Explore/Library navigation and the responsive shell share one transport, while the focused and miniature piano rolls expose accurate accessible timing and provenance.

| Check | ID | Status | Deliverable and UI | Automated evidence | Browser evidence |
| --- | --- | --- | --- | --- | --- |
| [ ] | `IA-001` | not started | Provide four primary destinations: Create, Evolve, Explore, and Library. Implementation: Planned app navigation state/router. UI: Persistent application shell. | Navigation/component tests | Navigate all four at desktop/mobile |
| [ ] | `IA-002` | not started | Preserve current project, playback state, candidate selections, tonal settings, beat, history, and per-mode state while navigating destinations. Implementation: Planned root project/session state. UI: Application shell. | Navigation state-retention tests | Navigate during playback and return to each mode |
| [ ] | `IA-003` | not started | Desktop shell has a compact header with identity, project name/menu, history access, and Library access. Implementation: Planned shell components. UI: Header. | Component tests | 1440/1280/1024 screenshots |
| [ ] | `IA-004` | not started | Provide exactly one sticky shared transport directly below the header. Implementation: Planned transport service/component. UI: Sticky transport. | Single-authority/render tests | Scroll and navigate while playing |
| [ ] | `IA-005` | not started | Desktop shell has a collapsible destination/mode-specific left control rail. Implementation: Planned shell/control rail. UI: Left rail. | State/component tests | Collapse/restore in each destination |
| [ ] | `IA-006` | not started | Desktop shell has a large central creative workspace. Implementation: Planned destination layouts. UI: Main workspace. | Layout tests | Required desktop/tablet viewports |
| [ ] | `IA-007` | not started | Desktop shell has a collapsible right inspector for candidate, inheritance, provenance, annotations, sound, and export. Implementation: Planned inspector. UI: Right inspector. | Selection/inspector tests | Open/collapse/drawer checks |
| [ ] | `IA-008` | not started | Provide a compact branch/history strip with the active path visible. Implementation: Planned history visualization. UI: Shell history strip. | History render tests | Branch navigation workflow |
| [ ] | `IA-009` | not started | Remove the oversized landing hero after a project exists; retain the warm charcoal/cream/amber identity in a denser professional workspace. Implementation: Planned shell/CSS. UI: Header/workspaces. | State/style tests | Before/after-project screenshots |
| [ ] | `IA-010` | not started | Reduce excessive nested bordered rectangles; use spacing, typography, restrained surfaces, and dividers for hierarchy. Implementation: Planned design tokens/CSS. UI: All destinations. | Visual lint where practical | Visual review at all viewports |
| [ ] | `IA-011` | not started | At narrower desktop widths, turn the right inspector into a drawer. Implementation: Planned responsive shell. UI: Inspector drawer. | Responsive component tests | 1024/768-width checks |
| [ ] | `IA-012` | not started | On mobile, use a single-column workspace, bottom sheets for Controls/Inspector, and sticky compact transport rather than crushed desktop columns. Implementation: Planned responsive shell/sheets. UI: Mobile shell. | Responsive/state tests | 390×844 and tablet workflow |
| [ ] | `IA-013` | not started | Keep common creative controls immediately visible. Implementation: Planned control rails. UI: All destinations. | Control-presence tests | Task-completion inspection |
| [ ] | `IA-014` | not started | Put operator weights, tonal relationships, borrowing/modulation, seeds, descriptor formulas, raw provenance, hashes, genomes, and JSON under labelled advanced/technical disclosure. Implementation: Planned advanced panels. UI: Rails/inspector. | Disclosure/component tests | Keyboard and mobile disclosure checks |
| [ ] | `IA-015` | not started | Raw provenance JSON must not dominate ordinary candidate cards. Implementation: Planned card/inspector split. UI: Candidate cards/technical inspector. | Snapshot/component tests | Visual hierarchy review |
| [ ] | `IA-016` | not started | Use amber for creation/primary actions, teal for active playback, distinct patterned Parent A/B styles, a distinct mutation style, green/equivalent elite retention, and red only for stop/errors/destructive actions. Implementation: Planned tokens/visual semantics. UI: Whole application. | Semantic-class tests | Colour/pattern visual review |
| [ ] | `IA-017` | not started | Never rely on colour alone; label/icon/pattern/texture inherited, mutated, repaired, borrowed, selected, playing, elite, and favorite states. Implementation: Planned accessible visual semantics. UI: Cards, piano roll, inspector. | Accessible-name/state tests | Keyboard/colour-vision inspection |
| [ ] | `IA-018` | not started | A fresh-install welcome panel is optional but, when shown, is no taller than 320 CSS px at 1440×900, contains Legacy, Modern, Import MIDI, and Start from Library actions, and disappears for the session after generation/open. Implementation: Planned Create shell/welcome state. UI: Create welcome panel. | Exact-height/action/session-lifecycle and project-state tests | Inspect fresh/open/generated states at 1440×900 |
| [ ] | `IA-019` | not started | The desktop application header is exactly 52–60 CSS px tall while retaining the complete named project/history/Library/menu controls and accessible full project name. Implementation: Planned shell/header tokens. UI: Desktop header. | Min/max geometry, content, truncation/accessible-name, and responsive tests | Inspect header at required desktop viewports and 200% zoom |
| [ ] | `IA-020` | not started | The collapsed branch/history strip is exactly 36–48 CSS px tall, remains above mobile sheet triggers, exposes the active path/current node/overflow, and never covers focused content. Implementation: Planned history strip/layout tokens. UI: Desktop/mobile history strip. | Min/max geometry, path/content, sticky collision, safe-area, and focus tests | Inspect collapsed/expanded history at required viewports |
| [ ] | `IA-021` | not started | Controls default with Common open and Common openness is not stored; Locks opens exactly when an applicable active lock exists; the tonal summary opens exactly when tonic/scale differs from the mode default or borrowing/modulation is active. Locks and tonal-summary openness derive from project state rather than UI preferences. Advanced mutation, relationship weights, borrowing/modulation details, custom-scale operators, formulas, seeds, and technical versions are initially collapsed, and closing never resets values. Implementation: Planned disclosure/default-state registry. UI: Control rails/sheets. | Exact Common/non-persistence, lock and tonal derived-state predicates, advanced initial-state, close/no-reset, reset-preview, and accessibility tests | Open each workspace, inspect each predicate, edit/close/reopen, and reload |
| [ ] | `IA-022` | not started | Inspector defaults to Summary; Technical is never selected automatically, focus changes retain an applicable tab or return to announced Summary, and raw JSON remains in a bounded copyable region. Implementation: Planned inspector tab/default state. UI: Inspector rail/drawer/sheet. | Exact default, applicability fallback, focus announcement, bounded-code, and edit-prompt tests | Change focus/tabs at desktop/mobile and inspect behavior |
| [ ] | `ROLL-001` | not started | Replace contour-only focus visualization with a compact real piano roll whose geometry comes from pitch, onset, and duration. Implementation: Planned piano-roll model/component. UI: Focused timeline and card miniatures. | Geometry tests | Visual timing/pitch inspection |
| [ ] | `ROLL-002` | not started | Show explicit rests. Implementation: Planned timeline model. UI: Piano roll. | Rest geometry/text tests | Rest fixture inspection |
| [ ] | `ROLL-003` | not started | Show bar, beat, and subdivision grid derived from shared transport. Implementation: Planned grid renderer. UI: Piano roll. | Meter/grid geometry tests | Common/compound/odd/partial screenshots |
| [ ] | `ROLL-004` | not started | Show a synchronized playhead. Implementation: Planned transport subscription. UI: Piano roll. | Playhead tick-position tests | Playback synchronization inspection |
| [ ] | `ROLL-005` | not started | Show tonic/scale tonal segments and modulation boundaries in a labelled tonal-context track above the roll. Implementation: Planned tonal timeline renderer. UI: Tonal track. | Segment coverage/geometry tests | Modulation timeline inspection |
| [ ] | `ROLL-006` | not started | Visually distinguish modal borrowing. Implementation: Planned event provenance renderer. UI: Piano roll/tonal track. | Borrowed-style attribution tests | Borrowing workflow inspection |
| [ ] | `ROLL-007` | not started | Visually distinguish Parent A inheritance. Implementation: Planned inheritance renderer. UI: Piano roll. | Attribution tests | Parent A overlay inspection |
| [ ] | `ROLL-008` | not started | Visually distinguish Parent B inheritance. Implementation: Planned inheritance renderer. UI: Piano roll. | Attribution tests | Parent B overlay inspection |
| [ ] | `ROLL-009` | not started | Visually distinguish mutation-created material. Implementation: Planned inheritance renderer. UI: Piano roll. | Attribution tests | Mutation inspection |
| [ ] | `ROLL-010` | not started | Visually distinguish retained elite material. Implementation: Planned inheritance renderer. UI: Piano roll. | Attribution tests | Elite inspection |
| [ ] | `ROLL-011` | not started | Visually distinguish deterministic repair/remapping/adaptation. Implementation: Planned provenance renderer. UI: Piano roll/inspector. | Repair attribution tests | Adapted-child inspection |
| [ ] | `ROLL-012` | not started | Offer an optional collapsed drum lane below the melody. Implementation: Planned drum-lane renderer. UI: Piano roll. | Lane visibility/geometry tests | Expand/collapse and playback |
| [ ] | `ROLL-013` | not started | Keep the focused timeline large enough for close inspection; candidate miniatures reuse the same visual semantics. Implementation: Planned responsive roll/card components. UI: Workspace/cards. | Shared-semantic component tests | Desktop/mobile visual review |
| [ ] | `ROLL-014` | not started | Provide child-only inheritance view. Implementation: Planned inheritance view state. UI: Timeline view switcher. | View-state tests | Inspect child-only |
| [ ] | `ROLL-015` | not started | Provide Parent A overlay. Implementation: Planned inheritance view state. UI: Timeline view switcher. | Overlay geometry tests | Inspect A overlay |
| [ ] | `ROLL-016` | not started | Provide Parent B overlay. Implementation: Planned inheritance view state. UI: Timeline view switcher. | Overlay geometry tests | Inspect B overlay |
| [ ] | `ROLL-017` | not started | Provide side-by-side Parent A, Parent B, and child. Implementation: Planned comparison renderer. UI: Timeline view switcher. | Layout/data tests | Desktop/mobile side-by-side |
| [ ] | `ROLL-018` | not started | Provide pitch-difference view. Implementation: Planned difference calculator. UI: Timeline view switcher. | Pitch-diff tests | Inspect changed pitches |
| [ ] | `ROLL-019` | not started | Provide rhythm-difference view. Implementation: Planned difference calculator. UI: Timeline view switcher. | Rhythm-diff tests | Inspect changed timing |
| [ ] | `ROLL-020` | not started | Provide tonal-context-difference view. Implementation: Planned difference calculator. UI: Timeline view switcher. | Tonal-diff tests | Inspect modulation/scale changes |
| [ ] | `ROLL-021` | not started | Inspector/tooltips expose exact pitch, scale degree, onset, duration, velocity, active tonal context, source parent, mutation operator, and repair information. Implementation: Planned event inspection model. UI: Piano roll/inspector. | Exact field/content tests | Pointer and keyboard inspection |
| [ ] | `ROLL-022` | not started | Timeline remains usable without hover and has accessible textual alternatives. Implementation: Planned semantic event table/description. UI: Beneath/alongside piano roll. | Accessibility/component tests | Keyboard-only and screen-reader-oriented audit |
| [ ] | `ROLL-023` | not started | Inheritance visualization defaults to Child only; Parent A/B overlays, side-by-side, pitch, rhythm, and tonal difference views appear only after explicit selection and never auto-expand raw provenance on every card. Implementation: Planned inheritance-view default state. UI: Timeline view switcher/offspring cards. | Exact default, explicit-switch, focus/change, persistence, and no-raw-expansion tests | Open Breed result and exercise every view at desktop/mobile |
| [ ] | `RSP-001` | not started | At widths at least 1,200 CSS px use left rail 224–264 px, center `minmax(0,1fr)` with at least 560 px, right inspector 288–336 px, 12–20 px gaps, and 16–24 px side padding; fall back to an inspector drawer whenever the center minimum cannot hold. Implementation: Planned shell layout tokens/container queries. UI: Wide desktop shell. | Breakpoint and exact min/max geometry tests | 1440×900 and 1280×720 layout/screenshots |
| [ ] | `RSP-002` | not started | At 900–1,199 CSS px use a 216–240 px collapsible left rail and center column, with a modal inspector drawer `320px–min(420px,90vw)`; at 1024×768 the roll remains at least 480 px with rail open. Implementation: Planned compact-desktop shell CSS. UI: Compact desktop shell/drawer. | Breakpoint, drawer, focus, and roll-width geometry tests | 1024×768 workflow/screenshot |
| [ ] | `RSP-003` | not started | At 768–899 CSS px use one center column, opposite-edge Controls/Inspector drawers, Islands tabs, and first-class Map/Pareto list switches; exactly 768 px is tablet and dialogs scroll internally. Implementation: Planned tablet shell CSS. UI: Tablet shell/drawers. | Exact breakpoint/layout/dialog tests | 768 px tablet workflow/screenshot |
| [ ] | `RSP-004` | not started | Below 768 CSS px use one column with 8–12 px padding, compact sticky transport, one bottom sheet at a time capped at 85dvh, persistent 44 px triggers, one card per row, and no page overflow; 390×844 is the required mobile fixture. Implementation: Planned mobile shell CSS. UI: Mobile shell/bottom sheets. | Breakpoint, sheet, target, and overflow tests | 390×844 workflow/screenshot |
| [ ] | `RSP-005` | not started | At 1280×720 and landscape tablet, combined sticky header/transport/history height is at most 176 CSS px; rails/drawers scroll internally and collision-prone popovers flip then become dialog/sheet. Implementation: Planned compact-height/collision layout rules. UI: Shell/popovers/drawers. | Height budget, overflow, and collision behavior tests | 1280×720 and landscape-tablet inspection |

## Milestone 4 — Percussion, curated voices/effects, multitrack MIDI, and WAV

Exit gate: Deterministic meter-aware beat generation, separated audio buses, every curated voice, multitrack MIDI, and deterministic non-silent WAV rendering work end-to-end.

| Check | ID | Status | Deliverable and UI | Automated evidence | Browser evidence |
| --- | --- | --- | --- | --- | --- |
| [ ] | `BEAT-001` | not started | Represent optional percussion as a separate deterministic track and genome that may be muted but is fully implemented. Implementation: Planned `src/percussion/` domain. UI: Beat controls/drum lane. | Domain/schema/determinism tests | Generate/audition/mute beat |
| [ ] | `BEAT-002` | not started | Every beat event uses shared transport meter, bars, grid, swing, and exact loop boundary. Implementation: Planned beat renderer/scheduler. UI: Transport/drum lane. | Alignment/invariant tests | Odd/partial loop audition |
| [ ] | `BEAT-003` | not started | Provide beat regenerate. Implementation: Planned beat actions. UI: Beat controls. | Deterministic regenerate tests | Regenerate workflow |
| [ ] | `BEAT-004` | not started | Provide previous/next beat variation navigation. Implementation: Planned variation state. UI: Beat controls. | Variation-index state tests | Navigate variations |
| [ ] | `BEAT-005` | not started | Provide beat lock. Implementation: Planned beat locks. UI: Beat controls. | Lock-preservation tests | Lock across melody comparisons |
| [ ] | `BEAT-006` | not started | Provide independent deterministic beat seed. Implementation: Planned beat seed stream. UI: Advanced beat controls. | Seed independence/golden tests | Repeat beat by seed |
| [ ] | `BEAT-007` | not started | Provide variation index. Implementation: Planned variation domain. UI: Beat controls. | Identity/index tests | Navigate and restore index |
| [ ] | `BEAT-008` | not started | Provide beat density control. Implementation: Planned beat settings. UI: Beat controls. | Density descriptor/generation tests | Compare low/high density |
| [ ] | `BEAT-009` | not started | Provide swing control. Implementation: Planned transport/beat settings. UI: Advanced beat controls. | Swing timing tests | Audition/inspect swing |
| [ ] | `BEAT-010` | not started | Provide fill-frequency control. Implementation: Planned beat settings. UI: Advanced beat controls. | Fill scheduling tests | Audition final-bar fills |
| [ ] | `BEAT-011` | not started | Provide variation-strength control. Implementation: Planned beat settings. UI: Beat controls. | Nearness/difference tests | Compare low/high variation |
| [ ] | `BEAT-012` | not started | Provide separate melody and beat volume. Implementation: Planned audio buses. UI: Transport/inspector. | Gain-state/audio tests | Mix during playback |
| [ ] | `BEAT-013` | not started | Provide A/B beat audition where useful. Implementation: Planned beat comparison session. UI: Beat controls/Pair Lab. | Shared-melody A/B tests | A/B beat workflow |
| [ ] | `BEAT-014` | not started | Implement sparse pattern family. Implementation: Planned beat patterns. UI: Pattern-family selector. | Golden/invariant tests | Audition family |
| [ ] | `BEAT-015` | not started | Implement straight pattern family. Implementation: Planned beat patterns. UI: Pattern-family selector. | Golden/invariant tests | Audition family |
| [ ] | `BEAT-016` | not started | Implement half-time pattern family. Implementation: Planned beat patterns. UI: Pattern-family selector. | Golden/invariant tests | Audition family |
| [ ] | `BEAT-017` | not started | Implement four-on-the-floor pattern family. Implementation: Planned beat patterns. UI: Pattern-family selector. | Golden/invariant tests | Audition family |
| [ ] | `BEAT-018` | not started | Implement breakbeat pattern family. Implementation: Planned beat patterns. UI: Pattern-family selector. | Golden/invariant tests | Audition family |
| [ ] | `BEAT-019` | not started | Implement odd-meter-compatible grooves that understand grouping rather than truncate 4/4 patterns. Implementation: Planned meter-aware patterns. UI: Pattern-family selector. | 5/8, 7/8, 5/4 pattern tests | Odd-meter audition |
| [ ] | `BEAT-020` | not started | Variations preserve recognizable groove while changing appropriate kick, snare, hat, percussion, accent, or fill events. Implementation: Planned variation operators. UI: Drum lane/summary. | Layer-difference/identity tests | Compare variations |
| [ ] | `BEAT-021` | not started | Final-bar fills end exactly within the loop. Implementation: Planned fill generator. UI: Drum lane. | Boundary/property tests | Inspect odd/partial final fills |
| [ ] | `BEAT-022` | not started | Create, Breed, Drift, Islands, Map, and Pareto comparisons share one locked beat by default so drum preference cannot contaminate melody selection. Implementation: Planned comparison-session beat policy. UI: All ordinary comparison modes. | Shared-beat identity tests | Audition compared candidates |
| [ ] | `BEAT-023` | not started | Only Pair Lab/co-evolution may intentionally attach different beats to compared melodies. Implementation: Planned pairing domain. UI: Pair Lab. | Pairing-policy tests | Swap/audition partners |
| [ ] | `BEAT-024` | not started | Percussion uses built-in browser synthesis or bundled local assets with no runtime network dependency. Implementation: Planned percussion audio adapter/assets. UI: Sound inspector. | Offline/dependency tests | Offline beat playback |
| [ ] | `BEAT-025` | not started | Keep drum lanes separate in JSON and MIDI exports. Implementation: Planned schema/MIDI adapters. UI: Export inspector. | Round-trip/multitrack tests | Download/reimport/inspect |
| [ ] | `FORM-004` | not started | Implement and register exact immutable `meter-syncopation-v2.0.0`, including meter/group salience, onset/sustain handling, normalization, partial-loop behavior, and stable ties. Implementation: Planned meter-aware descriptor and formula registry. UI: Syncopation formula disclosure. | Common/compound/odd/partial exact fixtures, normalization/range, tie, and version-dispatch tests | Inspect syncopation contributions and value |
| [ ] | `FORM-012` | not started | Implement and register exact immutable `beat-descriptors-v2.0.0`, including lane opportunities, onset density, lane weights, meter syncopation, normalization, and empty-beat behavior. Implementation: Planned beat descriptor service and formula registry. UI: Beat-descriptor disclosure. | Exact opportunity/density/syncopation, empty, normalization/range, and version-dispatch tests | Inspect beat descriptor inputs and values |
| [ ] | `FORM-013` | not started | Implement and register exact immutable `accent-coincidence-v2.0.0`, including inclusive melody-onset windows, beat-lane/accent weighting, normalization, absent-beat behavior, and stable ties. Implementation: Planned pair descriptor service and formula registry. UI: Accent-coincidence disclosure. | Exact window/boundary/weight, absent-beat, normalization/range, tie, and version-dispatch tests | Inspect matching windows and value |
| [ ] | `VOICE-001` | not started | Implement curated Soft Pluck voice. Implementation: Planned voice presets/audio adapter. UI: Voice selector. | Preset/audio-plan tests | Audition same melody |
| [ ] | `VOICE-002` | not started | Implement curated Bell/Mallet voice. Implementation: Planned voice presets/audio adapter. UI: Voice selector. | Preset/audio-plan tests | Audition same melody |
| [ ] | `VOICE-003` | not started | Implement curated Warm Lead voice. Implementation: Planned voice presets/audio adapter. UI: Voice selector. | Preset/audio-plan tests | Audition same melody |
| [ ] | `VOICE-004` | not started | Implement curated Bass voice. Implementation: Planned voice presets/audio adapter. UI: Voice selector. | Preset/audio-plan tests | Audition same melody |
| [ ] | `VOICE-005` | not started | Implement curated Chiptune voice. Implementation: Planned voice presets/audio adapter. UI: Voice selector. | Preset/audio-plan tests | Audition same melody |
| [ ] | `VOICE-006` | not started | Implement curated Soft Keys voice. Implementation: Planned voice presets/audio adapter. UI: Voice selector. | Preset/audio-plan tests | Audition same melody |
| [ ] | `VOICE-007` | not started | Changing voice auditions the exact same melody without regeneration or candidate identity change. Implementation: Planned performance state. UI: Transport/inspector. | Identity/independence tests | Switch voices while comparing events |
| [ ] | `VOICE-008` | not started | Provide compact articulation control. Implementation: Planned performance settings. UI: Sound inspector. | Scheduling/gate tests | Audition values |
| [ ] | `VOICE-009` | not started | Provide velocity-accent performance control. Implementation: Planned performance settings. UI: Sound inspector. | Velocity-render tests | Audition/inspect accents |
| [ ] | `VOICE-010` | not started | Provide light reverb. Implementation: Planned effects bus. UI: Sound inspector. | Offline/live effects cleanup tests | Toggle/adjust reverb |
| [ ] | `VOICE-011` | not started | Provide light delay. Implementation: Planned effects bus. UI: Sound inspector. | Offline/live effects cleanup tests | Toggle/adjust delay |
| [ ] | `VOICE-012` | not started | Instrument/effect settings remain performance data, never melody genes. Implementation: Planned domain separation. UI: Inspector. | Mutation/identity independence tests | Change sound then evolve |
| [ ] | `VOICE-013` | not started | Use separate melody, percussion, effects, and master buses and dispose every schedule/node correctly. Implementation: Planned audio graph adapter. UI: Transport/sound inspector. | Graph lifecycle/ghost-node tests | Stop/replay/switch stress test |
| [ ] | `VOICE-014` | not started | Articulation is a bounded performance-only control from exactly 0–100%, mapping detached to connected gate length without changing pitch/rhythm genes or candidate identity. Implementation: Planned performance settings/audio-plan adapter. UI: Sound inspector articulation control. | Boundary-minus/exact/plus, gate-map, persistence, and identity tests | Audition 0/100 and reject out-of-range input |
| [ ] | `VOICE-015` | not started | Velocity accents are a bounded performance-only control from exactly 0–100%, scaling stored accent values without changing their genes or candidate identity. Implementation: Planned performance settings/audio-plan adapter. UI: Sound inspector accent control. | Boundary-minus/exact/plus, velocity-map, persistence, and identity tests | Audition 0/100 and inspect unchanged events |
| [ ] | `VOICE-016` | not started | Reverb wet amount is bounded to exactly 0–30%, with values outside that creative range unavailable/rejected and no general effects editor exposed. Implementation: Planned performance settings/effects bus. UI: Sound inspector reverb control. | Boundary-minus/exact/plus, audio-plan, persistence, and cleanup tests | Adjust 0/30 and reject out-of-range input |
| [ ] | `VOICE-017` | not started | Delay is exactly Off or 0–25% wet at a transport-synchronized subdivision, with out-of-range values unavailable/rejected and shared-transport scheduling preserved. Implementation: Planned performance settings/effects bus. UI: Sound inspector delay control. | Off/boundary-minus/exact/plus, subdivision, persistence, and cleanup tests | Toggle Off/25, change subdivision, and reject overflow |
| [ ] | `V1-019` | not started | Map migrated V1 playback to the internal `v1-triangle-compat` performance voice without inventing a persisted V1 voice setting; retain it until an explicit performance-only V2 voice choice. Implementation: Planned V1 performance migrator and compatibility voice. UI: Voice selector/technical inspector. | V1 audio-plan and migration golden tests | Migrate, audition, switch voice, and verify melody identity |
| [ ] | `MIDI-001` | not started | Preserve V1 melody-only format-0 MIDI behavior as a supported export path. Implementation: Existing `src/export/midi.ts`; planned versioned adapter. UI: Candidate export. | V1 byte/tick golden tests | Download/use V1 fixture |
| [ ] | `MIDI-002` | not started | Export multitrack melody-plus-beat MIDI with separate percussion channel/track. Implementation: Planned MIDI adapter. UI: Export inspector. | Parsed multitrack tests | Download/open/reimport |
| [ ] | `MIDI-003` | not started | Export MIDI tempo metadata. Implementation: Planned MIDI adapter. UI: Export summary. | Meta-event tests | Inspect exported file |
| [ ] | `MIDI-004` | not started | Export MIDI meter metadata, including odd/compound meters. Implementation: Planned MIDI adapter. UI: Export summary. | Time-signature meta tests | Inspect/reimport files |
| [ ] | `MIDI-005` | not started | V2 melody-plus-beat MIDI uses format 1 with a metadata/conductor track, distinct melody track/channel, distinct percussion track on General MIDI channel 10, exact shared loop-end ticks, and deterministic equal-tick event ordering. Implementation: Planned MIDI adapter. UI: Export inspector/summary. | Parsed format/track/channel/end-tick/order golden tests | Download/reimport and inspect format, tracks, channel, and duration |
| [ ] | `WAV-001` | not started | Export browser-rendered WAV for melody only. Implementation: Planned offline audio renderer. UI: Export inspector. | Header/duration/non-silence tests | Download/play WAV |
| [ ] | `WAV-002` | not started | Export browser-rendered WAV for melody plus beat. Implementation: Planned offline audio renderer. UI: Export inspector. | Mixed-render alignment tests | Download/play WAV |
| [ ] | `WAV-003` | not started | Offline rendering is deterministic and supports configurable effect tails without clipping. Implementation: Planned offline renderer. UI: Export options. | Byte/sample/determinism/peak tests | Export twice and audition |
| [ ] | `WAV-004` | not started | WAV duration is correct and audio is non-silent. Implementation: Planned offline renderer. UI: Export result/status. | Duration/RMS tests | Download/play and inspect duration |
| [ ] | `WAV-005` | not started | WAV export accepts a positive safe-integer number of one or more loop passes and computes output duration/frame count from exactly `loop duration × passes` before the selected tail. Implementation: Planned offline renderer/options validator. UI: WAV loop-pass option/result summary. | Invalid/one/multiple/large-safe-integer boundary, frame-count, duration, and determinism tests | Export one and multiple passes and inspect duration |
| [ ] | `WAV-006` | not started | WAV effect tail is bounded inclusively to exactly 0–4 seconds, is added once after all loop passes, uses the same deterministic effects plan, and rejects non-finite/out-of-range input. Implementation: Planned offline renderer/options validator. UI: WAV tail option/result summary. | Boundary-minus/exact/plus, finite, frame-count, duration, effects, and determinism tests | Export 0/4-second tails and reject invalid input |

## Milestone 5 — Versioned genome, locks, mutation controls, descriptors, diversity, and invariants

Exit gate: The separated genome renders valid events; every lock is exact; mutation policies and descriptors are versioned and deterministic; bounded repair/fallback enforces all hard invariants.

| Check | ID | Status | Deliverable and UI | Automated evidence | Browser evidence |
| --- | --- | --- | --- | --- | --- |
| [ ] | `GEN-001` | not started | Represent pitch shape separately as extended degrees, octave/register, and contour. Implementation: Planned versioned genome domain. UI: Genome inspector. | Serialization/render tests | Inspect candidate genome |
| [ ] | `GEN-002` | not started | Represent rhythm separately as onsets, durations, rests, accents, and ties. Implementation: Planned versioned genome domain. UI: Rhythm inspector. | Serialization/render tests | Inspect/audition rhythm |
| [ ] | `GEN-003` | not started | Represent tonal timeline separately as tonic, scale, borrowing rules, and modulation segments. Implementation: Planned tonal domain. UI: Tonal controls/track. | Coverage/serialization tests | Inspect tonal timeline |
| [ ] | `GEN-004` | not started | Represent beat separately by lane, onset, applicable duration, velocity, seed/variation metadata. Implementation: Planned beat genome domain. UI: Drum lane/beat inspector. | Serialization/render tests | Inspect beat genome |
| [ ] | `GEN-005` | not started | Render genome components into concrete MIDI/scheduled events without making render artifacts canonical genes. Implementation: Planned render pipeline. UI: Technical inspector. | Pure renderer tests | Compare roll/playback/MIDI |
| [ ] | `LOCK-001` | not started | Freeze relative pitch shape independently. Implementation: Planned lock domain/operators. UI: Locks panel. | All-algorithm preservation tests | Lock and evolve in applicable modes |
| [ ] | `LOCK-002` | not started | Freeze absolute sounding MIDI pitches independently. Implementation: Planned lock domain/operators. UI: Locks panel. | Incompatibility/preservation tests | Lock then attempt tonal mutation |
| [ ] | `LOCK-003` | not started | Freeze rhythm independently. Implementation: Planned lock domain/operators. UI: Locks panel. | All-algorithm preservation tests | Lock and evolve |
| [ ] | `LOCK-004` | not started | Freeze contour independently. Implementation: Planned lock domain/operators. UI: Locks panel. | All-algorithm preservation tests | Lock and evolve |
| [ ] | `LOCK-005` | not started | Freeze opening and closing notes independently. Implementation: Planned lock domain/operators. UI: Locks panel. | Boundary preservation tests | Lock and evolve |
| [ ] | `LOCK-006` | not started | Freeze rest positions independently. Implementation: Planned lock domain/operators. UI: Locks panel. | Rest-mask preservation tests | Lock and evolve |
| [ ] | `LOCK-007` | not started | Freeze selected events or timeline regions independently. Implementation: Planned region-lock domain. UI: Piano roll/locks panel. | Region preservation tests | Select region, evolve, inspect |
| [ ] | `LOCK-008` | not started | Freeze tonal context independently. Implementation: Planned lock domain/operators. UI: Locks panel. | Tonal preservation tests | Lock and evolve |
| [ ] | `LOCK-009` | not started | Freeze complete beat independently. Implementation: Planned lock domain/operators. UI: Beat controls. | Beat identity tests | Lock across workflows |
| [ ] | `LOCK-011` | not started | Explain that relative-pitch lock preserves scale-degree shape while allowing tonic/scale changes. Implementation: Product copy/docs. UI: Locks panel. | Copy/component tests | Inspect explanation and behavior |
| [ ] | `LOCK-012` | not started | Explain that absolute-pitch lock preserves sounding notes and prevents incompatible tonal mutations. Implementation: Product copy/docs. UI: Locks panel. | Copy/error tests | Attempt incompatible mutation |
| [ ] | `LOCK-013` | not started | Changing an unrelated component must never modify a frozen component. Implementation: All operators. UI: Locks panel. | Cross-component independence/property tests | Mixed lock workflows |
| [ ] | `LOCK-014` | not started | Event-lock UI provides per-row/focused-event `Freeze event` actions, visible `L` shortcut with editable-field guard, modifier-free multi-select, Select all/Clear, and one stable-event-ID lock from `Freeze selected events`. Implementation: Planned event selection/lock commands. UI: Piano roll and Events table. | Command, selection, shortcut-guard, stable-ID, and removal tests | Keyboard/touch select, freeze, inspect, and remove events |
| [ ] | `LOCK-015` | not started | Region-lock UI uses authoritative start/exclusive-end bar-beat-tick controls and exact ticks, playhead/event-bound setters, explicit clamp confirmation, pitch/rhythm/both preview, overlap union summary, Edit/Remove, and exact conflict preview without weakening locks. Implementation: Planned region-lock editor/constraint preview. UI: Locks panel and piano roll. | Boundary, validation, overlap, coverage, edit/remove, and conflict tests | Keyboard/touch create/edit/remove/conflict workflow |
| [ ] | `MUT-001` | not started | Provide independent pitch mutation strength. Implementation: Planned operator settings. UI: Advanced mutation controls. | Distribution/operator tests | Compare strength changes |
| [ ] | `MUT-002` | not started | Provide independent rhythm mutation strength. Implementation: Planned operator settings. UI: Advanced mutation controls. | Distribution/operator tests | Compare strength changes |
| [ ] | `MUT-003` | not started | Provide independent motif mutation strength. Implementation: Planned operator settings. UI: Advanced mutation controls. | Distribution/operator tests | Compare strength changes |
| [ ] | `MUT-004` | not started | Provide independent tonal-context mutation strength. Implementation: Planned operator settings. UI: Advanced mutation controls. | Distribution/operator tests | Compare strength changes |
| [ ] | `MUT-005` | not started | Provide independent borrowing mutation strength. Implementation: Planned operator settings. UI: Advanced mutation controls. | Distribution/operator tests | Compare strength changes |
| [ ] | `MUT-006` | not started | Provide independent modulation mutation strength. Implementation: Planned operator settings. UI: Advanced mutation controls. | Distribution/operator tests | Compare strength changes |
| [ ] | `MUT-007` | not started | Provide independent custom-scale mutation strength. Implementation: Planned operator settings. UI: Advanced mutation controls. | Distribution/operator tests | Compare strength changes |
| [ ] | `MUT-008` | not started | Provide independent beat mutation strength. Implementation: Planned operator settings. UI: Advanced mutation controls. | Distribution/operator tests | Compare strength changes |
| [ ] | `MUT-009` | not started | Implement Conservative directed preset with visible explanation and genuinely distinct operator weights. Implementation: Planned preset table. UI: Mutation preset control. | Exact distribution tests | Apply/inspect preset |
| [ ] | `MUT-010` | not started | Implement Pitch directed preset with visible explanation and genuinely distinct operator weights. Implementation: Planned preset table. UI: Mutation preset control. | Exact distribution tests | Apply/inspect preset |
| [ ] | `MUT-011` | not started | Implement Rhythm directed preset with visible explanation and genuinely distinct operator weights. Implementation: Planned preset table. UI: Mutation preset control. | Exact distribution tests | Apply/inspect preset |
| [ ] | `MUT-012` | not started | Implement Contour directed preset with visible explanation and genuinely distinct operator weights. Implementation: Planned preset table. UI: Mutation preset control. | Exact distribution tests | Apply/inspect preset |
| [ ] | `MUT-013` | not started | Implement Motif directed preset with visible explanation and genuinely distinct operator weights. Implementation: Planned preset table. UI: Mutation preset control. | Exact distribution tests | Apply/inspect preset |
| [ ] | `MUT-014` | not started | Implement Tonal directed preset with visible explanation and genuinely distinct operator weights. Implementation: Planned preset table. UI: Mutation preset control. | Exact distribution tests | Apply/inspect preset |
| [ ] | `MUT-015` | not started | Implement Wild directed preset with visible explanation and genuinely distinct operator weights. Implementation: Planned preset table. UI: Mutation preset control. | Exact distribution tests | Apply/inspect preset |
| [ ] | `DSC-001` | not started | Version, document, normalize where meaningful, and deterministically test every descriptor; descriptors are descriptive, not aesthetic unless explicitly selected as an objective/preference. Implementation: Planned descriptor registry. UI: Technical descriptor panels. | Registry/version/range tests | Inspect formulas/labels |
| [ ] | `DSC-002` | not started | Descriptor: sounding-note density. Implementation: Planned descriptors. UI: Map/Pareto/inspector. | Fixture/formula tests | Inspect value |
| [ ] | `DSC-003` | not started | Descriptor: rest density. Implementation: Planned descriptors. UI: Map/Pareto/inspector. | Fixture/formula tests | Inspect value |
| [ ] | `DSC-004` | not started | Descriptor: pitch-range usage. Implementation: Planned descriptors. UI: Map/Pareto/inspector. | Fixture/formula tests | Inspect value |
| [ ] | `DSC-005` | not started | Descriptor: mean interval/angularity. Implementation: Planned descriptors. UI: Map/Pareto/inspector. | Fixture/formula tests | Inspect value |
| [ ] | `DSC-006` | not started | Descriptor: contour change. Implementation: Planned descriptors. UI: Map/Pareto/inspector. | Fixture/formula tests | Inspect value |
| [ ] | `DSC-007` | not started | Descriptor: repeated motif/n-gram amount. Implementation: Planned descriptors. UI: Map/Pareto/inspector. | Fixture/formula tests | Inspect value |
| [ ] | `DSC-008` | not started | Descriptor: rhythmic-duration diversity. Implementation: Planned descriptors. UI: Map/Pareto/inspector. | Fixture/formula tests | Inspect value |
| [ ] | `DSC-009` | not started | Descriptor: syncopation relative to active meter. Implementation: Planned descriptors. UI: Map/Pareto/inspector. | Common/compound/odd fixture tests | Inspect value |
| [ ] | `DSC-010` | not started | Descriptor: base-scale stability. Implementation: Planned descriptors. UI: Map/Pareto/inspector. | Borrowing fixture tests | Inspect value |
| [ ] | `DSC-011` | not started | Descriptors: borrowed-note amount and borrowed duration. Implementation: Planned descriptors. UI: Map/Pareto/inspector. | Borrowing fixture tests | Inspect values |
| [ ] | `DSC-012` | not started | Descriptors: modulation amount and distance. Implementation: Planned descriptors. UI: Map/Pareto/inspector. | Modulation fixture tests | Inspect values |
| [ ] | `DSC-013` | not started | Descriptor: pitch parent distance. Implementation: Planned distance service. UI: Drift/Map/inspector. | Parent fixture tests | Inspect value |
| [ ] | `DSC-014` | not started | Descriptor: rhythm parent distance. Implementation: Planned distance service. UI: Drift/Map/inspector. | Parent fixture tests | Inspect value |
| [ ] | `DSC-018` | not started | Descriptor: beat density. Implementation: Planned beat descriptors. UI: Map/Pareto/inspector. | Beat fixture tests | Inspect value |
| [ ] | `DSC-019` | not started | Descriptor: beat syncopation. Implementation: Planned beat descriptors. UI: Map/Pareto/inspector. | Meter-aware tests | Inspect value |
| [ ] | `DSC-020` | not started | Descriptor: melody–beat accent coincidence. Implementation: Planned pair descriptors. UI: Map/Pareto/Pair Lab. | Alignment fixture tests | Inspect value |
| [ ] | `DSC-021` | not started | Store parent-relative descriptors separately for each actual parent ID; Drift always uses its immutable anchor, while two-parent Map/Pareto scalar use explicitly persists Parent A, Parent B, nearest-parent `min` (Map default), or midpoint `(A+B)/2` plus both source values; population/archive novelty remains parent-independent. Implementation: Planned parent-reference policy/descriptor schema. UI: Map/Pareto axis controls, Drift/technical inspector. | Per-parent isolation, four-policy formula/default, persistence, rebin, and parent-independent novelty tests | Select each policy, reload/rebin, and inspect source/reference values |
| [ ] | `FORM-001` | not started | Implement and register the exact immutable `descriptor-core-v2.0.0` pitch, contour, repetition, duration-diversity, and normalization formulas with their empty/silent edge cases and stable version dispatch. Implementation: Planned descriptor core and formula registry. UI: Technical formula disclosure and descriptor explanations. | Exact descriptor fixtures, normalization/range, empty/silent, and version-dispatch tests | Inspect core descriptor inputs, formula version, and value |
| [ ] | `FORM-002` | not started | Implement and register exact immutable `motif-ngrams-v2.0.0`, including tokenization, n-gram sizes, normalization, short-input behavior, and stable ties. Implementation: Planned motif descriptor and formula registry. UI: Motif formula disclosure. | Exact motif-token, short-input, normalization, tie, and version-dispatch tests | Inspect motif formula inputs and value |
| [ ] | `FORM-003` | not started | Implement and register exact immutable `rhythm-entropy-v2.0.0`, including duration-category probabilities, logarithm base, normalization, empty/single-category behavior, and stable version dispatch. Implementation: Planned rhythm descriptor and formula registry. UI: Rhythm formula disclosure. | Exact entropy fixtures, empty/single-category, normalization/range, and version-dispatch tests | Inspect rhythm-entropy inputs and value |
| [ ] | `FORM-006` | not started | Implement and register exact immutable `pitch-parent-distance-v2.0.0`, including rendered token alignment, insertion/deletion/substitution costs, normalization, empty cases, and stable ties. Implementation: Planned parent-distance service and formula registry. UI: Parent-distance disclosure. | Exact alignment/cost, empty, symmetry/range, tie, and version-dispatch tests | Inspect pitch-parent alignment and value |
| [ ] | `FORM-007` | not started | Implement and register exact immutable `rhythm-parent-distance-v2.0.0`, including event-token alignment, onset/duration/rest/accent/tie costs, normalization, empty cases, and stable ties. Implementation: Planned parent-distance service and formula registry. UI: Parent-distance disclosure. | Exact alignment/cost, empty, symmetry/range, tie, and version-dispatch tests | Inspect rhythm-parent alignment and value |
| [ ] | `FORM-010` | not started | Implement and register exact immutable `beat-distance-v2.0.0` as the renormalized lane-weighted mean of per-lane rhythm distances, including absent/empty lanes and stable ties. Implementation: Planned beat-distance service and formula registry. UI: Beat-distance disclosure. | Exact lane-weight, absent/empty, symmetry/range, tie, and version-dispatch tests | Inspect per-lane contributions and value |
| [ ] | `FORM-009` | not started | Implement and register exact immutable `structural-distance-v2.0.0` as the specified normalized combination of contour change, repetition, and duration diversity, including absent-component behavior. Implementation: Planned structural-distance service and formula registry. UI: Structural-distance disclosure. | Exact component, absent, symmetry/range, and version-dispatch tests | Inspect structural components and value |
| [ ] | `DIV-001` | not started | Prevent convergence through exact phenotype deduplication. Implementation: Planned versioned phenotype service. UI: Diversity summaries. | Fingerprint/dedup tests | Trigger duplicate state |
| [ ] | `DIV-002` | not started | Use limited elitism. Implementation: Planned strategy policies. UI: Mode controls/summaries. | Elite-limit tests | Inspect next generation |
| [ ] | `DIV-003` | not started | Use novelty quotas. Implementation: Planned strategy policies. UI: Diversity summaries. | Quota tests | Inspect population |
| [ ] | `DIV-004` | not started | Use crowding/distance preservation. Implementation: Planned strategy policies. UI: Map/Pareto/Islands summaries. | Crowding tests | Inspect population/front |
| [ ] | `DIV-005` | not started | Use deterministic random immigrants where applicable. Implementation: Planned strategy policies. UI: Provenance. | Stream/fallback tests | Inspect immigrant |
| [ ] | `DIV-006` | not started | Use bounded retries and deterministic fallback. Implementation: Planned operator runner. UI: Progress/error UI. | Termination/fallback tests | Impossible-operation workflow |
| [ ] | `BUD-001` | not started | Enforce `operator-attempt-budget-v2.0.0`: 12 primary proposals, at most one repair of the last rejection, up to seven alternate operators ×12 in stable ID order, up to 16 immigrants, 113 validations per output slot, and `requestedSlots×113` per batch; record every count/rejection/fallback and visibly underfill when no distinct valid elite exists. Implementation: Planned shared operator runner/validation ledger. UI: Progress, provenance, underfill/error UI. | Exact ceiling, ordering, forced-rejection, repair, immigrant, elite, underfill, and termination tests | Trigger constrained success/fallback/underfill and inspect ledger |
| [ ] | `INV-001` | not started | Every result has valid positive integer timing and exact loop/phrase duration. Implementation: Planned shared validators. UI: Error/repair UI. | Property tests | Stress workflows |
| [ ] | `INV-002` | not started | Every melody remains monophonic with no illegal overlap. Implementation: Planned melody validator. UI: Error/import UI. | Property/import tests | Import/evolve stress |
| [ ] | `INV-003` | not started | Every sounding note is valid MIDI and within configured register. Implementation: Planned renderer/validator. UI: Inspector/error UI. | All-tonic/register property tests | Inspect extremes |
| [ ] | `INV-004` | not started | Every note is in active scale or explicitly tagged borrowing. Implementation: Planned tonal validator. UI: Roll/inspector. | Legality/property tests | Borrow/evolve stress |
| [ ] | `INV-005` | not started | Exactly one tonal context covers every tick and modulation is bar-aligned. Implementation: Planned tonal validator. UI: Tonal track. | Coverage/boundary property tests | Stress/inspect |
| [ ] | `INV-007` | not started | Enforce enabled tonic-boundary rules. Implementation: Planned melody validator. UI: Constraint/error UI. | Boundary tests | Generate/evolve/import |
| [ ] | `INV-008` | not started | Preserve every frozen component exactly under every applicable algorithm. Implementation: Planned lock validator/operator wrapper. UI: Locks/error UI. | Algorithm×lock matrix | Representative per-mode lock runs |
| [ ] | `INV-009` | not started | Beat lanes are valid and align exactly with melody/transport. Implementation: Planned beat validator. UI: Drum lane/error UI. | Lane/alignment property tests | Odd/partial stress |
| [ ] | `INV-010` | not started | Contradictory locks/impossible constraints produce clear actionable errors and never silently violate locks. Implementation: Planned constraint solver/result type. UI: Mode error state. | Contradiction tests | Impossible-constraint workflows |
| [ ] | `INV-011` | not started | Operator failure uses bounded attempts, visible permitted repair, another allowed operator, then valid elite fallback; it never loops indefinitely or emits invalid material. Implementation: Planned operator runner. UI: Provenance/progress/error UI. | Forced-failure/termination tests | Trigger constrained fallback |

## Milestone 6 — Cross-scale breeding, relationships, borrowing, modulation, and custom scales

Exit gate: The custom-scale core, identity, repair, operators, relationship graph, borrowing, modulation, cross-scale adaptation, and founded persistence/MIDI integrations pass their exact property and browser workflows; later mode and Library/I/O surface integrations remain in dependency order.

| Check | ID | Status | Deliverable and UI | Automated evidence | Browser evidence |
| --- | --- | --- | --- | --- | --- |
| [ ] | `CSC-001` | not started | Canonically represent scales as tonic-relative 12-bit pitch-class sets. Implementation: Planned custom-scale domain. UI: Scale inspector. | Canonicalization tests | Inspect diagram/formula |
| [ ] | `CSC-002` | not started | Custom scales always include tonic and unique ordered pitch classes. Implementation: Planned invariants/repair. UI: Scale editor/inspector. | Property tests | Mutation stress inspection |
| [ ] | `CSC-003` | not started | Custom-scale cardinality is configurable from 4–9 notes with conservative defaults. Implementation: Planned custom-scale settings. UI: Scale controls. | Bounds/default tests | Configure extremes |
| [ ] | `CSC-030` | not started | Enforce `custom-scale-degeneracy-v2.0.0`: reject or visibly repair any 4–9-note mask fitting within a circular chromatic arc of `cardinality−1`, while preserving valid catalogue/whole-tone/octatonic sets and reporting distinct empty/chromatic/degenerate errors. Implementation: Planned scale validator/repair/codecs. UI: Scale editor and import/constraint errors. | Exhaustive 4,096-mask, transposition, repair, and error-code tests | Trigger degenerate edit/import and inspect repair/rejection |
| [ ] | `CSC-011` | not started | Catalogue matches use canonical ID/aliases; unmatched sets get stable content-derived custom ID and human-readable label. Implementation: Planned catalogue matcher/identity. UI: Scale inspector. | Match/naming/identity tests | Inspect matched/custom labels |
| [ ] | `CSC-005` | not started | Custom-scale identity and ordering are exact and deterministic: a non-catalogue mask uses ID `custom-pcs-${tonicRelativePitchClassMask.toString(16).padStart(3,'0')}` in lowercase, ascending unique mask-enumerating `pitchClasses`, matching popcount/cardinality, and derived label `Custom ${pitchClasses.join(', ')}`; catalogue-matching masks require catalogue references and no custom row. Version, label, display spelling, and provenance are excluded from identity. `ScaleOriginV2` is exact ID/version/kind/create-add-remove-shift-recombine-repair-import, unique ID-sorted soft parent IDs, operator ID, ordered seed path, and finite canonical JSON parameters; its ID is `stableId('scale-origin',allFieldsExceptId)`. `ScaleProvenanceV2` contains unique origin-ID-sorted origins. Same custom ID requires byte-equal structural fields/label and persistence/import merges origins by stable ID union; a same-origin ID with different bytes rejects. This registered provenance merge is the sole custom-scale exception to immutable-ID/different-byte collision, and repeated merge is idempotent. Implementation: Planned custom-scale identity, strict codecs, provenance stable-union merger, and catalogue resolver. UI: Technical identity/provenance inspector. | Mask permutation/content-ID, declared-order, derived-label, catalogue-ref, parent/origin sorting/dedup, stable-union permutation/idempotence, structural collision, and same-origin differing-byte rejection tests | Create/import the same mask through multiple derivations, reload, inspect one canonical ID and complete sorted origins, then exercise catalogue/collision recovery |
| [ ] | `CSC-010` | not started | Deterministically repair cardinality and tonic invariants. Implementation: Planned repair service. UI: Repair provenance. | Repair/golden tests | Inspect repaired result |
| [ ] | `CSC-004` | not started | Reject/repair empty, chromatic, or degenerate custom-scale results. Implementation: Planned invariants/repair. UI: Actionable error/repair UI. | Invalid-input/property tests | Trigger invalid constraints |
| [ ] | `CSC-006` | not started | Implement add-non-tonic-pitch-class operator. Implementation: Planned custom-scale operators. UI: Scale mutation provenance. | Operator/property tests | Apply/inspect |
| [ ] | `CSC-007` | not started | Implement remove-non-tonic-pitch-class operator. Implementation: Planned custom-scale operators. UI: Scale mutation provenance. | Operator/property tests | Apply/inspect |
| [ ] | `CSC-008` | not started | Implement move-one-pitch-class-by-semitone operator. Implementation: Planned custom-scale operators. UI: Scale mutation provenance. | Operator/property tests | Apply/inspect |
| [ ] | `CSC-009` | not started | Implement two-parent pitch-class recombination. Implementation: Planned custom-scale operators. UI: Scale inheritance view. | Parent-contribution tests | Breed/inspect |
| [ ] | `CSC-012` | not started | Show interval formula. Implementation: Planned scale formatter. UI: Scale inspector. | Formula tests | Inspect all scale types |
| [ ] | `CSC-013` | not started | Show pitch-class diagram or compact keyboard. Implementation: Planned scale visualization. UI: Scale inspector. | Geometry/accessibility tests | Visual/keyboard inspection |
| [ ] | `CSC-014` | not started | Show parent scales and added/removed/shifted degrees. Implementation: Planned scale provenance. UI: Scale inspector. | Provenance tests | Breed/mutate and inspect |
| [ ] | `INV-006` | not started | Custom scales always satisfy tonic/cardinality/uniqueness invariants. Implementation: Planned scale validator. UI: Scale error/repair UI. | Property tests | Stress/inspect |
| [ ] | `CSC-024` | not started | Project persistence, install/replace, and reload preserve each custom scale's exact mask-derived structural fields, canonical ID, derived label, complete origin-ID-sorted provenance, references, and formula/operator versions. Equal structure under one custom ID merges provenance by stable origin-ID union without losing prior origins; a structural mismatch or same origin ID with different bytes aborts the whole transaction. Replacement removes only obsolete target-owned rows, never referenced global Library content, and strict read-back proves the complete merged closure before activation. Implementation: Planned V2 custom-scale codec/repository merge plus install/activate transaction integration. UI: Project/Library recovery and Replace preview UI. | IndexedDB save/install/replace/read-back/reload, provenance-union/idempotence, reference integrity, obsolete-target cleanup/global-Library retention, and atomic collision rollback tests | Persist two derivations, reload and replace a project, inspect identical structure and complete origins, then exercise collision recovery without active-state change |
| [ ] | `CSC-027` | not started | MIDI rendering maps custom-scale degrees through the active tonal timeline to exact legal sounding MIDI while retaining scale/provenance in accompanying JSON/project data. Implementation: Planned renderer/MIDI adapter. UI: MIDI export summary/inspector. | All-tonic/custom-degree MIDI golden tests | Export/audition MIDI from custom-scale candidate |
| [ ] | `REL-002` | not started | Relationship distance accounts for shared pitch classes and pitch-class-set distance. Implementation: Planned distance model. UI: Relationship explanation. | Metric tests | Inspect explanation |
| [ ] | `REL-003` | not started | Relationship distance accounts for same-tonic modes, relative/parallel relationships, and modal families. Implementation: Planned distance model. UI: Relationship explanation. | Theory fixture tests | Inspect explanation |
| [ ] | `REL-004` | not started | Relationship distance accounts for one-note alterations. Implementation: Planned distance model. UI: Relationship explanation. | Alteration-distance tests | Inspect changed pitch classes |
| [ ] | `REL-005` | not started | Relationship distance accounts for tonic distance and circle-of-fifths relationships. Implementation: Planned distance model. UI: Relationship explanation. | All-tonic tests | Inspect explanation |
| [ ] | `FORM-005` | not started | Implement and register exact immutable `tonal-relationship-v2.0.0`, including pitch-class-set, tonic/fifths, catalogue/custom-scale evidence, normalization, edge cases, and stable ties. Implementation: Planned tonal relationship module and formula registry. UI: Relationship graph/list formula disclosure. | Catalogue/custom exact fixtures, symmetry/range, edge-case, tie, and version-dispatch tests | Inspect relationship evidence and value |
| [ ] | `REL-001` | not started | Build a deterministic scale-relationship graph covering catalogue and custom scales. Implementation: Planned tonal relationship module. UI: Explore/tonal inspector. | Graph determinism/connectivity tests | Browse relationships |
| [ ] | `REL-008` | not started | Tonic and scale are optional independent genes that can each be frozen or evolved. Implementation: Planned tonal genome/locks. UI: Tonal controls. | Gene-presence/lock tests | Freeze/evolve each independently |
| [ ] | `FORM-016` | not started | Implement and register exact immutable `tonal-mutation-bands-v2.0.0`: Low one-hop `0 < d <= .25` weighted `1-d` with minimum-positive fallback; Medium exactly two labelled `0 < d <= .50` hops with prior/source exclusions and fallback/rejection; High one non-self draw weighted `d`; stable-ID cumulative selection and lower-band boundary ownership. Implementation: Planned tonal mutation policy/formula registry. UI: Tonal mutation band controls/explanation. | Exact boundary, probability, fallback, two-hop exclusion/cancellation, rejection, stable-order/stream, lock, and version-dispatch tests | Exercise Low/Medium/High including empty bands and inspect probability/hops |
| [ ] | `REL-006` | not started | Low tonal mutation stays near current context; higher mutation can travel farther. Implementation: Planned graph traversal/operator. UI: Tonal mutation controls. | Bounded-distance tests | Compare low/high tonal mutation |
| [ ] | `REL-007` | not started | UI explains why a destination is related and exactly which pitch classes changed. Implementation: Planned relationship explanation model. UI: Inspector/graph. | Explanation-content tests | Inspect keyboard/pitch-class diff |
| [ ] | `CSC-015` | not started | Show scale relationship information. Implementation: Planned relationship integration. UI: Scale inspector. | Relationship integration tests | Inspect custom relation |
| [ ] | `BOR-001` | not started | Represent modal borrowing as base scale plus donor scale, never as an anonymous merged scale. Implementation: Planned borrowing domain. UI: Tonal controls/track. | Domain/serialization tests | Inspect borrowed context |
| [ ] | `BOR-002` | not started | Provide donor-scale control. Implementation: Planned borrowing settings. UI: Borrowing controls. | Selection tests | Configure donor |
| [ ] | `BOR-003` | not started | Provide borrowing-amount control. Implementation: Planned borrowing settings. UI: Borrowing controls. | Zero/bounds/distribution tests | Compare amounts |
| [ ] | `BOR-004` | not started | Provide donor-exclusive-notes-only control. Implementation: Planned borrowing rules. UI: Borrowing controls. | Legality tests | Configure and inspect |
| [ ] | `BOR-005` | not started | Provide keep-strong-beats-in-base-scale control. Implementation: Planned borrowing rules. UI: Borrowing controls. | Meter-aware strong-beat tests | Inspect strong beats |
| [ ] | `BOR-006` | not started | Provide configurable borrowed-note resolution time. Implementation: Planned borrowing rules. UI: Borrowing controls. | Resolution-window tests | Inspect/audition resolution |
| [ ] | `BOR-007` | not started | Provide tonic-boundary preservation control. Implementation: Planned borrowing rules. UI: Borrowing controls. | Boundary tests | Inspect phrase boundaries |
| [ ] | `BOR-008` | not started | A pitch shared by base and donor counts as base; every genuinely borrowed event records donor and resolution provenance. Implementation: Planned event provenance. UI: Roll/inspector. | Shared-pitch and provenance tests | Inspect borrowed events |
| [ ] | `BOR-009` | not started | Show borrowing distinctly in piano roll, tonal track, candidate summary, and inspector. Implementation: Planned tonal renderers. UI: Roll/track/card/inspector. | Cross-view semantic tests | Browser visual audit |
| [ ] | `BOR-010` | not started | A newly enabled borrowing configuration defaults amount to exactly zero/Off and produces no borrowed events until the user explicitly raises it. Implementation: Planned borrowing defaults/rules. UI: Borrowing amount control/summary. | Exact default, zero boundary, generation/evolution, persistence, and replay tests | Enable borrowing, inspect Off, generate, and verify none |
| [ ] | `BOR-011` | not started | Borrowing defaults `donor-exclusive notes only` to On; shared base/donor pitches remain base notes and only donor-exclusive pitch classes may receive borrowed status. Implementation: Planned borrowing defaults/legality rules. UI: Donor-exclusive control and explanations. | Exact default, shared/exclusive classification, legality, persistence, and replay tests | Enable borrowing and inspect shared/exclusive notes |
| [ ] | `BOR-012` | not started | Borrowing defaults meter-aware strong-beat protection to On across common, compound, odd, and partial meters. Implementation: Planned borrowing defaults/timing rules. UI: Strong-beat protection control. | Exact default and meter×beat-strength protection/override tests | Enable borrowing in each meter and inspect protected beats |
| [ ] | `BOR-013` | not started | Borrowed-note resolution defaults to within exactly one active-meter denominator beat; Off and one-bar remain explicit alternatives, and required resolution cannot cross a tonal boundary or loop end. Implementation: Planned borrowing defaults/resolution rules. UI: Resolution-time control and provenance. | Exact default, Off/beat/bar, meter, boundary, loop-end, reject/repair, and persistence tests | Inspect default and exercise each window/boundary |
| [ ] | `BOR-014` | not started | Borrowing defaults tonic-boundary preservation to On and rejects or visibly repairs any proposed borrowing operation that would violate it. Implementation: Planned borrowing defaults/boundary rules. UI: Tonic-boundary control/conflict preview. | Exact default, opening/closing boundary, reject/repair, lock, persistence, and replay tests | Enable borrowing and trigger boundary-safe/conflicting operations |
| [ ] | `CSC-019` | not started | Custom scales work as borrowing bases/donors with donor-exclusive legality and complete event provenance. Implementation: Planned borrowing/custom-scale integration. UI: Borrowing controls/roll. | Zero/legality/donor/provenance tests | Configure and inspect custom-scale borrowing |
| [ ] | `MOD-001` | not started | Represent a complete non-overlapping tonal timeline with exactly one active context at every tick. Implementation: Planned tonal timeline domain. UI: Tonal track. | Coverage/property tests | Inspect full loop |
| [ ] | `MOD-002` | not started | Modulation boundaries align to bar boundaries. Implementation: Planned modulation invariants. UI: Tonal track. | Common/odd/partial bar tests | Inspect boundaries |
| [ ] | `MOD-012` | not started | Adding modulation requires at least two complete bars and leaves at least one complete bar on each side of a full-bar boundary; shorter or partial-only phrases show `Modulation needs two complete bars` and offer non-destructive phrase-length adaptation rather than silently no-op. Implementation: Planned modulation constraint/default service. UI: Modulation controls/error/adaptation preview. | Exact one-/two-bar/partial boundary, grouping, copy, adaptation, and nonmutation tests | Attempt modulation below/at minimum and inspect recovery |
| [ ] | `MOD-003` | not started | When phrase length cannot support modulation, explain why rather than silently no-op. Implementation: Planned constraint/result errors. UI: Tonal controls/status. | Impossible-operation tests | Short-phrase error workflow |
| [ ] | `MOD-004` | not started | Evolution can add a modulation. Implementation: Planned modulation operator. UI: Tonal provenance. | Operator tests | Apply/inspect |
| [ ] | `MOD-005` | not started | Evolution can remove a modulation. Implementation: Planned modulation operator. UI: Tonal provenance. | Operator tests | Apply/inspect |
| [ ] | `MOD-006` | not started | Evolution can move a modulation boundary. Implementation: Planned modulation operator. UI: Tonal provenance. | Boundary/operator tests | Apply/inspect |
| [ ] | `MOD-007` | not started | Evolution can mutate destination tonic or scale. Implementation: Planned modulation operator. UI: Tonal provenance. | Graph/lock tests | Apply/inspect |
| [ ] | `MOD-008` | not started | Crossover can exchange complete tonal sections. Implementation: Planned tonal-section crossover. UI: Inheritance view. | Coverage/source tests | Breed segmented parents |
| [ ] | `MOD-009` | not started | Tonal timeline may return to opening context. Implementation: Planned modulation operator. UI: Tonal controls/track. | Return-pattern tests | Generate/inspect |
| [ ] | `MOD-010` | not started | Prefer common-tone continuity and small voice-leading at boundaries when compatible with operators/locks. Implementation: Planned boundary renderer/operator. UI: Inspector. | Voice-leading tests | Audition/inspect boundaries |
| [ ] | `MOD-011` | not started | Candidate summaries describe tonic/scale movement, bar ranges, and borrowed-note source in readable language. Implementation: Planned summary formatter. UI: Cards/inspector. | Exact summary fixture tests | Inspect example summaries |
| [ ] | `CSC-020` | not started | Custom scales work as modulation sources/destinations with graph-bounded choices and complete bar-aligned timelines. Implementation: Planned modulation/custom-scale integration. UI: Tonal track/modulation controls. | Relationship, boundary, coverage, and operator tests | Add/mutate/remove custom-scale modulation |
| [ ] | `FORM-008` | not started | Implement and register exact immutable `tonal-parent-distance-v2.0.0`, including atomic-span context weighting, exact-boundary Jaccard cost, normalization, empty-boundary behavior, and stable ties. Implementation: Planned parent-distance service and formula registry. UI: Parent-distance disclosure. | Exact tonal-span/boundary, empty, symmetry/range, tie, and version-dispatch tests | Inspect tonal-parent spans, boundaries, and value |
| [ ] | `FORM-014` | not started | Implement and register exact immutable `tonal-axis-v2.0.0`, including base-stability/borrowing/modulation components, normalization, absent-segment behavior, and stable version dispatch. Implementation: Planned tonal-axis descriptor and formula registry. UI: Tonal-axis disclosure. | Exact component, empty/absent, normalization/range, and version-dispatch tests | Inspect tonal-axis contributions and value |
| [ ] | `DSC-015` | not started | Descriptor: tonal parent distance. Implementation: Planned distance service. UI: Drift/Map/inspector. | Parent fixture tests | Inspect value |
| [ ] | `XSC-002` | not started | Never cross raw MIDI or merge parent scales into an unrestricted pitch pool. Implementation: Planned normalized-degree crossover. UI: Technical provenance. | Legality/source audit tests | Inspect child notes/provenance |
| [ ] | `XSC-003` | not started | Before incompatible breeding, preview and explicitly/default-select child transport, meter/bars, timing adaptation, and target tonal strategy. Implementation: Planned adaptation proposal domain. UI: Adaptation preview dialog. | Proposal/confirmation tests | Complete incompatible-parent flow |
| [ ] | `XSC-010` | not started | Deterministically map different cardinalities through root-relative octave position, nearest valid child degree, contour/octave preservation, and stable documented tie-breaking. Implementation: Planned degree-mapping service. UI: Adaptation provenance. | Exhaustive mapping/property tests | Inspect mapped child |
| [ ] | `XSC-012` | not started | Preserve original parents and record every remapping/adaptation. Implementation: Planned immutable lineage/provenance. UI: Inspector/history. | Parent immutability/provenance tests | Return to parents and inspect child |
| [ ] | `XSC-013` | not started | Incompatible-parent adaptation defaults to the active comparison transport, fit by bar/beat position without changing event order, and Parent A tonal context; every trim, pad, collision, pitch repair, or lock conflict remains previewed and requires explicit confirmation. Implementation: Planned adaptation proposal/default registry. UI: Adaptation preview dialog/sheet. | Exact default, source-immutability, event-order, destructive-change, lock-conflict, and confirmation tests | Open incompatible parents, inspect defaults, cancel/confirm, and compare sources |
| [ ] | `XSC-004` | not started | Child tonal strategy can inherit Parent A context. Implementation: Planned tonal strategy. UI: Adaptation preview. | Strategy tests | Breed and inspect |
| [ ] | `XSC-005` | not started | Child tonal strategy can inherit Parent B context. Implementation: Planned tonal strategy. UI: Adaptation preview. | Strategy tests | Breed and inspect |
| [ ] | `XSC-006` | not started | Child tonal strategy can render both parents into a user-selected context. Implementation: Planned tonal strategy. UI: Adaptation preview. | Strategy/mapping tests | Breed and inspect |
| [ ] | `XSC-007` | not started | Child tonal strategy can inherit and mutate a related context. Implementation: Planned tonal strategy/graph. UI: Adaptation preview. | Graph-bounded tests | Breed and inspect |
| [ ] | `XSC-008` | not started | Child tonal strategy can create a constrained hybrid/custom scale. Implementation: Planned tonal/custom-scale strategy. UI: Adaptation preview. | Validity/recombination tests | Breed and inspect |
| [ ] | `XSC-009` | not started | Child tonal strategy can preserve/recombine a segmented tonal timeline. Implementation: Planned tonal-section crossover. UI: Adaptation preview. | Coverage/crossover tests | Breed and inspect |
| [ ] | `XSC-001` | not started | Allow parents with different tonics, scales/cardinalities, registers, event counts, rhythms, phrase lengths, and meters. Implementation: Planned adaptation/crossover service. UI: Parent adaptation preview. | Cross-product tests | Breed incompatible library items |
| [ ] | `XSC-011` | not started | Test pentatonic, heptatonic, whole-tone, octatonic, catalogue, and custom-scale mapping across tonic classes. Implementation: Planned mapping tests. UI: N/A. | Required mapping matrix | Representative browser adaptations |
| [ ] | `FORM-011` | not started | Implement and register exact immutable `phenotype-distance-v2.0.0`, including `.30/.25/.15/.20/.10` component weights, absent-component renormalization, one-sided distance, nearest-neighbour novelty, and stable ID ties. Implementation: Planned phenotype/novelty service and formula registry. UI: Phenotype-distance disclosure. | Exact weight, absence, symmetry/range, neighbour-count, tie, and version-dispatch tests | Inspect component contributions, neighbours, and value |
| [ ] | `DSC-016` | not started | Descriptor: combined parent distance. Implementation: Planned distance service. UI: Drift/Map/inspector. | Component/weight tests | Inspect explanation |
| [ ] | `DSC-017` | not started | Descriptor: population/archive novelty. Implementation: Planned novelty service. UI: Mode summaries. | Neighbour/population tests | Inspect value |
| [ ] | `MUT-016` | not started | Provide Near/Medium/Far descendants from the same parent state in one three-way mutation comparison. Implementation: Planned distance-calibrated variants. UI: Comparison workspace. | Shared-parent/distance tests | Three-way audition |
| [ ] | `CSC-017` | not started | Custom scales work as first-class inputs and outputs in Legacy-compatible/V2 generation where the selected strategy permits them. Implementation: Planned shared scale registry/generators. UI: Create tonal controls. | Generation legality/determinism tests | Generate, inspect, audition custom-scale melody |
| [ ] | `CSC-018` | not started | Custom scales participate in one-/two-parent breeding, cross-cardinality mapping, recombination, locks, and provenance. Implementation: Planned scale-aware Breed/crossover services. UI: Breed adaptation/inheritance UI. | Parent contribution, mapping, lock, and validity tests | Breed catalogue/custom parents and inspect |

## Milestone 7 — Complete Breed and Drift workflows

Exit gate: Breed and Drift have distinct persisted state, algorithms, layouts, provenance, deterministic tests, and browser workflows without breaking shared mode-state guarantees.

| Check | ID | Status | Deliverable and UI | Automated evidence | Browser evidence |
| --- | --- | --- | --- | --- | --- |
| [ ] | `BRD-001` | not started | Breed supports one or two parent slots. Implementation: Planned Breed state. UI: Parent A/B slots. | Selection/state tests | One-/two-parent workflow |
| [ ] | `BRD-002` | not started | Breed provides independent pitch, rhythm, motif, tonal, and phrase crossover at musically aligned boundaries. Implementation: Planned crossover operators. UI: Breed controls/inheritance view. | Component/boundary tests | Configure and inspect children |
| [ ] | `BRD-003` | not started | Breed applies component-specific mutation. Implementation: Planned Breed reproduction. UI: Breed controls. | Operator-routing tests | Inspect provenance |
| [ ] | `BRD-004` | not started | Breed retains selected elites byte-for-byte when enabled. Implementation: Planned Breed elitism. UI: Offspring grid/elite badges. | Exact identity tests | Evolve and inspect elites |
| [ ] | `BRD-005` | not started | Breed removes exact duplicates and protects novelty/diversity. Implementation: Planned phenotype/diversity service. UI: Diversity summary. | Dedup/novelty tests | Inspect underfill/diversity state |
| [ ] | `BRD-006` | not started | Breed shows Parent A, Parent B, mutation, remapping, and repair contributions. Implementation: Planned inheritance provenance. UI: Roll/inspector/cards. | Attribution tests | Inspect every contribution type |
| [ ] | `BRD-007` | not started | One parent yields mutation-derived descendants; two parents make genuine contribution from both when feasible. Implementation: Planned Breed reproduction. UI: Parent slots/offspring. | Parent-contribution tests | Compare one/two parent outputs |
| [ ] | `BRD-008` | not started | Zero mutation returns an exact parent copy. Implementation: Planned Breed reproduction. UI: Mutation control/summary. | Byte-equivalence tests | Zero-mutation workflow |
| [ ] | `BRD-009` | not started | Breed layout places Parent A/B slots above an offspring grid. Implementation: Planned Breed layout. UI: Breed workspace. | Layout/component tests | Desktop/mobile screenshot |
| [ ] | `DFT-001` | not started | Drift is reversible single-anchor local exploration with distinct Near, Medium, and Far bands. Implementation: Planned Drift strategy/state. UI: Drift workspace. | Determinism/band tests | Generate/audition bands |
| [ ] | `DFT-002` | not started | Drift distance is documented and combines pitch, rhythm, tonal, structural, and beat differences rather than arbitrary labels. Implementation: Planned distance model. UI: Band explanations/inspector. | Calibration/component tests | Inspect distances |
| [ ] | `DFT-003` | not started | Drift bias can target pitch, rhythm, motif, tonal context, or mixed change. Implementation: Planned Drift policy. UI: Bias controls. | Policy distribution tests | Compare biases |
| [ ] | `DFT-004` | not started | Descendant promotion changes anchor only after explicit action. Implementation: Planned Drift actions. UI: Promote action. | State-transition tests | Promote workflow |
| [ ] | `DFT-005` | not started | Drift preserves prior trail and supports branching from any anchor. Implementation: Planned Drift graph state. UI: Breadcrumb trail. | Branch/reversal persistence tests | Backtrack/branch/reload |
| [ ] | `DFT-006` | not started | Drift layout shows pinned anchor, visible distance bands, and breadcrumb trail. Implementation: Planned Drift layout. UI: Drift workspace. | Layout/component tests | Desktop/mobile screenshot |
| [ ] | `DFT-007` | not started | Drift bands are exactly Near `[0.05, 0.22]`, Medium `(0.22, 0.50]`, and Far `(0.50, 1]`; exact zero remains only the pinned anchor, every generated descendant is classified by persisted `phenotype-distance-v2.0.0`, and boundary values use the stated inclusivity. Implementation: Planned Drift band classifier/state. UI: Drift bands/cards/technical inspector. | Below/exact/above every boundary, zero-anchor, formula-version, persistence, and replay tests | Generate/inspect boundary fixtures and band explanations |

## Milestone 8 — Complete Islands and Map workflows

Exit gate: Independent deterministic islands migrate visibly without losing elites, and the MAP-Elites archive—including custom-scale descriptor/bin integration—supports transparent replacement, re-binning, pinning, audition, and an accessible list.

| Check | ID | Status | Deliverable and UI | Automated evidence | Browser evidence |
| --- | --- | --- | --- | --- | --- |
| [ ] | `ISL-001` | not started | Islands maintains at least three real deterministic subpopulations with independent derived RNG, policies, elites, and diversity state. Implementation: Planned Islands strategy. UI: Islands workspace. | Stream/state separation tests | Inspect/evolve lanes |
| [ ] | `ISL-002` | not started | Conservative island favors small pitch changes, parent fidelity, and preserved rhythm. Implementation: Planned Conservative policy. UI: Conservative lane. | Operator distribution tests | Evolve lane |
| [ ] | `ISL-003` | not started | Rhythmic island favors durations, rests, syncopation, and motif timing. Implementation: Planned Rhythmic policy. UI: Rhythmic lane. | Operator distribution tests | Evolve lane |
| [ ] | `ISL-004` | not started | Adventurous/Tonal island favors borrowing, modulation, custom scales, and larger structure. Implementation: Planned Tonal policy. UI: Adventurous lane. | Operator distribution tests | Evolve lane |
| [ ] | `ISL-005` | not started | Deterministic visible migration schedule shows migrants and source islands and never overwrites pinned elites. Implementation: Planned migration service. UI: Migration summary/lanes. | Schedule/elite tests | Trigger and inspect migration |
| [ ] | `ISL-006` | not started | Support evolving one island independently. Implementation: Planned Islands actions. UI: Per-island action. | Isolation tests | Evolve one lane |
| [ ] | `ISL-007` | not started | Support one “Evolve all islands” action. Implementation: Planned Islands actions. UI: Global action. | Deterministic all-islands tests | Full migration workflow |
| [ ] | `ISL-008` | not started | Use three swimlanes on wide screens and tabs on narrow screens with readable migration summaries. Implementation: Planned responsive Islands layout. UI: Islands workspace. | Responsive/state tests | Desktop/mobile screenshots |
| [ ] | `ISL-009` | not started | Implement `islands-global-diversity-v2.0.0` as the arithmetic mean over every unpinned membership record `(islandId, candidateId)` of its nearest `phenotype-distance-v2.0.0` distance to a membership on another represented island: equal-distance ties use island ID then candidate ID, byte-identical phenotypes on different islands remain separate memberships that each contribute zero, an island is represented only when it has an unpinned membership, and fewer than two represented islands returns zero. Implementation: Planned Islands diversity service. UI: Formula disclosure/global summary. | Membership-preservation, duplicate-phenotype-zero, represented-island, nearest/tie, mean, empty/single-island, and formula-version tests | Inspect every source/nearest-destination membership and value on duplicate, populated, and sparse islands |
| [ ] | `ISL-010` | not started | Persist global-diversity formula ID/value, every contributing source/nearest-destination `(islandId, candidateId)` membership and distance, generation vector, threshold state, bounded immigrant attempts, and last accepted/rejected immigrant action or `no-replaceable-survivor` reason without conflating global diversity with per-island diversity. Implementation: Planned Islands state/schema/codecs. UI: Islands technical/global summary. | Complete membership-evidence codec, duplicate-membership, no-replaceable-reason, reload, reference, bounded-attempt, and deterministic replay tests | Run/reload and compare complete global-diversity evidence, action, and reason |
| [ ] | `ISL-011` | not started | Migrants maximize distance from the destination population after pinned exclusion, then candidate ID; after ordinary survivor selection, a global round below `0.18` considers only islands with a replaceable unpinned survivor, chooses the lowest mean membership contribution then island ID, proposes one immigrant from the dedicated labelled stream against that island's lowest-novelty survivor then candidate ID, commits at most one replacement only when the recomputed total metric strictly increases, and otherwise preserves every population, persisting `no-replaceable-survivor` when none is eligible and persisting/reporting other bounded failure. Implementation: Planned migration/survivor/immigrant policy. UI: Migration and global-diversity explanations. | Migrant comparator, threshold, eligibility, island/survivor tie, strict-total-improvement, one-replacement, all-pinned/no-replaceable preservation, bounded-failure, and cancellation-atomicity tests | Trigger above/below-threshold, improving, non-improving, and all-pinned rounds and inspect decisions/reasons |
| [ ] | `ISL-012` | not started | Islands UI shows labelled Global diversity formula/version, current value, `0.18` threshold, every contributing source/nearest-destination membership and distance including duplicate-phenotype zero contributions, immigrant/migrant decisions and failure reasons including `no-replaceable-survivor`, and separate per-island metrics in swimlanes and tabs. Implementation: Planned Islands summary/detail components. UI: Islands workspace. | Membership-content, duplicate-zero, decision/reason, semantic, responsive, and formula-link tests | Desktop/mobile inspect duplicate, threshold, no-replaceable, and per-island evidence |
| [ ] | `ISL-013` | not started | Verification covers exact membership-record formula fixtures, duplicate phenotype memberships, deterministic state/reload, migrant destination distance, below-threshold strict-total-improvement, non-improvement/underfill, all-pinned `no-replaceable-survivor` preservation, pinned protection, cancellation atomicity, and browser-visible evidence. Implementation: Planned Islands unit/property/integration/E2E suites. UI: Islands workspace. | Required Islands membership-diversity and replacement-decision test matrix | Record duplicate, threshold, no-replaceable, migration, cancel, and reload workflows |
| [ ] | `MAP-001` | not started | Implement real interactive MAP-Elites/quality-diversity exploration. Implementation: Planned Map strategy/archive. UI: Explore Map workspace. | Archive algorithm tests | Populate/explore archive |
| [ ] | `MAP-002` | not started | Axis option: smooth↔angular. Implementation: Planned descriptor registry. UI: Axis selector. | Descriptor/binning tests | Select/populate axis |
| [ ] | `MAP-003` | not started | Axis option: sparse↔dense. Implementation: Planned descriptor registry. UI: Axis selector. | Descriptor/binning tests | Select/populate axis |
| [ ] | `MAP-004` | not started | Axis option: narrow↔wide pitch range. Implementation: Planned descriptor registry. UI: Axis selector. | Descriptor/binning tests | Select/populate axis |
| [ ] | `MAP-005` | not started | Axis option: repetitive↔varied. Implementation: Planned descriptor registry. UI: Axis selector. | Descriptor/binning tests | Select/populate axis |
| [ ] | `MAP-006` | not started | Axis option: straight↔syncopated. Implementation: Planned descriptor registry. UI: Axis selector. | Descriptor/binning tests | Select/populate axis |
| [ ] | `MAP-007` | not started | Axis option: tonally stable↔borrowed/modulating. Implementation: Planned descriptor registry. UI: Axis selector. | Descriptor/binning tests | Select/populate axis |
| [ ] | `MAP-008` | not started | Axis option: parent-faithful↔novel. Implementation: Planned descriptor registry. UI: Axis selector. | Descriptor/binning tests | Select/populate axis |
| [ ] | `MAP-009` | not started | Axis option: low↔high beat density. Implementation: Planned descriptor registry. UI: Axis selector. | Descriptor/binning tests | Select/populate axis |
| [ ] | `MAP-010` | not started | Axis option: low↔high melody–beat accent coincidence. Implementation: Planned descriptor registry. UI: Axis selector. | Descriptor/binning tests | Select/populate axis |
| [ ] | `MAP-011` | not started | Display a labelled binned 2D archive with occupied/empty cells; occupied cells contain compact candidate visualization and direct audition. Implementation: Planned Map archive renderer. UI: Map grid. | Binning/render tests | Populate/audition cells |
| [ ] | `MAP-012` | not started | Each cell stores one representative, short challenger queue, and optional pinned user elite. Implementation: Planned Map cell state. UI: Cell inspector. | Queue/pinning persistence tests | Inspect/pin/reload |
| [ ] | `MAP-019` | not started | Map rating is exactly `Unrated` or an integer from 1–5; rating comparison precedes centre distance, neighbour novelty, and ID, while a direct Blind A/B preferred-over edge breaks only an equal-rating tier. Implementation: Planned Map rating validator/replacement comparator. UI: Cell inspector, grid/list rating and evidence. | Unrated/1–5/invalid boundary, comparator tier, A/B equal-rating, persistence, and replay tests | Rate/challenge cells at every tier and inspect replacement |
| [ ] | `MAP-013` | not started | Replacement order is explicit preference/rating, centre closeness, neighbour novelty, then stable tie-break; never fabricate aesthetic quality. Implementation: Planned replacement comparator. UI: Map explanation. | Comparator-order tests | Rate/challenge cell |
| [ ] | `MAP-014` | not started | Unrated candidates may fill empty cells; pinned occupants cannot be replaced. Implementation: Planned archive insertion. UI: Map grid. | Fill/pin tests | Generate around pinned cell |
| [ ] | `MAP-015` | not started | Changing axes preserves the full candidate archive and deterministically re-bins without loss. Implementation: Planned archive/rebin service. UI: Axis selector. | Rebin/losslessness tests | Change axes/reload |
| [ ] | `MAP-016` | not started | Generate more candidates around selected cells or regions. Implementation: Planned regional sampling. UI: Map grid/actions. | Region-seed tests | Select region/generate |
| [ ] | `MAP-020` | not started | Map coverage is exactly occupied cells divided by 64 for the fixed 8×8 archive, including zero/full values, and is labelled as coverage rather than quality. Implementation: Planned Map metrics service. UI: Map metrics/formula disclosure. | Empty/partial/full exact fraction, rebin, persistence, version, and label tests | Populate/rebin archive and inspect coverage |
| [ ] | `MAP-021` | not started | Map diversity is the versioned mean pairwise descriptor or phenotype distance selected by the disclosed metric, with empty/single-occupant behavior defined by that formula and no quality/preference credit. Implementation: Planned Map metrics/formula registry. UI: Map metrics/formula disclosure. | Empty/single/multiple exact fixtures, metric/version dispatch, rebin, persistence, and label tests | Switch disclosed metric and inspect contributors/value |
| [ ] | `MAP-017` | not started | Report coverage and diversity without calling them quality. Implementation: Planned metrics. UI: Map summary. | Metric/label tests | Inspect summary |
| [ ] | `MAP-018` | not started | Provide an accessible sortable/list alternative linked to the same archive. Implementation: Planned archive table. UI: Map alternate view. | Table parity/accessibility tests | Keyboard/table workflow |
| [ ] | `MAP-022` | not started | At mobile widths below 768 CSS px, Map defaults to the complete action-parity List view; Grid remains an explicit labelled scroll/zoom alternative with sticky axis labels and no pinch-only dependency. Implementation: Planned responsive Map view state. UI: Mobile Map Grid/List switch. | Breakpoint, exact default, action-parity, persistence, keyboard, and no-pinch tests | Open Map at 390×844, exercise List actions, then switch Grid |
| [ ] | `CSC-021` | not started | Map computes every eligible descriptor and bins custom-scale candidates without catalogue-only branches or identity loss. Implementation: Planned Map/descriptor scale integration. UI: Map grid/list/inspector. | Descriptor/bin/rebin/identity tests | Populate/re-bin custom-scale Map |

## Milestone 9 — Complete Pareto and Pair Lab/co-evolution workflows

Exit gate: NSGA-II trade-offs and cooperative melody/beat co-evolution are genuine, deterministic, persistable, accessible, and verified in the browser; custom-scale Pareto integration closes and all six modes satisfy their shared distinct-state contract.

| Check | ID | Status | Deliverable and UI | Automated evidence | Browser evidence |
| --- | --- | --- | --- | --- | --- |
| [ ] | `PAR-014` | not started | `Stay in range` is a transparent objective with zero inside and normalized nearest-boundary distance outside, never implicit feasibility; hard constraints are a separate ordered list with stable IDs, units, bounds, normalized violations, feasible-first ordering, then violation-sum/vector/ID ties. Implementation: Planned Pareto objective transforms and constraint comparator. UI: Objective controls, Hard constraints, inspector. | Range transform, constraint independence, violation normalization, feasibility, and tie-order tests | Configure the same descriptor as objective and separate constraint; inspect rankings |
| [ ] | `PAR-001` | not started | Implement genuine deterministic NSGA-II-style multi-objective exploration with hard-constraint handling, nondominated sorting, Pareto ranks, crowding preservation, and reproduction. Implementation: Planned Pareto strategy. UI: Explore Pareto workspace. | Canonical algorithm tests | Configure/evolve/inspect front |
| [ ] | `PAR-002` | not started | Allow two to four user-selected descriptive objectives. Implementation: Planned objective state. UI: Objective controls. | Bounds/state tests | Configure 2/3/4 objectives |
| [ ] | `PAR-003` | not started | Every objective supports minimize. Implementation: Planned objective transform. UI: Objective controls. | Direction tests | Configure/inspect |
| [ ] | `PAR-004` | not started | Every objective supports maximize. Implementation: Planned objective transform. UI: Objective controls. | Direction tests | Configure/inspect |
| [ ] | `PAR-005` | not started | Every objective supports approach-target. Implementation: Planned objective transform. UI: Objective controls. | Distance-transform tests | Configure/inspect |
| [ ] | `PAR-006` | not started | Every `Stay in range` objective transforms its normalized value to zero inside the inclusive range and normalized distance to the nearest boundary outside; this transparent objective distance never changes feasibility, while hard constraints and their violations remain separately configured and evaluated. Implementation: Planned Pareto objective transform. UI: Objective controls/explanation. | Inclusive-boundary, inside/outside normalized-distance, and hard-constraint-independence tests | Configure overlapping range objective/hard constraint and inspect separate values |
| [ ] | `PAR-007` | not started | Never silently scalarize objectives. Implementation: Planned vector fitness/ranking. UI: Pareto explanation. | Ranking/source audit tests | Inspect all objective values |
| [ ] | `PAR-008` | not started | Display keyboard-focusable candidate points, nondominated frontier, dominated candidates, objective values/directions, and crowding/diversity information. Implementation: Planned scatter/front renderer. UI: Pareto chart. | Render/semantic tests | Keyboard/chart workflow |
| [ ] | `PAR-009` | not started | Provide compare tray. Implementation: Planned Pareto selection state. UI: Pareto workspace. | Selection/persistence tests | Add/audition/remove candidates |
| [ ] | `PAR-010` | not started | Provide an accessible table alternative with parity to chart. Implementation: Planned Pareto table. UI: Pareto alternate view. | Table parity/accessibility tests | Keyboard table workflow |
| [ ] | `PAR-011` | not started | With more than two objectives, choose display axes while retaining all objectives in evolution. Implementation: Planned display state separate from algorithm. UI: Axis controls. | Independence/ranking tests | Change display axes |
| [ ] | `PAR-012` | not started | Never describe the frontier as “best”; explain it as explicit trade-offs. Implementation: Product copy/docs. UI: Pareto workspace. | Label/copy tests | Text audit |
| [ ] | `PAR-013` | not started | Pareto layout links scatter/front table to candidate inspector. Implementation: Planned Pareto layout. UI: Pareto workspace/inspector. | Selection-link tests | Desktop/mobile inspection |
| [ ] | `CSC-022` | not started | Pareto evaluates objectives and separate constraints for custom-scale candidates without catalogue-only branches or identity loss. Implementation: Planned Pareto/descriptor scale integration. UI: Pareto chart/table/inspector. | Objective/constraint/ranking/identity tests | Run custom-scale Pareto and inspect |
| [ ] | `FORM-015` | not started | Implement and register exact immutable `pair-phenotype-distance-v2.0.0`: `.55` melody phenotype + `.30` beat + `.15` accent-coincidence difference, with both-beats-absent renormalization, one-beat-absent distance `1`, three-pair collaborator novelty, duplicate fallback counting once, and fewer-than-two-distinct returning zero. Implementation: Planned Pair Lab distance/novelty service and formula registry. UI: Pair formula/collaborator-novelty disclosure. | Exact weights, absence, duplicate, sparse-neighbour, symmetry/range, and version-dispatch tests | Inspect three pairings, component distances, and novelty |
| [ ] | `PAIR-001` | not started | Implement cooperative co-evolution with separate melody and beat populations. Implementation: Planned Pair Lab strategy. UI: Pair Lab workspace. | Population/lineage tests | Co-evolve workflow |
| [ ] | `PAIR-002` | not started | All pairings share meter, tempo, loop duration, and tick resolution. Implementation: Planned pairing compatibility contract. UI: Pair Lab transport. | Alignment tests | Swap partners while playing |
| [ ] | `PAIR-003` | not started | Lock one beat and evolve melodies. Implementation: Planned Pair Lab actions. UI: Pair Lab. | Lock/credit tests | Workflow |
| [ ] | `PAIR-004` | not started | Lock one melody and evolve beats. Implementation: Planned Pair Lab actions. UI: Pair Lab. | Lock/credit tests | Workflow |
| [ ] | `PAIR-005` | not started | Evolve both populations. Implementation: Planned Pair Lab reproduction. UI: Pair Lab. | Deterministic co-evolution tests | Workflow |
| [ ] | `PAIR-006` | not started | Freeze individual beat layers. Implementation: Planned layer locks. UI: Pair Lab. | Layer-preservation tests | Workflow |
| [ ] | `PAIR-007` | not started | Swap partners explicitly. Implementation: Planned pairing state. UI: Pair Lab. | Pair identity tests | Swap/audition |
| [ ] | `PAIR-008` | not started | Audition melody only. Implementation: Planned audition routing. UI: Pair Lab. | Routing tests | Audition |
| [ ] | `PAIR-009` | not started | Audition beat only. Implementation: Planned audition routing. UI: Pair Lab. | Routing tests | Audition |
| [ ] | `LOCK-010` | not started | Freeze individual beat layers in Pair Lab. Implementation: Planned beat-layer locks. UI: Pair Lab. | Layer preservation tests | Freeze/swap/evolve layers |
| [ ] | `PAIR-010` | not started | Audition melody and beat together. Implementation: Planned audition routing. UI: Pair Lab. | Synchronization tests | Audition |
| [ ] | `PAIR-011` | not started | Favorite melody components, beat components, and complete pairings independently. Implementation: Planned library item types. UI: Pair Lab/Library. | Separate persistence tests | Favorite/reuse each type |
| [ ] | `PAIR-012` | not started | Use deterministic collaborator sampling and a small hall of fame so components are evaluated with multiple partners. Implementation: Planned collaborator scheduler. UI: Pairing evidence/summary. | Schedule/hall-of-fame tests | Inspect partnerships |
| [ ] | `PAIR-013` | not started | Feedback options are Prefer melody, Prefer beat, Prefer pairing, and Reject pairing. Implementation: Planned explicit feedback domain. UI: Pair Lab feedback controls. | Credit-assignment tests | Exercise all feedback |
| [ ] | `PAIR-014` | not started | Never transfer pairing preference blindly to a component; keep melody, beat, and pairing parentage, feedback, evidence, and provenance separate. Implementation: Planned credit/lineage model. UI: Inspector. | Credit isolation/serialization tests | Inspect lineage/evidence |
| [ ] | `PAIR-015` | not started | Pair Lab layout has separate melody, beat, and active-pair panels with paired piano-roll/drum lanes. Implementation: Planned responsive Pair Lab layout. UI: Pair Lab workspace. | Layout/component tests | Desktop/mobile screenshots |
| [ ] | `PAIR-019` | not started | Implement `pair-lab-transition-v2.0.0` cold start: derive eight melodies and eight beats from selected sources and distinct labelled streams, form the active pair, start all three halls empty unless imported evidence explicitly seeds them, and use marked stable fallback rather than random sampling. Implementation: Planned Pair Lab cold-start transition. UI: Pair Lab cold-start/progress summary. | Exact population, stream, first-pair, empty/imported-hall, and fallback fixtures | Cold-start from each source shape and inspect IDs/halls/fallback |
| [ ] | `PAIR-020` | not started | Implement the transition's three-role collaborator schedule: deterministic strong, diversity, and opposite-hall collaborators with the specified evidence/distance comparators, rotating hall index, compatibility filtering, duplicate replacement, and stable fallback. Implementation: Planned Pair Lab collaborator scheduler. UI: Collaborator schedule/evidence summary. | Comparator, rotation, compatibility, duplicate, empty-hall, permutation, and replay tests | Inspect schedules across populated/empty halls and reload |
| [ ] | `PAIR-021` | not started | Keep transition evidence as isolated melody-positive, beat-positive, one-round exclusion, and ordered-pair positive/negative records; each feedback action mutates only its named vector and pair evidence never enters component reproduction keys. Implementation: Planned Pair Lab evidence reducer/schema. UI: Melody/beat/pair evidence panels. | Scope/action credit-isolation, ordered-pair, undo, persistence, and reproduction-key tests | Exercise every feedback action and inspect isolated evidence |
| [ ] | `PAIR-022` | not started | For each unlocked population, retain pinned components first, run deterministic evidence/novelty/ID tournaments, produce labelled crossover/mutation children under locks, merge parent/child candidates, deduplicate by distinct phenotype, and select exactly eight survivors when feasible. Implementation: Planned Pair Lab reproduction/survivor transition. UI: Reproduction/provenance/survivor summary. | Pin, comparator-order, crossover/mutation, lock, merge, dedup, survivor, permutation, and replay tests | Evolve melody and beat populations separately and inspect survivors |
| [ ] | `PAIR-023` | not started | A locked Pair Lab population is copied byte-for-byte, consumes no reproduction stream, and cannot be altered indirectly by collaborator, hall, repair, completion-order, or opposite-population work. Implementation: Planned Pair Lab locked-population transition. UI: Lock state/progress/provenance summary. | Exact-byte, zero-stream-consumption, opposite-work, hall, repair, and replay tests | Lock each side, evolve the other/both, and compare bytes/streams |
| [ ] | `PAIR-024` | not started | `Evolve both` computes melody and beat proposals from one immutable starting state with separate labelled streams, validates both and the next collaborator schedule before one atomic commit, and is invariant to worker completion order or cancellation. Implementation: Planned Pair Lab atomic dual transition/task adapter. UI: Evolve-both progress/cancel/result summary. | Immutable-start, two-proposal, validation, atomicity, cancellation, stale-result, permuted-completion, and replay tests | Evolve both; cancel/restart; reload and compare IDs/state |
| [ ] | `PAIR-025` | not started | Maintain exactly three separate capacity-four halls for melody components, beat components, and ordered pairings; admit only the explicitly preferred scope and apply the specified evidence, distinct-successful-collaborator, admission-generation, and ID ranking before stable replacement. Implementation: Planned Pair Lab hall service/schema. UI: Three hall panels and admission/replacement evidence. | Scope admission, capacity, ranking, successful-collaborator, tie, replacement, rejection-isolation, codec, and replay tests | Fill/challenge each hall and inspect persisted decisions |
| [ ] | `PAIR-026` | not started | Pair transition retries and deterministic immigrants obey the shared attempt budget, expose every rejection/fallback, preserve elites and locks, return a visibly bounded underfill when eight distinct valid survivors are impossible, and never fabricate variation. Implementation: Planned Pair Lab bounded-output transition. UI: Underfill/fallback/progress/error summary. | Attempt-ceiling, forced-rejection, immigrant, elite/lock, underfill, termination, and replay tests | Trigger constrained success/fallback/underfill and inspect ledger |
| [ ] | `PAIR-016` | not started | After the Pair Lab state/evidence/transition schemas are frozen, freeze and implement the independent melody-component JSON closure. The outer envelope is exactly `{kind:'melody-forge-melody-component', schemaVersion:2, component:MelodyComponentExportBundleV2}` in declared order. Before this row can be done, the bundle and every melody-component evidence/lineage support record must have exact version/root-ID/table-key/table-type/key-order/identity/reference/reachability codecs: exactly one root melody candidate plus its complete strong genome, timing profile or transport, tonal/custom-scale, scoped lock, descriptor, provenance, component-evidence, and component-lineage closure; no unreachable rows and no beat, pairing, or pair-credit ownership. Strict kind/version/reference/shared-timing validation precedes mutation; canonical UTF-8 encoding decode-checks itself; raw or encoded size above 5,242,880 bytes rejects; import previews/creates only the named Library/Pair item and incompatible timing uses explicit non-destructive adaptation. Implementation: Planned exact MelodyComponentExportBundleV2/evidence/lineage schemas, closure assembler, strict PairLabJsonCodec method, and browser adapter. UI: Pair Lab/Library melody-component export and import/adaptation preview. | Exact outer/bundle/table/key/order/version/identity/reference/reachability and forbidden-owner tests; canonical-byte/hash/decode-after-encode; 5,242,880/5,242,881 raw/encoded bounds; timing/adaptation and atomic nonreplacement tests | Export twice, import/save/open/audition, inspect complete tables/credit ownership, and exercise wrong-kind, extra/missing-row, oversize, and incompatible-timing failures |
| [ ] | `PAIR-017` | not started | After the Pair Lab state/evidence/transition schemas are frozen, freeze and implement the independent beat-component JSON closure. The outer envelope is exactly `{kind:'melody-forge-beat-component', schemaVersion:2, component:BeatComponentExportBundleV2}` in declared order. Before this row can be done, the bundle and every beat-component evidence/lineage/provenance support record must have exact version/root-ID/table-key/table-type/key-order/identity/reference/reachability codecs: exactly one root beat genome plus its strong transport, BeatLockSet/layer locks, descriptors, variation/seed provenance, component evidence, and component lineage; no unreachable rows and no melody, pairing, or pair-credit ownership. Strict kind/version/reference/shared-timing validation precedes mutation; canonical UTF-8 encoding decode-checks itself; raw or encoded size above 5,242,880 bytes rejects; import previews/creates only the named Library/Pair item and incompatible timing uses explicit non-destructive adaptation. Implementation: Planned exact BeatComponentExportBundleV2/evidence/lineage/provenance schemas, closure assembler, strict PairLabJsonCodec method, and browser adapter. UI: Pair Lab/Library beat-component export and import/adaptation preview. | Exact outer/bundle/table/key/order/version/identity/reference/reachability and forbidden-owner tests; lane/lock/provenance closure; canonical-byte/hash/decode-after-encode; 5,242,880/5,242,881 raw/encoded bounds; timing/adaptation and atomic nonreplacement tests | Export twice, import/save/pair/audition, inspect complete tables/credit ownership, and exercise wrong-kind, extra/missing-row, oversize, and incompatible-timing failures |
| [ ] | `PAIR-018` | not started | After the component, state, evidence, lineage, and transition schemas are frozen, freeze and implement the portable complete-pairing JSON closure. The outer envelope is exactly `{kind:'melody-forge-pairing', schemaVersion:2, pairing:PairingExportBundleV2}` in declared order. Before this row can be done, the pairing bundle and ordered-pair evidence/lineage records must have exact version/root-ID/table-key/table-type/key-order/identity/reference/reachability codecs and contain exactly one root AuditionPairing, one complete melody-component bundle, exactly one complete beat-component bundle when `beatId` is non-null or explicit null otherwise, exactly its resolved PerformanceSettings row, and only that ordered pair's evidence/lineage. Bundle root IDs, null-beat equivalence, every shared timing relation, and separate component identities/credit are validated; no component credit is inferred or merged and no unreachable rows are accepted. The entire envelope has one 5,242,880-byte raw/encoded budget with no multiplier, canonical UTF-8 decode-after-encode, named-item-only preview/creation, atomic failure, and explicit non-destructive timing adaptation. Implementation: Planned exact PairingExportBundleV2/pair-evidence/lineage schemas, portable closure assembler, strict PairLabJsonCodec method, and browser adapter. UI: Pair Lab/Library complete-pairing export and import/adaptation preview. | Exact outer/bundle/table/key/order/version/root/null-beat/performance/evidence/reference/reachability tests; component-credit isolation; canonical-byte/hash/decode-after-encode; one-envelope 5,242,880/5,242,881 bounds; timing/adaptation and atomic nonreplacement tests | Export twice/reimport pairing, swap/audition, inspect both component closures and pair-only evidence, and exercise null-beat, wrong-kind, extra/missing-row, oversize, and incompatible-timing failures |
| [ ] | `EVO-001` | not started | Implement six genuinely distinct modes—Breed, Drift, Islands, Map, Pareto, Pair Lab—sharing domain, transport, playback, persistence, provenance, and export. Implementation: Planned strategy interfaces/mode state. UI: Evolve/Explore destinations. | Strategy identity/integration tests | One full workflow per mode |
| [ ] | `EVO-002` | not started | Each mode has distinct state, algorithm, layout, visualization, persistence, deterministic tests, documentation, and browser workflow; labels over one mutation routine are insufficient. Implementation: Planned mode modules. UI: Mode workspaces. | Mode-specific contract/tests | Mode screenshots/workflows |
| [ ] | `EVO-003` | not started | Switching modes preserves each mode’s state and never mutates/discards work; seeding another mode is explicit. Implementation: Planned root/mode state. UI: Mode navigation/seed actions. | State-preservation tests | Switch/return/seed workflow |

## Milestone 10 — MIDI import, transformations, Library, branching history, tournament, and continuous audition

Exit gate: Every remaining creative loop, import/export path, branch, shortcut, audition mode, and Library reuse path round-trips and restores exact state, including the final custom-scale Library/I/O/transform/history integrations and whole-product custom-scale closure.

| Check | ID | Status | Deliverable and UI | Automated evidence | Browser evidence |
| --- | --- | --- | --- | --- | --- |
| [ ] | `SEL-001` | not started | Blind A/B tournament hides origin until decision and offers A, B, neither, or both. Implementation: Planned tournament state. UI: Tournament overlay/workspace. | Blind-state/choice tests | Exercise four decisions |
| [ ] | `SEL-002` | not started | Tournament result feeds the active evolutionary mode. Implementation: Planned mode feedback adapters. UI: Tournament result. | Integration/credit tests | Decide then evolve |
| [ ] | `SEL-003` | not started | Tournament order/naming never changes creative randomness; provenance appears only after selection. Implementation: Planned UI-only shuffle stream. UI: Tournament reveal. | Randomness-isolation/reveal tests | Repeat with swapped order |
| [ ] | `SEL-004` | not started | Continuous audition automatically advances a selected population and visibly marks the playing candidate. Implementation: Planned audition queue. UI: Population controls/cards. | Queue/controller tests | Continuous audition workflow |
| [ ] | `SEL-005` | not started | During continuous audition, favorite, select parent/anchor, skip, and stop without leaving flow. Implementation: Planned queue actions. UI: Audition controls. | Action/state tests | Exercise all actions |
| [ ] | `SEL-006` | not started | Breed Blind A/B persists equality hashes and A/B/Neither/Both evidence; chosen/Both explicitly fill parent slots, Neither leaves slots and excludes both for one tournament, and preference only breaks ties after feasibility/elite before novelty/ID. Implementation: Planned Breed preference adapter. UI: Breed tournament/reveal/parent slots. | Four-outcome state, equality, undo, tie-order, and seed-isolation tests | Exercise four outcomes then run Breed |
| [ ] | `SEL-007` | not started | Drift Blind A/B updates descendant/operator positive evidence or Neither negative evidence, never changes anchor, and orders next same-anchor operator buckets by positive-minus-negative then stable operator ID; only Promote changes anchor. Implementation: Planned Drift preference adapter. UI: Drift tournament/reveal. | Four-outcome evidence, operator ordering, anchor, undo, and replay tests | Exercise four outcomes, evolve, then explicit Promote |
| [ ] | `SEL-008` | not started | Islands Blind A/B updates only island-candidate preference or one-round exclusions; after pinned/feasibility it precedes novelty in survivor/migrant ties and never pins or migrates by itself. Implementation: Planned Islands preference adapter. UI: Islands tournament/reveal. | Four-outcome, exclusion expiry, survivor/migrant tie, pin isolation, and replay tests | Decide within/across lanes then evolve/migrate |
| [ ] | `SEL-009` | not started | Map Blind A/B records preferred-over edges, equal observations, or one-batch exclusions; direct edges break only equal-rating replacement tiers and never change axes, descriptors, bins, pins, or ratings. Implementation: Planned Map preference adapter. UI: Map tournament/reveal/cell explanation. | Four-outcome, equal-rating comparator, exclusion expiry, state-isolation, and rebin tests | Decide then challenge/re-bin a cell |
| [ ] | `SEL-010` | not started | Pareto Blind A/B updates only reproduction preference/exclusions after feasibility, rank, and crowding; it never changes objective values, dominance, rank, frontier, chart axes, or constraints. Implementation: Planned Pareto preference adapter. UI: Pareto tournament/reveal. | Four-outcome, parent-tie, exclusion expiry, objective/front isolation, undo tests | Decide then run/reinspect unchanged frontier semantics |
| [ ] | `SEL-011` | not started | Pair Lab Blind A/B applies only the selected Melody, Beat, or Pair scope; component Neither creates one-round exclusions, pair Neither rejects each ordered pair, and no melody/beat/pair credit crosses scope. Implementation: Planned Pair Lab preference adapter. UI: Pair Lab scoped tournament/reveal. | Scope×four-outcome, ordered-pair, exclusion, undo, equality, and credit-isolation tests | Exercise every scope/outcome then co-evolve |
| [ ] | `SEL-012` | not started | Melody-scope Blind A/B requires one identical locked-beat hash plus identical transport/performance hashes for A and B, persists those fairness hashes in `PreferenceRecordV2`, and displays `Same locked beat` without exposing hidden origin. Implementation: Planned tournament fairness validator/record codec. UI: Melody-scope Blind A/B fairness summary/reveal. | Same/different beat, transport, performance, record-codec, rejection, and hidden-origin tests | Open valid/invalid Melody comparisons and inspect summary/record |
| [ ] | `SEL-013` | not started | Beat-scope Blind A/B requires one identical melody hash plus identical transport/performance hashes for A and B, persists those fairness hashes in `PreferenceRecordV2`, and displays `Same melody` without claiming beat equality. Implementation: Planned tournament fairness validator/record codec. UI: Beat-scope Blind A/B fairness summary/reveal. | Same/different melody, transport, performance, record-codec, rejection, and copy tests | Open valid/invalid Beat comparisons and inspect summary/record |
| [ ] | `SEL-014` | not started | Pair-scope Blind A/B permits both components to differ but requires identical transport/performance and normalized-routing hashes, persists them in `PreferenceRecordV2`, and displays `Same transport and routing` without claiming melody or beat equality. Implementation: Planned tournament fairness validator/record codec. UI: Pair-scope Blind A/B fairness summary/reveal. | Same/different transport, performance, normalized-routing, record-codec, rejection, and copy tests | Open valid/invalid Pair comparisons and inspect summary/record |
| [ ] | `HIST-001` | not started | Replace linear destructive navigation with genuine branch-preserving history; evolving an old node creates a branch and never erases later work silently. Implementation: Planned history graph. UI: Branch strip/tree. | Fork/restore tests | Fork older generation/reload |
| [ ] | `HIST-009` | not started | Keep undo/redo distinct from evolutionary-history navigation. Implementation: Planned command/history state. UI: Shell/history strip. | Independence tests | Use undo then branch navigation |
| [ ] | `SEL-015` | not started | Every committed Blind A/B decision creates an immutable `preference-record-v2`: allocate its project-global non-negative safe-integer `occurrenceOrdinal` from persisted `nextPreferenceOccurrence`, increment and never decrement/reuse it across undo, branch navigation, reload, or an identical repeated decision; derive its ID exactly as `stableId('preference', preferenceRecordWithoutId)` over every immutable field; store the typed A/B items and neutral playback order, mode/generation reference, scope/outcome, fairness fingerprints, and the exact canonical `preference-effect-v2` payload for that mode/scope/items/outcome; retain the record in an immutable project decision log while a separate active-record-ID membership controls its invertible effect, so undo deactivates/inverts without deleting the record or releasing its ordinal. Implementation: Planned preference record codec, ordinal allocator, canonical effect adapter, decision log, and undo membership. UI: Blind A/B Reveal Technical details and undo/history evidence. | Schema/item/fairness-union, monotonic ordinal across repeat/undo/branch/reload, all-field stable-ID golden/sensitivity, mode×scope×outcome canonical-effect/rejection, immutable-log, active-membership, undo/redo, and forbidden-side-effect tests | Commit an identical decision twice, inspect distinct ordinals/IDs and exact effect evidence, then undo/reload/branch and verify retained inactive history with no ordinal reuse |
| [ ] | `AUD-001` | not started | Continuous audition accepts exactly a configured one-loop or two-loop count and advances only after that many complete loop ends, while visibly marking the current item and loop count. Implementation: Planned audition queue/controller. UI: Continuous-audition bar and playing item. | One/two-loop, exact-end, partial-loop, queue-order, and visible-state tests | Run both loop counts across population/Map/Library queues |
| [ ] | `AUD-002` | not started | Continuous audition keeps Previous, Skip/Next, Favorite, mode-appropriate Parent/Anchor, Stop, and Resume available; Stop cancels audio/auto-advance but retains the queue, while End audition alone clears transient queue state. Implementation: Planned audition queue actions/state. UI: Continuous-audition bar. | Per-action, persistence, stop/resume, end/clear, focus, and no-stale-schedule tests | Exercise every control, stop/resume, and end |
| [ ] | `AUD-003` | not started | Manual Play outside an active continuous-audition queue pauses that queue and requires `Resume queue` or `End audition`; it never leaves two schedules active or silently discards the queue. Implementation: Planned audition/transport arbitration. UI: Candidate Play and audition resume prompt. | Arbitration, prompt, queue-retention, schedule-atomicity, and cancellation tests | Play outside queue, choose each response, and inspect playback |
| [ ] | `KEY-001` | not started | Number keys audition visible candidates. Implementation: Planned shortcut service. UI: All candidate grids. | Shortcut/focus tests | Keyboard-only workflow |
| [ ] | `KEY-002` | not started | Space plays/stops focused candidate. Implementation: Planned shortcut service. UI: All candidate views. | Shortcut/focus tests | Keyboard-only workflow |
| [ ] | `KEY-003` | not started | Arrow keys move focused candidate or chart point. Implementation: Planned roving focus. UI: Grids/charts. | Navigation tests | Keyboard-only workflow |
| [ ] | `KEY-004` | not started | `P` selects parent/anchor, `F` favorites, and `E` performs current primary evolution action. Implementation: Planned shortcut service. UI: All applicable modes. | Shortcut/action tests | Keyboard-only workflow |
| [ ] | `KEY-005` | not started | Ignore creative shortcuts while typing in form fields; document shortcuts and provide visible equivalent controls. Implementation: Planned shortcut guard/docs. UI: Help/visible controls. | Input-guard/equivalence tests | Type in forms and use visible actions |
| [ ] | `HIST-002` | not started | Persist/restore Create settings, populations, and parent selection. Implementation: Planned project schema. UI: Create/Evolve. | Round-trip tests | Reload state |
| [ ] | `HIST-003` | not started | Persist/restore Drift trails and anchors. Implementation: Planned mode schema. UI: Drift. | Round-trip tests | Reload state |
| [ ] | `HIST-004` | not started | Persist/restore island populations, elites, diversity, and migration. Implementation: Planned mode schema. UI: Islands. | Round-trip tests | Reload state |
| [ ] | `HIST-005` | not started | Persist/restore Map archive, challenger queues, pins, and axis state. Implementation: Planned mode schema. UI: Map. | Round-trip tests | Reload state |
| [ ] | `HIST-006` | not started | Persist/restore Pareto objectives, populations, ranks/fronts, and display state. Implementation: Planned mode schema. UI: Pareto. | Round-trip tests | Reload state |
| [ ] | `HIST-007` | not started | Persist/restore co-evolution populations, partnerships, feedback, and hall of fame. Implementation: Planned mode schema. UI: Pair Lab. | Round-trip tests | Reload state |
| [ ] | `HIST-008` | not started | Persist/restore tonal timelines, custom scales, beats, locks, ratings, annotations, seeds, and algorithm versions. Implementation: Planned project schema. UI: Inspector/Library. | Complete round-trip tests | Reload/import state |
| [ ] | `IMP-001` | not started | Import monophonic Standard MIDI through preview-and-confirm, never direct destructive adaptation. Implementation: Planned MIDI parser/import proposal. UI: Import dialog. | Parser/confirmation tests | Import/confirm/cancel |
| [ ] | `IMP-002` | not started | Preview available tracks and permit selecting a monophonic track from multi-track MIDI. Implementation: Planned import model. UI: Import dialog. | Track parsing/selection tests | Multi-track workflow |
| [ ] | `IMP-003` | not started | Preview detected tempo and meter. Implementation: Planned metadata parser. UI: Import dialog. | Meta parsing tests | Inspect preview |
| [ ] | `IMP-004` | not started | Preview exact event timing and detected polyphony. Implementation: Planned event analyzer. UI: Import dialog/piano roll. | Timing/polyphony tests | Inspect preview |
| [ ] | `IMP-005` | not started | Preview proposed quantization and phrase length. Implementation: Planned quantization proposal. UI: Import dialog. | Proposal tests | Change/inspect proposal |
| [ ] | `IMP-006` | not started | Preview proposed tonic/scale matches. Implementation: Planned tonal detector. UI: Import dialog. | Ranked-match tests | Inspect choices |
| [ ] | `IMP-007` | not started | Preview register adaptation and every trimming, padding, snapping, or conversion. Implementation: Planned adaptation proposal. UI: Import dialog. | Diff/proposal tests | Inspect destructive changes |
| [ ] | `IMP-008` | not started | Polyphonic input is either rejected actionably or handled only by an explicitly selected deterministic monophonic extraction strategy; never silently drop notes. Implementation: Planned polyphony policy. UI: Import dialog/error state. | Strategy/rejection tests | Polyphonic workflow |
| [ ] | `IMP-009` | not started | Confirmed imports become normal versioned candidates usable by every workflow. Implementation: Planned import strategy/schema. UI: All destinations. | Cross-mode integration tests | Import then seed/evolve/export |
| [ ] | `IMP-010` | not started | Reject project JSON larger than exactly 50 MiB = 52,428,800 raw file bytes before UTF-8 decoding, parsing, or allocation-heavy validation and state the exact limit without changing active data. Implementation: Planned import byte-limit gate. UI: Project import error/preview. | Byte boundary-minus-one/exact/plus-one, predecode/preparse, copy, and nonreplacement tests | Select oversized project JSON and verify actionable unchanged state |
| [ ] | `IMP-011` | not started | Reject candidate and each Pair Lab melody-component, beat-component, or pairing JSON envelope larger than exactly 5 MiB = 5,242,880 raw file bytes before UTF-8 decoding, parsing, or allocation-heavy validation. Implementation: Planned import byte-limit gate. UI: Candidate/component/pair import error/preview. | Per-envelope byte boundary-minus-one/exact/plus-one, predecode/preparse, and nonreplacement tests | Select each oversized individual JSON kind and verify unchanged state |
| [ ] | `IMP-012` | not started | Reject MIDI larger than exactly 5 MiB = 5,242,880 raw file bytes before parsing or allocation-heavy analysis and state the exact limit without changing project or preview state. Implementation: Planned MIDI byte-limit/parser gate. UI: MIDI import error/preview. | Byte boundary-minus-one/exact/plus-one, preparse/preanalysis, copy, preview, and nonreplacement tests | Select oversized MIDI and verify actionable unchanged state |
| [ ] | `IMP-013` | not started | A truly polyphonic selected track cannot proceed until the user explicitly chooses exactly Highest note, Lowest note, Earliest-onset priority, or Reject import and confirms the resulting extraction/rejection decision; no note is dropped beforehand. Implementation: Planned MIDI polyphony proposal/decision domain. UI: MIDI Adapt/Confirm steps. | Exact four-choice, unchosen/confirmed/cancelled, no-predrop, codec, and source-immutability tests | Import polyphony, inspect four choices, cancel and confirm each |
| [ ] | `IMP-014` | not started | `midi-monophonic-extraction-v2.0.0` Highest sweeps every source-PPQ half-open note start/end boundary with note-off before note-on, assigns each atomic span by key pitch descending then source note-on ordinal, and coalesces adjacent spans only for the same source note ID. Implementation: Planned MIDI monophonic sweep extractor. UI: MIDI extraction preview/provenance. | Boundary ordering, overlap/nesting/touching, pitch/ordinal tie, atomic ownership, coalescing, permutation, and replay tests | Choose Highest and inspect exact span ownership/result |
| [ ] | `IMP-015` | not started | `midi-monophonic-extraction-v2.0.0` Lowest uses the same half-open boundary sweep but assigns each atomic span by key pitch ascending then source note-on ordinal and coalesces adjacent spans only for the same source note ID. Implementation: Planned MIDI monophonic sweep extractor. UI: MIDI extraction preview/provenance. | Boundary ordering, overlap/nesting/touching, pitch/ordinal tie, atomic ownership, coalescing, permutation, and replay tests | Choose Lowest and inspect exact span ownership/result |
| [ ] | `IMP-016` | not started | `midi-monophonic-extraction-v2.0.0` Earliest-onset priority uses the same half-open boundary sweep and assigns each atomic span by onset ascending, source note-on ordinal, then pitch; losing and regaining ownership creates stable separate fragments rather than bridging the gap. Implementation: Planned MIDI monophonic sweep extractor. UI: MIDI extraction preview/provenance. | Boundary ordering, onset/ordinal/pitch tie, loss/regain fragmentation, atomic ownership, permutation, and replay tests | Choose Earliest-onset and inspect exact spans/fragments |
| [ ] | `IMP-017` | not started | Reject import for a polyphonic track performs no extraction or project mutation, retains the source/preview for another choice or cancellation, and reports the track-specific polyphony reason actionably. Implementation: Planned MIDI polyphony decision/import transaction. UI: MIDI extraction error/preview. | Reject, retry-choice, cancel, source-retention, focus, and byte-exact nonmutation tests | Choose Reject, retry another choice, then cancel |
| [ ] | `IMP-018` | not started | MIDI quantization defaults exactly to Off and preserves positive ordered representable converted ticks; eighth-note or sixteenth-note suggestions never apply implicitly, and every snap/repair/trim/pad/register/meter adaptation requires checked confirmation. Implementation: Planned quantization proposal/default and import transaction. UI: MIDI Adapt/Confirm steps. | Exact Off default, suggestion-only, tick preservation, each destructive adaptation, confirmation, cancel, and replay tests | Import quantized/unquantized files, inspect Off, and confirm/cancel changes |
| [ ] | `IMP-019` | not started | `midi-import-bounds-v2.0.0` accepts at most 64 declared MIDI tracks and rejects 65 or more before allocation-heavy analysis, reporting measured count/limit without changing project or preview state. Implementation: Planned bounded MIDI reader. UI: MIDI file/track bound error. | 63/64/65, malformed declaration, early-reject, measured-value, copy, and nonmutation tests | Import 64/65-track fixtures and inspect error/state |
| [ ] | `IMP-020` | not started | `midi-import-bounds-v2.0.0` accepts at most 200,000 total decoded MIDI, meta, and SysEx events across all tracks; ignored metadata and unsupported events still count and 200,001 rejects atomically. Implementation: Planned bounded MIDI reader/event counter. UI: MIDI event-count bound error. | 199,999/200,000/200,001, ignored/unsupported bypass, multitrack-total, early-stop, and nonmutation tests | Import boundary event-count fixtures and inspect error/state |
| [ ] | `IMP-021` | not started | `midi-import-bounds-v2.0.0` accepts per-event delta ticks only through Standard MIDI variable-length maximum `0x0fffffff`; a larger, overflowing, truncated, or malformed delta rejects with event/track evidence and no state change. Implementation: Planned bounded MIDI reader/delta decoder. UI: MIDI delta bound error. | Max/plus-one, encoded-length, overflow/truncation/malformed, track/event path, and nonmutation tests | Import boundary/malformed-delta fixtures and inspect error |
| [ ] | `IMP-022` | not started | `midi-import-bounds-v2.0.0` accepts greatest absolute event/end tick at most 4,096 source quarter-note beats from zero, computed as `absoluteTick <= sourcePpq × 4096` with overflow-safe integer/rational arithmetic. Implementation: Planned bounded MIDI reader/timeline accumulator. UI: MIDI duration bound error. | Below/exact/above across PPQ extremes, running-delta overflow, end-event, multitrack, measured-value, and nonmutation tests | Import duration-boundary fixtures and inspect error/state |
| [ ] | `IMP-023` | not started | MIDI import accepts format 0 or 1 with PPQ division exactly 1–32,767; zero/32,768, SMPTE division, and format 2 each reject with distinct actionable issue codes before project mutation. Implementation: Planned MIDI header/division validator. UI: MIDI format/division error. | Format 0/1/2, PPQ 0/1/32,767/32,768, SMPTE variants, issue-code, and nonmutation tests | Import each supported/unsupported header and inspect error |
| [ ] | `IMP-024` | not started | Hitting any `midi-import-bounds-v2.0.0` byte/track/event/delta/duration/header cap reports the measured value and exact limit, retains both project and prior preview byte-for-byte, and cannot be bypassed through ignored or unsupported content. Implementation: Planned bounded MIDI reader/import transaction. UI: MIDI bound error/recovery UI. | Cap×phase, measured-copy, preview/project snapshot, ignored-content bypass, cancellation, and retry tests | Trigger every cap, verify unchanged preview/project, then retry |
| [ ] | `IMP-025` | not started | Every `midi-monophonic-extraction-v2.0.0` output fragment ID is derived from source hash, track, source note-on ordinal, and fragment ordinal; preview/provenance classifies each source note as kept, discarded, shortened, or split with exact source/output spans. Implementation: Planned MIDI extraction identity/provenance service. UI: MIDI extraction preview/technical provenance. | Stable-ID component, fragmentation ordinal, classification, exact-span, codec, replay, and source-immutability tests | Inspect kept/discarded/shortened/split fixture IDs and provenance |
| [ ] | `CREATE-001` | not started | Create supports Legacy generator with V1 behavior preserved exactly. Implementation: Existing generator behind versioned strategy. UI: Create. | Golden ordered-output tests | Generate/audition Legacy |
| [ ] | `CREATE-002` | not started | Create supports Modern generator with V1 behavior preserved. Implementation: Existing generator behind versioned strategy. UI: Create. | Golden ordered-output tests | Generate/audition Modern |
| [ ] | `CREATE-003` | not started | Create supports Import MIDI. Implementation: Planned MIDI import adapter. UI: Create. | Parse/preview/adaptation tests | Import workflow |
| [ ] | `IO-001` | not started | Keep whole-project import/export separate from individual-candidate export. Implementation: Planned distinct envelopes/actions. UI: Project menu vs candidate inspector. | Envelope/routing tests | Download/import both kinds |
| [ ] | `IO-002` | not started | Support versioned complete project JSON. Implementation: Planned project schema/codecs. UI: Project menu. | Complete deterministic round-trip tests | Export/import/reload |
| [ ] | `IO-003` | not started | Support versioned individual-candidate JSON. Implementation: Planned candidate schema/codecs. UI: Candidate/Library export. | Complete deterministic round-trip tests | Export/open candidate |
| [ ] | `IO-004` | not started | JSON round trips preserve root/sub-seeds and algorithm versions. Implementation: Planned codecs. UI: Technical inspector. | Golden round-trip tests | Export/import/compare |
| [ ] | `IO-005` | not started | JSON round trips preserve transport, exact events, and tonal timeline. Implementation: Planned codecs. UI: Transport/roll. | Golden round-trip tests | Export/import/audition |
| [ ] | `IO-006` | not started | JSON round trips preserve custom scales, borrowing, and beat. Implementation: Planned codecs. UI: Inspector/drum lane. | Golden round-trip tests | Export/import/inspect |
| [ ] | `IO-007` | not started | JSON round trips preserve locks, descriptors, lineage/history, ratings, and annotations. Implementation: Planned codecs. UI: History/Library/inspector. | Golden round-trip tests | Export/import/inspect |
| [ ] | `IO-008` | not started | Invalid/unsupported imports fail safely with actionable errors and never replace active project without explicit confirmation. Implementation: Planned staged import transaction. UI: Import/recovery dialog. | Failure/nonreplacement tests | Invalid/unsupported/cancel workflows |
| [ ] | `LIB-001` | not started | Provide a proper local Library containing migrated V1 favorites and saved V2 components/pairings. Implementation: Planned library repository/schema. UI: Library destination. | Migration/repository tests | Open/reload Library |
| [ ] | `LIB-002` | not started | Library item can be renamed. Implementation: Planned metadata actions. UI: Library/inspector. | Validation/persistence tests | Rename/reload |
| [ ] | `LIB-003` | not started | Library item can have a short annotation. Implementation: Planned metadata actions. UI: Library/inspector. | Validation/persistence tests | Annotate/reload |
| [ ] | `LIB-004` | not started | Library supports search and filters for name, tonic, scale, meter, origin, mode, and favorite state. Implementation: Planned library index/query. UI: Library controls. | Query/filter tests | Exercise each filter |
| [ ] | `LIB-005` | not started | Library item can be auditioned and opened. Implementation: Planned library actions. UI: Library. | Playback/open tests | Audition/open |
| [ ] | `LIB-006` | not started | Library item can seed Breed parent, Drift anchor, Islands, Map, Pareto, or Pair Lab. Implementation: Planned mode seed adapters. UI: Library actions. | Per-mode seed tests | Seed each mode |
| [ ] | `LIB-007` | not started | Library items can be combined only when compatible or after explicit adaptation. Implementation: Planned compatibility service. UI: Library/adaptation preview. | Compatibility tests | Combine incompatible items |
| [ ] | `LIB-008` | not started | Library item can be exported. Implementation: Planned export actions. UI: Library/inspector. | Export tests | Download exports |
| [ ] | `LIB-009` | not started | Project and Library item names accept at most 80 Unicode characters, validate deterministically by Unicode code points, expose the remaining/limit count, and reject overflow without truncating or changing identity. Implementation: Planned metadata validator/actions. UI: Project rename and Library rename. | Unicode boundary, normalization-policy, persistence, and identity tests | Rename at/over limit and reload |
| [ ] | `LIB-010` | not started | Library short annotations accept at most 280 Unicode characters, validate deterministically by Unicode code points, expose the remaining/limit count, and reject overflow without truncation or candidate identity change. Implementation: Planned metadata validator/actions. UI: Library/inspector annotation editor. | Unicode boundary, persistence, cancel, and identity tests | Annotate at/over limit, switch focus, and reload |
| [ ] | `LIB-011` | not started | Library defaults to the dense responsive Grid view; List is an explicit sortable action-parity alternative, and changing either view affects presentation only. Implementation: Planned Library view state. UI: Library Grid/List switch. | Exact default, action/data parity, sort/view independence, persistence, and accessibility tests | Open Library fresh, switch/sort both views, and compare items/actions |
| [ ] | `CREATE-004` | not started | Create supports Start from Library. Implementation: Planned library seeding. UI: Create/Library. | Seed-copy tests | Open library item in Create |
| [ ] | `CREATE-005` | not started | After generation, collapse detailed settings to a readable one-line summary with Edit action so candidates start near viewport top. Implementation: Planned Create disclosure state. UI: Create workspace. | Summary/edit tests | Generate and inspect viewport |
| [ ] | `CREATE-006` | not started | Direct transform: keep rhythm and regenerate pitches deterministically. Implementation: Planned transformations. UI: Candidate actions/inspector. | Lock/seed tests | Apply/compare |
| [ ] | `CREATE-007` | not started | Direct transform: keep pitches and regenerate rhythm deterministically. Implementation: Planned transformations. UI: Candidate actions/inspector. | Lock/seed tests | Apply/compare |
| [ ] | `CREATE-008` | not started | Direct transform: remap tonic. Implementation: Planned tonal remapper. UI: Candidate actions/inspector. | All-tonic/timing tests | Apply/compare |
| [ ] | `CREATE-009` | not started | Direct transform: remap scale. Implementation: Planned degree mapper. UI: Candidate actions/inspector. | Cross-cardinality tests | Preview/apply |
| [ ] | `CREATE-010` | not started | Direct transform: Near/Medium/Far variants. Implementation: Planned distance-calibrated variants. UI: Candidate actions. | Distance tests | Generate/audition |
| [ ] | `CREATE-011` | not started | Direct transform: continuation preserving recognizable motif/contour, transport, tonal constraints, and provenance. Implementation: Planned phrase transformation. UI: Candidate actions. | Relationship/invariant tests | Generate/audition continuation |
| [ ] | `CREATE-012` | not started | Direct transform: answering phrase preserving recognizable motif/contour, transport, tonal constraints, and provenance. Implementation: Planned phrase transformation. UI: Candidate actions. | Relationship/invariant tests | Generate/audition answer |
| [ ] | `CREATE-013` | not started | Continuations/answers remain short-form melody generation, not song arrangement. Implementation: Product/domain bounds. UI: Create. | Length/scope tests | UI/workflow audit |
| [ ] | `CSC-023` | not started | Library stores, searches, filters, opens, auditions, and seeds workflows from custom-scale items with stable scale identity. Implementation: Planned Library scale index/adapters. UI: Library. | Repository/query/seed/identity tests | Save/reload/filter/open/seed custom item |
| [ ] | `CSC-025` | not started | Project and individual JSON import validate custom-scale masks, identity, version, references, and degeneracy transactionally before proposing any commit. Implementation: Planned JSON decoders/import transaction. UI: JSON import preview/errors. | Valid/invalid/future/reference import tests | Import valid and invalid custom-scale JSON |
| [ ] | `CSC-026` | not started | Project, candidate, and applicable component JSON export include every reachable custom scale as the exact declared-order `CustomScaleV2` structural fields plus its complete merged `ScaleProvenanceV2`/`ScaleOriginV2` set; catalogue matches remain catalogue references and emit no custom row. Import validates canonical IDs/derived labels/order, merges same-ID provenance by stable origin-ID union, rejects a structural mismatch or same-origin differing bytes transactionally, and reimport is byte-stable and idempotent; aliases and derived relationship-graph results are not invented serialized custom-scale fields. Implementation: Planned project/candidate/component JSON codecs and custom-scale provenance merger. UI: Export/import preview and Technical inspector. | Canonical key/table/order golden, complete reachability, catalogue-ref/no-row, stable-union permutation/idempotence, same-origin collision, transactional rollback, and exact round-trip tests | Export/reimport project and individual closures with convergent custom-scale origins, compare bytes/IDs/origins, and inspect catalogue/collision behavior |
| [ ] | `CSC-028` | not started | Direct remap, continuation, answering phrase, Drift, and Islands transformations accept custom scales through the shared registry and preserve/remap identity explicitly. Implementation: Planned transform/mode scale adapters. UI: Create/Drift/Islands. | Per-surface legality/identity/provenance tests | Exercise each custom-scale transform/mode |
| [ ] | `CSC-029` | not started | Scale inspector, relationships, piano roll, candidate cards, History, and technical provenance render custom scales with the same capabilities as catalogue scales. Implementation: Planned shared scale presentation model. UI: All scale presentation surfaces. | Cross-view parity/accessibility tests | Inspect one custom scale in every named surface |
| [ ] | `CSC-016` | not started | Custom scales work in generation, breeding, borrowing, modulation, Map, Pareto, Library, persistence, import/export, MIDI rendering, and every other catalogue-scale surface. Implementation: Planned shared scale registry. UI: All applicable destinations. | Cross-feature integration matrix | End-to-end custom-scale workflow |

## Milestone 11 — Migration regression, performance/cancellation, accessibility, browser QA, and adversarial audit

Exit gate: All automated suites and required browser/accessibility viewports pass, every traceability row is evidenced and done, `/legacy` hashes match, and the final handoff is complete.

| Check | ID | Status | Deliverable and UI | Automated evidence | Browser evidence |
| --- | --- | --- | --- | --- | --- |
| [ ] | `DATA-004` | not started | Freeze schema-2 canonical envelopes and normalized entity-array tables: `ProjectEnvelopeV2` is exactly kind `melody-forge-project`, schema version `2`, and `project={rootProjectId, project, tables}`; `CandidateEnvelopeV2` is exactly kind `melody-forge-candidate`, schema version `2`, and `candidate={rootCandidateId, performanceId, tables}`. Candidate `performanceId` is nullable: null requires an empty `performanceSettings` table, while non-null requires exactly one matching strong performance row. Project tables are exactly `createStates`, `modeStates`, `historyGraphs`, `historyNodes`, `snapshots`, `melodyCandidates`, `melodyGenomes`, `transports`, `v1TimingProfiles`, `tonalTimelines`, `customScales`, `beats`, `performanceSettings`, `pairings`, `preferenceRecords`, `libraryItems`, `ratings`, `annotations`, `undoStates`, `migrationReceipts`; candidate tables are exactly `melodyCandidates`, `melodyGenomes`, `transports`, `v1TimingProfiles`, `tonalTimelines`, `customScales`, `performanceSettings`, `ratings`, `annotations`. Tables use that order; each array is embedded-ID Unicode-code-point sorted and duplicate-free; documented ID sets use the same order; domain arrays retain declared order; canonical bytes use codec-declared keys, envelope `kind`/`schemaVersion`/payload order, UTF-8 without BOM, and one trailing LF. Strict versioned decode rejects unknown keys/versions, duplicates, invalid order, persistence key/embedded-ID or identity mismatch, missing rows, unresolved strong references, unreachable extras, and any unregistered tagged payload while allowing only documented ancestry/external-lineage soft references; encode must strict decode-after-encode. A standalone candidate envelope contains only melody-candidate rating/annotation rows targeting its root candidate; each embedded row ID must recompute from its complete singleton identity fields, no extra row is allowed, the candidate carries no metadata ID collection, and row `projectId` is soft source-project evidence only at this boundary. Open-preview mutates nothing. Save or Seed into a target project deterministically substitutes the target project ID, recomputes singleton IDs, creates target-owned metadata rows, and adds only those rehomed IDs to the target project's root sets in one expected-revision transaction without adding project-local metadata IDs to or otherwise rewriting the globally reusable candidate row; an existing target singleton's local value/text wins with preview disclosure, absent rows use imported values, source rows/IDs never persist, and candidate identity remains unchanged. This M11 full-graph closure cannot be done until every table row, all six mode payloads, native and migrated snapshot/history payloads, Pair Lab evidence/lineage/state, and component/pairing bundle prerequisites have exact versioned codecs registered by their dependency milestones. Implementation: Planned complete schema-2 project/candidate codecs, all-table/payload registry, normalized closure traversal, and canonical encoder. UI: Project/candidate import preview, errors, export inspector, and Technical tables. | Exact envelope/root/all-table/all-payload key/order/subset, six-mode/native/migrated/Pair registration completeness, null-versus-single performance closure, candidate metadata target/embedded-ID-recompute/no-candidate-collection/source-soft/rehome/local-wins/global-row/identity semantics, Unicode ID sort, domain-order, canonical-byte golden, unknown/duplicate/version/key-ID/identity/reference/missing/unreachable/unregistered rejection, soft-reference, and decode-after-encode tests | Export twice after all modes/history/Pair Lab are populated, compare bytes, Open/Save/Seed candidate metadata into another project, reimport, and exercise malformed envelope/table/payload/order/reference failures without replacing state |
| [ ] | `PERS-002` | not started | Close whole-product schema-2 durability only after every exact entity and registered payload schema exists: the versioned IndexedDB adapter is the sole durable authority for complete projects, candidates and Pair Lab components/bundles, Library, all six workflow populations/archives/fronts/halls, normalized branch history/snapshots, undo, ratings, annotations, locks, preferences, migration receipts, and active-project metadata. Complete save/read-back/reload traverses and validates the exact full strong-reference closure for every kind; no capacity threshold, heuristic, unregistered row, opaque payload, or failure may redirect domain data to localStorage, and the lone V2 preference record never becomes domain authority. `installAndActivateProject` accepts exactly a complete graph, `expectedStoredRevision` as a number or null, complete `expectedPriorActive` metadata or null, and reason `native-create` or `confirmed-schema2-replace`: null requires target absence and graph revision 1, numeric requires the exact stored target revision and graph revision one greater, and native creation permits only null/revision 1. One transaction CAS-checks both expectations, validates and writes the complete graph, removes obsolete rows owned only by the replaced target but never global Library rows, strict-reads the write, and only then installs exact active metadata; any failure rolls back everything, and schema-2 import cannot invoke it before preview plus explicit Replace. `activateProject` accepts exactly project ID, expected target revision, and expected prior active metadata; it writes no project row, strict-loads the complete inactive graph at that revision, CAS-checks prior active, and swaps only active metadata. Implementation: Planned complete IndexedDB repository/adapter, all-entity transaction/read-back loaders, explicit install/activate commands, and bootstrap restore. UI: Storage status/recovery, Open project, native-create, and confirmed Replace UI across every destination/mode. | Repository routing and exact save/read-back/reload for every final entity/payload kind, all-six-mode/history/Pair closure, install absent/present and native/replacement revision rules, active switch without graph writes, prior-active/target CAS, obsolete target-row cleanup/global-Library retention, strict read-back, preview/confirmation gate, rollback boundaries, no-threshold/no-domain-localStorage, and bootstrap authority tests | Populate every mode/history/Pair/Library kind, save/reload, create/switch/replace same- and different-ID projects, inspect sole authority and exact restoration, then exercise stale/quota/blocked/read-back recovery without fallback |
| [ ] | `LONG-001` | not started | Long Islands, Map, Pareto, and Pair Lab operations do not block playback or interaction. Implementation: Planned task runner/worker adapters. UI: Progress surfaces. | Performance/concurrency tests | Interact/play during operations |
| [ ] | `LONG-002` | not started | Long operations show progress and support cancellation. Implementation: Planned cancellable task protocol. UI: Mode progress UI. | Progress/cancel tests | Cancel each long mode |
| [ ] | `LONG-003` | not started | Ignore stale cancelled results, use bounded attempts, and add workers only if profiling shows main-thread blocking. Implementation: Planned task tokens/limits; performance ADR if needed. UI: Progress/error UI. | Stale-result/termination/perf tests | Cancel/restart stress workflow |
| [ ] | `QA-001` | in progress | Full V1 suite remains passing and `/legacy` remains byte-identical. Implementation: Existing and migration test suites. UI: Existing workflows. | Pre-change full suite and current hash guard pass; every later/final gate remains | V1 desktop/mobile regression passed; final rerun remains |
| [ ] | `QA-002` | in progress | Regression fixtures prove V1 projects, favorites, histories, arbitrary lengths, JSON, MIDI, retained payloads, and baseline screenshots remain usable with exact stored pitches/event timing. Implementation: `src/test/fixtures/v1/`; V1 migration/equivalence/coordinator tests. UI: Project import/Library/history. | Ten fixture/artifact tests plus golden V1 conversion/equivalence and headless bootstrap tests pass, including five-beat/non-480/mixed profiles, full payload manifest, and six screenshot hashes; migrated V2 UI remains | V1 import/reload/history/favorite audition passed; V2 migrated UI remains |
| [ ] | `QA-003` | in progress | Migration never silently clears storage and retains interpretable algorithm versions and stable scale IDs. Implementation: `src/persistence/v2/bootstrapCoordinator.test.ts`; migration/kernel tests. UI: Recovery UI. | Success, retry, mismatch, conflict, unavailable-IDB, corrupt-source, version/ID, and byte-nonoverwrite tests pass; recovery UI remains | Seed/reload old storage |
| [ ] | `QA-004` | in progress | Transport tests cover tick/bar/beat conversion in common, compound, odd, and partial meters. Implementation: `src/domain/transport/*.test.ts`. UI: N/A. | Required-meter fixtures and 1,000 deterministic partial-loop property cases pass; wider adapter workflows remain | Representative meter workflows remain |
| [ ] | `QA-005` | in progress | Transport tests cover tempo change, swing, loop boundaries, and zero drift within defined tolerance. Implementation: `src/domain/transport/`; planned scheduler tests. UI: N/A. | Exact rational swing/inverse/loop tests pass; fake-audio/live gates remain | Live tempo/swing/loop run remains |
| [ ] | `QA-005A` | in progress | Pure score/performed tick transforms and loop endpoints have exactly zero rational tick error across 1,024 loops for straight and supported swing values, including incomplete final pairs. Implementation: `src/domain/transport/transport.property.test.ts`; `swing.test.ts`. UI: Technical timing evidence. | 1,000 deterministic meter/swing/tail cases assert exact rational inverse/endpoints and 1,024-loop wrap | Browser technical evidence remains |
| [ ] | `QA-005B` | not started | Fake-audio tick-to-seconds scheduling has absolute timestamp error at most `1e-9` seconds and 1,024-loop phase growth no more than `1e-9` seconds above first-loop error, including a tempo change. Implementation: Planned fake-clock/audio scheduler tests. UI: Technical timing evidence. | Deterministic fake-audio tolerance and phase-growth tests | Inspect recorded fake-clock evidence |
| [ ] | `QA-005C` | not started | Instrumented browser playback keeps melody, beat, effects, and playhead targets within one canonical score tick for 128 loop boundaries including mid-play tempo change; callback jitter up to 25 ms is reported separately and may not grow phase or relax alignment. Implementation: Planned Playwright/audio instrumentation. UI: Shared transport/piano roll. | Instrumentation parser/alignment assertions | Recorded 128-loop real-browser timing run |
| [ ] | `QA-006` | not started | Playback tests cover melody/beat/playhead synchronization, stop/replay cleanup, and no ghost schedules/duplicate graphs. Implementation: Planned audio integration tests. UI: Shared transport/roll. | Fake/offline lifecycle tests | Stress workflow |
| [ ] | `QA-007A` | not started | Verify beat determinism across repeated generation, seed replay, reload, UI reordering, and independent melody streams. Implementation: Planned beat property/integration tests. UI: Beat controls/technical provenance. | Determinism and independence property matrix | Repeat/reload/reorder beat workflow |
| [ ] | `QA-007B` | not started | Verify every beat variation remains recognizably related while allowed lanes/onsets/accents/fills change by configured strength. Implementation: Planned beat variation tests. UI: Beat controls/drum lane. | Family/strength/phenotype fixture tests | Compare previous/current/next variations |
| [ ] | `QA-007C` | not started | Verify every beat family/generator is grouping-aware in common, compound, odd, and partial meters rather than truncating 4/4. Implementation: Planned meter-aware beat tests. UI: Beat controls/drum lane. | Required meter×family matrix | Audition/inspect each required meter family |
| [ ] | `QA-007D` | not started | Verify final-bar fills honor frequency, grouping, lane legality, and exact half-open loop endpoint for complete and partial bars. Implementation: Planned fill property tests. UI: Drum lane/fill summary. | Frequency/boundary/property matrix | Trigger and inspect complete/partial fills |
| [ ] | `QA-007E` | not started | Verify full-beat/layer locks and ordinary-mode shared-beat policy preserve exact bytes and prevent drum preference contamination. Implementation: Planned beat lock/comparison tests. UI: Beat controls/all comparison modes. | Lock×mode identity and shared-beat matrix | Lock/share across ordinary modes and Pair Lab |
| [ ] | `QA-007F` | not started | Verify all six curated voices switch performance on identical melody/beat candidate IDs/events without regeneration. Implementation: Planned voice/audio-plan tests. UI: Voice selector. | Preset/identity/independence matrix | Switch all voices while auditioning same candidate |
| [ ] | `QA-007G` | not started | Verify reverb/delay and every melody/percussion/effects/master node/schedule dispose on stop, replacement, replay, export, and unmount. Implementation: Planned audio graph lifecycle tests. UI: Sound inspector/transport. | Fake/offline graph and ghost-node stress tests | Stop/replay/switch/effect stress workflow |
| [ ] | `QA-007H` | not started | Verify multitrack MIDI has exact tempo/meter/ticks, separate melody/percussion tracks/channels, legal drum notes, and deterministic bytes/parse result. Implementation: Planned MIDI export parser tests. UI: Export inspector. | Parsed MIDI golden/property matrix | Download/reimport/inspect multitrack MIDI |
| [ ] | `QA-007I` | not started | Verify melody-only and combined WAV exact duration, deterministic frame count, non-silence when events sound, fixed tails, headroom, and no clipping. Implementation: Planned offline WAV tests. UI: Export inspector/result. | Header/frame/RMS/peak/determinism matrix | Export twice, play, and inspect both scopes/tails |
| [ ] | `QA-007` | not started | Beat/voice/export verification umbrella is done only when QA-007A through QA-007I are independently evidenced. Implementation: Planned beat/audio/export suites. UI: Beat/sound/export UI. | Child-ID completeness audit | Child browser-evidence audit |
| [ ] | `QA-008A` | not started | Verify pitch/onset/duration geometry, bar/beat/subdivision grid, shared horizontal ruler, zoom, clipping, and partial-loop endpoints from canonical ticks. Implementation: Planned roll geometry tests. UI: Piano roll/miniatures. | Exact geometry fixtures across viewports/meters | Visual inspect common/odd/partial timelines |
| [ ] | `QA-008B` | not started | Verify explicit rests have correct geometry, table rows, labels, selection, and non-sounding semantics. Implementation: Planned roll rest tests. UI: Piano roll/Events table. | Rest geometry/semantic/action tests | Inspect and lock rest fixtures |
| [ ] | `QA-008C` | not started | Verify tonic/scale segments, borrowing, and modulation boundaries cover the loop exactly and align between tonal track and events. Implementation: Planned tonal-track visualization tests. UI: Piano roll tonal track. | Coverage/geometry/attribution tests | Inspect borrowed/modulating fixture |
| [ ] | `QA-008D` | not started | Verify playhead position derives from transport ticks and remains aligned through play/pause/resume/loop/tempo/candidate replacement. Implementation: Planned playhead integration tests. UI: Piano roll. | Controller/geometry/synchronization tests | Recorded playback interaction run |
| [ ] | `QA-008E` | not started | Verify exact Parent A and Parent B attribution in child-only, overlays, side-by-side, and textual change list. Implementation: Planned inheritance visualization tests. UI: Piano roll view switcher. | Source-byte/geometry/semantic tests | Breed and inspect every inheritance view |
| [ ] | `QA-008F` | not started | Verify mutation attribution names the exact event/component/operator and uses non-colour semantics in roll, cards, table, and inspector. Implementation: Planned mutation provenance visualization tests. UI: Roll/cards/Events table/inspector. | Cross-view attribution parity tests | Mutate and inspect by keyboard/pointer |
| [ ] | `QA-008G` | not started | Verify deterministic remap, adaptation, and repair attribution names changed ticks/degrees/pitches, before/after values, and source ruler without visual stretching. Implementation: Planned provenance/difference visualization tests. UI: Roll/difference/inspector. | Exact diff, ruler, and attribution tests | Adapt/remap/repair and inspect |
| [ ] | `QA-008H` | not started | Verify the visible sortable Events table and concise chart summary provide complete operable textual alternatives without hover. Implementation: Planned accessible event-table tests. UI: Adjacent/below piano roll. | Data-parity, keyboard, sort, accessible-name tests | Keyboard-only and screen-reader-oriented workflow |
| [ ] | `QA-008` | not started | Piano-roll verification umbrella is done only when QA-008A through QA-008H are independently evidenced. Implementation: Planned visualization suites. UI: Piano roll. | Child-ID completeness audit | Child browser-evidence audit |
| [ ] | `QA-009A` | not started | Verify cross-cardinality degree mappings over all required scale families and all 12 tonics preserve root-relative octave/contour intent and legal range. Implementation: Planned mapping property tests. UI: Adaptation preview/provenance. | Required family×tonic exhaustive/property matrix | Representative family/tonic adaptations |
| [ ] | `QA-009B` | not started | Verify every nearest-degree, spelling-independent, relationship, register-repair, and mapping tie uses the documented stable rule and ID byte order. Implementation: Planned tonal tie-break tests. UI: Adaptation/relationship explanations. | Exact tie fixtures/permutation tests | Inspect deliberately tied adaptations |
| [ ] | `QA-009C` | not started | Verify `tonal-relationship-v2.0.0` symmetry/range/identity and every shared-PC, set-distance, modal-family, alteration, tonic/fifths component. Implementation: Planned relationship property tests. UI: Relationship graph/list/inspector. | Exhaustive catalogue/custom metric matrix | Inspect component explanations and pitch-class diffs |
| [ ] | `QA-009D` | not started | Verify relative-pitch locks preserve extended degree/contour bytes across tonic, scale, borrowing, modulation, and all mode operations. Implementation: Planned tonal lock matrix. UI: Locks/tonal UI. | Relative-lock×operator×mode property matrix | Representative relative-lock workflows |
| [ ] | `QA-009E` | not started | Verify absolute-pitch locks preserve sounding MIDI exactly and reject every incompatible tonal operation with actionable conflict evidence. Implementation: Planned tonal lock matrix. UI: Locks/conflict preview. | Absolute-lock×operator×mode rejection/preservation matrix | Attempt incompatible operations and inspect |
| [ ] | `QA-009F` | not started | Verify borrowing amount zero produces no borrowed events or anonymous merged scale and remains deterministic across generation/evolution. Implementation: Planned borrowing property tests. UI: Borrowing controls/roll. | Zero boundary and seed-replay tests | Set zero, generate/evolve, inspect |
| [ ] | `QA-009G` | not started | Verify donor-exclusive legality, shared-as-base classification, donor/resolution provenance, register validity, and base-or-borrowed invariant for every event. Implementation: Planned borrowing legality/property tests. UI: Roll/tonal track/inspector. | All-tonic/donor legality/provenance matrix | Generate and inspect borrowed events |
| [ ] | `QA-009H` | not started | Verify meter-aware strong-beat protection and configurable borrowed-note resolution windows in common, compound, odd, and partial meters. Implementation: Planned borrowing timing tests. UI: Borrowing controls/roll. | Meter×amount×window property matrix | Compare protected/unprotected strong beats and resolution |
| [ ] | `QA-009I` | not started | Verify tonal timeline has one context per tick, non-overlap, bar-aligned modulation, valid partial-bar handling, add/remove/move/destination operations, and actionable short-phrase failure. Implementation: Planned modulation property tests. UI: Tonal track/controls. | Coverage/boundary/operator/short-loop matrix | Exercise every modulation action and short-loop error |
| [ ] | `QA-009J` | not started | Verify crossover exchanges complete tonal sections, preserves coverage/locks/provenance, and can deterministically return to opening context. Implementation: Planned tonal crossover tests. UI: Breed inheritance/tonal track. | Section-boundary/source/coverage/lock tests | Breed segmented parents and inspect |
| [ ] | `QA-009K` | not started | Verify custom-scale tonic/cardinality/uniqueness/degeneracy, stable content IDs/catalogue aliases, operator validity, exhaustive masks, and transposition invariance. Implementation: Planned custom-scale property tests. UI: Scale editor/inspector/errors. | 4,096-mask/operator/identity/transposition matrix | Create/mutate/import valid and invalid masks |
| [ ] | `QA-009L` | not started | Verify custom-scale project/candidate/component serialization, persistence/reload, remap/cross-scale use, and every CSC-017–CSC-029 integration surface. Implementation: Planned custom-scale integration/I/O tests. UI: All scale surfaces. | Round-trip/reference and surface-parity matrix | End-to-end custom-scale create/evolve/save/export/import |
| [ ] | `QA-009` | not started | Tonal verification umbrella is done only when QA-009A through QA-009L are independently evidenced. Implementation: Planned tonal/property suites. UI: Tonal UI. | Child-ID completeness audit | Child browser-evidence audit |
| [ ] | `QA-010A` | not started | Verify complete Breed semantics: one/two parents, independent crossover, mutation, elites, dedup/diversity, contributions, zero mutation, incompatible adaptation, persistence, and replay. Implementation: Planned Breed deterministic/property tests. UI: Breed workspace. | Breed contract matrix | Full Breed browser workflow |
| [ ] | `QA-010B` | not started | Verify complete Drift semantics: same-anchor bands/formulas, bias, explicit promotion, reversible trail/branching, persistence, and replay. Implementation: Planned Drift deterministic/property tests. UI: Drift workspace. | Drift contract matrix | Full Drift browser workflow |
| [ ] | `QA-010C` | not started | Verify complete Islands semantics: isolated streams/policies, one/all atomic actions, migration, global diversity, pins, persistence, cancellation, and replay. Implementation: Planned Islands deterministic/property tests. UI: Islands workspace. | Islands contract matrix | Full Islands browser workflow |
| [ ] | `QA-010D` | not started | Verify complete Map semantics: descriptor axes, 8×8 binning, replacement/queues/pins, rebin losslessness, regions, metrics, table parity, persistence, and replay. Implementation: Planned Map deterministic/property tests. UI: Map grid/list. | Map contract matrix | Full Map browser workflow |
| [ ] | `QA-010E` | not started | Verify complete Pareto semantics: objectives/directions/targets/ranges, separate constraints, sorting/crowding/reproduction, display independence, table parity, persistence, and replay. Implementation: Planned Pareto deterministic/property tests. UI: Pareto chart/table. | Pareto contract matrix | Full Pareto browser workflow |
| [ ] | `QA-010F` | not started | Verify complete Pair Lab semantics: independent populations/actions/locks/audition/favorites, deterministic transition/collaborators/hall/evidence, JSON scopes, persistence, atomicity, and replay. Implementation: Planned Pair Lab deterministic/property tests. UI: Pair Lab workspace. | Pair Lab contract matrix | Full Pair Lab browser workflow |
| [ ] | `QA-010G` | not started | Verify every applicable lock against every generator, transform, crossover, mutation, survivor/migrant/immigrant path, and all six mode operations with byte-exact preservation or actionable rejection. Implementation: Planned global lock matrix. UI: Locks/conflict UI. | Lock×operation exhaustive/property matrix | Representative lock workflows in every mode |
| [ ] | `QA-010H` | not started | Verify cancellation in every long Islands, Map, Pareto, Pair Lab, import, and WAV operation commits no partial creative/persistent state and leaves playback/UI responsive. Implementation: Planned cancellable-task integration tests. UI: Progress/cancel UI. | Operation×cancel-phase atomicity tests | Cancel each long workflow at multiple phases |
| [ ] | `QA-010I` | not started | Verify late worker/task results with cancelled tokens or changed starting-state hashes are ignored, cannot overwrite newer work, and replay identically regardless of completion order. Implementation: Planned stale-result/concurrency tests. UI: Progress/status UI. | Token/hash/permuted-completion tests | Cancel/restart stress workflow |
| [ ] | `QA-010` | not started | Evolution verification umbrella is done only when QA-010A through QA-010I are independently evidenced. Implementation: Planned mode suites. UI: Six mode workspaces. | Child-ID completeness audit | Child browser-evidence audit |
| [ ] | `QA-011A` | not started | Verify complete project JSON canonical round trip preserves all normalized entities/references, settings, seeds/versions, performance, ratings, annotations, history, and deterministic bytes. Implementation: Planned project codec tests. UI: Project import/export. | Golden/full-field/reference/hash round-trip tests | Export, replace, reload, and compare project |
| [ ] | `QA-011B` | not started | Verify candidate, melody-component, beat-component, and complete-pairing JSON envelopes each round trip independently without identity/lineage/evidence ownership leakage. Implementation: Planned individual codec tests. UI: Candidate/Pair Lab/Library import/export. | Per-envelope golden/hash/reference/isolation tests | Export/import/open each envelope kind |
| [ ] | `QA-011C` | in progress | Verify committed IndexedDB/localStorage state reloads exact current focus, transport, performance, candidates, Library, and migration backups without silent loss. Implementation: `src/persistence/v2/bootstrapCoordinator.test.ts`; authority/registered-boundary tests; planned complete repository tests. UI: Recovery/status UI. | M2 fresh/migrated graph, active precedence, crash/retry, pending/verified receipt, and recovery-byte reload tests pass; later full-state/quota/UI matrix remains | Reload/private/quota recovery workflows |
| [ ] | `QA-011D` | not started | Verify all six mode states and complete branch graph, active path, snapshots, per-node settings/seeds/ratings/annotations, undo/redo scope, and archives/fronts restore exactly. Implementation: Planned project-state/history codec tests. UI: Mode workspaces/history. | Full mode/branch fixture round trips | Build branches/mode state, reload, and compare |
| [ ] | `QA-011E` | not started | Verify custom-scale masks/IDs/aliases/provenance/relationships/references survive every project/item repository and JSON path. Implementation: Planned persistence/I/O scale tests. UI: Library/project/import/export. | Custom-scale storage/codec/reference matrix | Save/reload/export/import custom scales |
| [ ] | `QA-011F` | not started | Verify complete tonal timelines, borrowing rules/events/resolution, modulation segments, formula versions, and exact tick coverage survive persistence and JSON. Implementation: Planned tonal persistence/I/O tests. UI: Tonal track/inspector/import/export. | Tonal full-field round-trip and invariant tests | Save/reload/export/import tonal fixture |
| [ ] | `QA-011G` | not started | Verify MIDI preview/adaptation reports tracks, metadata, exact timing, polyphony, extraction, quantization, phrase/tonal/register proposals, every destructive change, confirmation, cancellation, and source preservation. Implementation: Planned MIDI import integration tests. UI: MIDI import wizard. | Parser/proposal/strategy/confirmation/failure matrix | Full mono/multitrack/polyphonic/cancel workflows |
| [ ] | `QA-011H` | not started | Verify melody MIDI, multitrack MIDI, melody WAV, and combined WAV use shared transport/render plans and meet exact metadata/timing/audio requirements. Implementation: Planned export integration tests. UI: Export inspector/result. | Parsed MIDI and rendered WAV cross-adapter matrix | Download/play/reimport every output |
| [ ] | `QA-011I` | not started | Verify malformed, wrong-kind, invalid-reference/hash/invariant, unsupported/future, incompatible, empty, and oversized inputs fail actionably before atomic commit and never replace active state. Implementation: Planned staged import failure tests. UI: Import/recovery errors. | Input-kind×failure-phase×nonreplacement matrix | Exercise representative failure for JSON and MIDI |
| [ ] | `QA-011` | not started | Persistence/import/export verification umbrella is done only when QA-011A through QA-011I are independently evidenced. Implementation: Planned persistence/I/O suites. UI: Project/Library/import/export. | Child-ID completeness audit | Child browser-evidence audit |
| [ ] | `QA-012` | not started | Browser run covers Legacy and Modern Create, MIDI import, beat variation, instrument change, and Breed. Implementation: Playwright specs. UI: Create/Evolve. | E2E spec | Recorded run/screenshots |
| [ ] | `QA-013` | not started | Browser run covers Drift, Islands migration, Map population, Pareto front, and Pair Lab. Implementation: Playwright specs. UI: Evolve/Explore. | E2E spec | Recorded run/screenshots |
| [ ] | `QA-014` | not started | Browser run covers tournament, continuous audition, history fork, Library reuse, remapping, continuation, and answering phrase. Implementation: Playwright specs. UI: All destinations. | E2E spec | Recorded run/screenshots |
| [ ] | `QA-015` | not started | Browser run covers project/candidate JSON, melody MIDI, multitrack MIDI, WAV, reload, and exact restored state. Implementation: Playwright specs. UI: Project/export UI. | E2E spec | Download/import/reload evidence |
| [ ] | `QA-016` | not started | Inspect and fail on unexpected console errors/warnings. Implementation: Playwright harness. UI: Whole app. | Console assertion | Every browser run |
| [ ] | `QA-017` | not started | Verify 1440×900. Implementation: Playwright project/artifact. UI: Whole app. | E2E project | Screenshots/workflow |
| [ ] | `QA-018` | not started | Verify 1280×720. Implementation: Playwright project/artifact. UI: Whole app. | E2E project | Screenshots/workflow |
| [ ] | `QA-019` | not started | Verify 1024×768. Implementation: Playwright project/artifact. UI: Whole app. | E2E project | Screenshots/workflow |
| [ ] | `QA-020` | not started | Verify 768 px tablet width. Implementation: Playwright project/artifact. UI: Whole app. | E2E project | Screenshots/workflow |
| [ ] | `QA-021` | not started | Verify 390×844 mobile. Implementation: Playwright project/artifact. UI: Whole app. | E2E project | Screenshots/workflow |
| [ ] | `QA-022` | not started | Verify 200% zoom. Implementation: Playwright/manual artifact. UI: Whole app. | Overflow/layout assertions | Recorded zoom workflow |
| [ ] | `QA-023` | not started | Verify keyboard-only navigation and equivalent visible controls. Implementation: Playwright/accessibility tests. UI: Whole app. | Focus/shortcut assertions | Recorded keyboard workflow |
| [ ] | `QA-024` | not started | Verify reduced-motion behavior. Implementation: CSS/component/E2E tests. UI: Whole app. | Media-query assertions | Reduced-motion workflow |
| [ ] | `QA-025` | not started | Verify colour-vision semantics or equivalent inspection. Implementation: Accessibility QA artifacts. UI: Whole app. | Semantic non-colour assertions | Simulation/inspection record |
| [ ] | `QA-026` | in progress | Every pure generator, transport, tonal, descriptor, mutation, lock, migration, and serialization invariant has reproducible property-based tests derived from `property/<suite-id>/v2`, at least 1,000 valid CI cases, and a replay fixture recording suite ID, root-seed digest, case index, and minimized counterexample; finite spaces are exhaustive where specified. Implementation: `src/domain/transport/transport.property.test.ts`; planned shared replay harness and remaining suites. UI: Technical test evidence. | First 1,000-case labelled transport suite passes; shared replay/minimization manifest and remaining domains remain | Representative replay UI evidence remains |
| [ ] | `A11Y-001` | not started | Meet WCAG 2.2 AA contrast with visible focus states and semantic labels. Implementation: Planned tokens/components. UI: Whole app. | Axe/contrast/semantic tests | Viewport/accessibility audit |
| [ ] | `A11Y-002` | not started | Provide at least 44×44 CSS-pixel touch targets. Implementation: Planned responsive CSS. UI: Interactive controls. | Geometry assertions | Mobile inspection |
| [ ] | `A11Y-003` | not started | Provide appropriate live-region announcements. Implementation: Planned status/announcer service. UI: Whole app. | Announcement tests | Screen-reader-oriented workflow |
| [ ] | `A11Y-004` | not started | Provide accessible chart/table alternatives for Map and Pareto. Implementation: Planned alternate views. UI: Explore. | Parity/accessibility tests | Keyboard-only workflows |
| [ ] | `A11Y-005A` | not started | Loading/working states name the operation, expose determinate completed/total or labelled indeterminate progress, keep safe navigation/playback available, and provide an accessible Cancel action when cancellable. Implementation: Planned loading/progress fixtures. UI: All long-operation surfaces. | Component/live-region/action tests | Recorded keyboard/mobile loading workflow |
| [ ] | `A11Y-005B` | not started | Empty states explain the minimum next action and expose an appropriate enabled primary action for no project, empty population/archive/front/Library, or silent export. Implementation: Planned empty-state fixtures. UI: All destinations/export. | Copy/action/accessible-name tests | Recorded empty-state screenshots/workflows |
| [ ] | `A11Y-005C` | not started | No-parent states name the missing parent/anchor/selection, focus or link the relevant control, and never leave an unexplained disabled primary action. Implementation: Planned prerequisite-state fixtures. UI: Breed/Drift/mode actions. | State/copy/focus tests | Keyboard no-parent recovery workflow |
| [ ] | `A11Y-005D` | not started | Incompatible-parent states preserve both sources, enumerate transport/tonal/timing mismatches, offer explicit adaptation preview/cancel, and announce that no content changed. Implementation: Planned compatibility-state fixtures. UI: Parent slots/adaptation preview. | Diff/copy/focus/nonmutation tests | Keyboard/mobile incompatible-parent workflow |
| [ ] | `A11Y-005E` | not started | Invalid-import states identify file/track/field/kind/version/size cause, state the applicable remedy/limit and that the current project was unchanged, and restore focus predictably. Implementation: Planned import-error fixtures. UI: JSON/MIDI import. | Error taxonomy/copy/focus/nonreplacement tests | Recorded malformed/oversized/future import workflows |
| [ ] | `A11Y-005F` | not started | Impossible-constraint states list exact conflicting locks/constraints and affected event IDs/ranges, offer Review locks/adaptation actions, and never silently weaken constraints. Implementation: Planned constraint-error fixtures. UI: Locks/mode errors. | Conflict-content/action/focus tests | Recorded impossible-constraint recovery workflow |
| [ ] | `A11Y-005G` | not started | Cancellation states announce requested/completed cancellation, distinguish ignored stale work, retain the last committed state, and expose a valid retry/next action without partial results. Implementation: Planned task-cancellation fixtures. UI: Progress/status surfaces. | Live-region/atomic-state/retry tests | Cancel each representative long operation |
| [ ] | `A11Y-005H` | not started | Generic audio, storage, export, validation, and unexpected application errors identify the affected object and state-change outcome, provide recovery, remain keyboard operable, and never rely on red alone. Implementation: Planned error-boundary/domain-error fixtures. UI: Whole application. | Error-boundary, copy, semantics, focus, and recovery tests | Recorded representative error screenshots/workflows |
| [ ] | `A11Y-005` | not started | Required state/accessibility verification umbrella is done only when A11Y-005A through A11Y-005H are independently evidenced. Implementation: Planned state fixtures/E2E. UI: All destinations. | Child-ID completeness audit | Child browser-evidence audit |
| [ ] | `A11Y-006` | not started | Capture screenshots for every primary destination and every evolutionary mode. Implementation: Planned screenshot manifest. UI: All destinations/modes. | Artifact manifest test | Retained screenshots |
| [ ] | `A11Y-007` | not started | Before completion fix clipping, overflow, crushed controls, unstable cards, clipped popovers, layout shifts, stale playback, confusing hierarchy, and console errors. Implementation: Whole UI/audio. UI: Whole app. | Regression assertions | Final visual/adversarial audit |
| [ ] | `V1-004` | in progress | Keep every file under `/legacy` byte-for-byte immutable through final completion. Implementation: `/legacy`; `src/persistence/v1Fixtures.test.ts`. UI: N/A. | Current path/hash guard passes; final sorted comparison remains mandatory | N/A |
| [ ] | `DOC-009` | in progress | Update `PROJECT_STATUS.md` after every substantial work unit and before ending each turn. Implementation: `PROJECT_STATUS.md`. UI: N/A. | Git/history review | N/A |
| [ ] | `DOC-010` | not started | Keep the repository runnable after every milestone and fix regressions before continuing. Implementation: Whole repository. UI: Whole application. | Per-milestone check suite | Per-milestone smoke workflow |
| [ ] | `GOV-001` | not started | Extend the working V1 application; do not rewrite or switch its identity merely because scope grew. Implementation: Existing `src/`; planned ADRs in `docs/adr/`. UI: Existing application, incrementally expanded. | Full V1 regression suite plus architecture-boundary tests | Side-by-side V1/V2 workflow checks |
| [ ] | `GOV-002` | not started | Preserve Legacy and Modern generation, deterministic seeds, playback, evolution, favorites, history, project import/export, MIDI, documentation, and tests unless an explicit V2 requirement changes the interface or schema. Implementation: Planned compatibility adapters and version dispatch. UI: All migrated V1 workflows. | Golden V1 fixtures and regression tests | Legacy/Modern end-to-end compatibility run |
| [ ] | `GOV-003` | not started | Every feature needs a real domain representation and working algorithm; placeholders, mock implementations, disconnected demos, TODO substitutes, hidden/disabled “coming soon” controls, and “experimental” labels do not count. Implementation: All feature modules; final source audit. UI: All feature surfaces. | Traceability audit and no-placeholder scan | Exercise every enabled feature |
| [ ] | `GOV-004` | not started | Every applicable feature needs complete controls and visualization. Implementation: Planned feature/UI modules. UI: Create, Evolve, Explore, Library. | Component/integration tests | Real-browser workflow per feature |
| [ ] | `GOV-005` | not started | Every applicable feature needs persistence and import/export. Implementation: Planned versioned persistence and I/O adapters. UI: Project, Library, and export surfaces. | Round-trip and reload tests | Reload plus export/import workflows |
| [ ] | `GOV-006` | not started | Every feature needs deterministic automated tests, documentation, and real-browser verification. Implementation: Test suites and `docs/`. UI: N/A. | Requirement-to-test audit | Requirement-to-browser-evidence audit |
| [ ] | `GOV-007` | in progress | Own inspect, design, implementation, migration, test, visual inspection, fixes, and documentation end-to-end without a plan-approval pause. Implementation: Project process; `PROJECT_STATUS.md`. UI: N/A. | Status/traceability audit | Browser evidence audit |
| [ ] | `GOV-008` | in progress | Use only safe local changes; never push/deploy, rewrite history, delete user work, create accounts, use credentials, or expand into unrelated work. Implementation: Repository workflow. UI: N/A. | Git/worktree audit | N/A |
| [ ] | `GOV-009` | in progress | Ask only for product-identity, external, or destructive decisions; resolve ordinary uncertainty with the smallest robust documented interpretation. Implementation: Product/architecture docs. UI: N/A. | Decision-log review | N/A |
| [ ] | `GOV-010` | in progress | Use bounded subagents where useful while retaining one integration owner and avoiding overlapping edits. Implementation: Implementation process. UI: N/A. | Worktree/integration review | N/A |
| [ ] | `FIN-001` | not started | At every milestone run relevant checks, update traceability/status, keep old workflows usable, checkpoint safely, and fix regressions before continuing. Implementation: Project process/status. UI: Whole app. | Milestone evidence audit | Milestone smoke run |
| [ ] | `FIN-002` | in progress | If interrupted, leave repository runnable and next action mechanically discoverable at first incomplete checklist item. Implementation: `PROJECT_STATUS.md` and plan. UI: N/A. | Recovery-state audit | N/A |
| [ ] | `FIN-003` | not started | Completion requires every traceability row done with implementation, tests, and browser evidence; no named feature scaffolded/mocked/hidden/disabled/postponed. Implementation: This matrix/status/source audit. UI: Whole app. | Final traceability audit | Full browser suite |
| [ ] | `FIN-004` | not started | Completion requires coherent Create/Evolve/Explore/Library, six distinct modes, synchronized melody/beat/transport/playhead/MIDI/WAV, complete tonal evolution, and exact locks. Implementation: Whole product. UI: Whole app. | Full automated suite | Full browser suite |
| [ ] | `FIN-005` | not started | UI must explain changes, frozen state, required selection, and the next primary action. Implementation: Planned explanatory UI. UI: All modes. | Copy/state tests | Usability audit |
| [ ] | `FIN-006` | not started | Completion requires no unresolved console errors, corrupted migrations, invalid saved state, ghost playback, or known hard-invariant violation. Implementation: Whole repository. UI: Whole app. | Full suite/adversarial tests | Final stress audit |
| [ ] | `FIN-007` | not started | Perform an independent adversarial audit against every requirement ID and fix gaps rather than merely report them. Implementation: Audit report/status. UI: Whole app. | Independent ID audit | Independent browser audit |
| [ ] | `FIN-008` | not started | Stop after all criteria are met; do not add unrelated features merely to consume capacity. Implementation: Project process. UI: N/A. | Scope audit | N/A |
| [ ] | `HAND-001` | not started | Final handoff reports what changed and key musical/architectural decisions. Implementation: Final report/docs. UI: N/A. | Report checklist | N/A |
| [ ] | `HAND-002` | not started | Final handoff reports migration behavior and exact run/verification commands/results. Implementation: Final report/docs. UI: N/A. | Report checklist | N/A |
| [ ] | `HAND-003` | not started | Final handoff reports browser/accessibility QA, representative screenshots, and deterministic seeds worth auditioning. Implementation: Final report/docs. UI: N/A. | Report/artifact checklist | Artifact links |
| [ ] | `HAND-004` | not started | Final handoff reports only genuine limitations outside goal and confirms all V2 rows complete and `/legacy` unchanged. Implementation: Final report/docs. UI: N/A. | Final matrix/hash audit | N/A |
